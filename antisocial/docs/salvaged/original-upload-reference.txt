:root { /* Base */ --bg: #0A0908; /* near-black, warm undertone so gold doesn't look sickly against blue-black */ --surface-1: #14100F; --surface-2: #1E1815; --border: rgba(255, 255, 255, 0.07); /* Text */ --text-primary: #F2EDE4; --text-secondary: #A79E93; /* Accents — gold is primary, silver is secondary, red is RARE */ --accent-gold: #D4AF37; /* primary CTA, active states, highlights */ --accent-gold-hover: #E4C158; --accent-silver: #C7CBD1; /* secondary actions, links, quiet emphasis */ --accent-silver-hover: #DBDEE2; /* Danger — deep, desaturated blood red. NEVER decorative. */ --danger: #7A1F1F; --danger-hover: #8F2626; --danger-text: #E8B4B4; /* readable-on-dark variant, for error copy */ /* Success — deliberately not green-on-green with gold; muted, low-saturation */ --success: #4C7A3C; /* Geometry / motion — unchanged, no reason to touch these */ --radius-sm: 6px; --radius-md: 8px; --radius-lg: 10px; --motion-fast: 150ms; --motion-slow: 220ms; --font-body: "Inter", sans-serif; --font-display: "Space Grotesk", sans-serif; --font-mono: "JetBrains Mono", monospace; }import { pgTable, uuid, text, timestamp, pgEnum, uniqueIndex, jsonb, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; // ASSUMPTION: adjust import to your real path/export name export const memberTierEnum = pgEnum("member_tier", ["street", "block", "house"]); export const siteRoleEnum = pgEnum("site_role", ["member", "moderator", "admin"]); // Deliberately NOT including a "responder" tier here. That belongs to Section 19, // which is explicitly gated. Adding it now would be exactly the premature // scaffolding the dossier warns against. export const memberRoles = pgTable("member_roles", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id") .notNull() .references(() => blockMembers.id, { onDelete: "cascade" }) .unique(), tier: memberTierEnum("tier").notNull().default("block"), siteRole: siteRoleEnum("site_role").notNull().default("member"), houseTrustedAt: timestamp("house_trusted_at", { withTimezone: true }), houseTrustedBy: uuid("house_trusted_by").references(() => blockMembers.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), }); export const badges = pgTable("badges", { id: uuid("id").defaultRandom().primaryKey(), key: text("key").notNull().unique(), // e.g. "workshop_builder" name: text("name").notNull(), description: text("description").notNull(), icon: text("icon"), // an icon identifier string, not a file — no storage dependency createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }); export const memberBadges = pgTable( "member_badges", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), badgeId: uuid("badge_id").notNull().references(() => badges.id, { onDelete: "cascade" }), awardedAt: timestamp("awarded_at", { withTimezone: true }).notNull().defaultNow(), sourceEvent: text("source_event").notNull(), }, (t) => ({ onePerMember: uniqueIndex("member_badges_member_badge_uq").on(t.memberId, t.badgeId), }) ); export const memberEvents = pgTable("member_events", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), eventType: text("event_type").notNull(), // dot-namespaced, e.g. "workshop.project_completed" payload: jsonb("payload").notNull().default({}), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), });import { getBlockViewer } from "./block-session"; // ASSUMPTION: returns `{ id: string; ... } | null` import { db } from "@/lib/db"; import { memberRoles } from "@/lib/db/schema/member-roles"; import { eq } from "drizzle-orm"; export type MemberTier = "street" | "block" | "house"; const TIER_RANK: Record<MemberTier, number> = { street: 0, block: 1, house: 2 }; const ROLE_RANK = { member: 0, moderator: 1, admin: 2 } as const; export class AccessDeniedError extends Error { constructor( public reason: "unauthenticated" | "insufficient_tier" | "insufficient_role", message: string ) { super(message); this.name = "AccessDeniedError"; } } export async function getMemberTier(memberId: string): Promise<MemberTier> { const [row] = await db .select({ tier: memberRoles.tier }) .from(memberRoles) .where(eq(memberRoles.memberId, memberId)) .limit(1); // No row yet == a verified member who has never been tier-adjusted. // They already passed email verification, which is what "Block" means. Default to block, not street. return row?.tier ?? "block"; } export async function requireBlockAccess() { const viewer = await getBlockViewer(); if (!viewer) throw new AccessDeniedError("unauthenticated", "Sign in required."); return viewer; } export async function requireHouseAccess() { const viewer = await requireBlockAccess(); const tier = await getMemberTier(viewer.id); if (TIER_RANK[tier] < TIER_RANK.house) { throw new AccessDeniedError("insufficient_tier", "House access required."); } return viewer; } export async function requireSiteRole(minRole: "moderator" | "admin") { const viewer = await requireBlockAccess(); const [row] = await db .select({ siteRole: memberRoles.siteRole }) .from(memberRoles) .where(eq(memberRoles.memberId, viewer.id)) .limit(1); const current = row?.siteRole ?? "member"; if (ROLE_RANK[current] < ROLE_RANK[minRole]) { throw new AccessDeniedError("insufficient_role", `${minRole} role required.`); } return viewer; }import { db } from "@/lib/db"; import { memberEvents, memberBadges, badges } from "@/lib/db/schema/member-roles"; import { eq } from "drizzle-orm"; type BadgeRule = { eventType: string; badgeKey: string; condition?: (payload: Record<string, unknown>) => boolean; }; // Add a rule here when a new feature wants to award a badge. // Features never touch member_badges directly — they emit an event and stop caring. const BADGE_RULES: BadgeRule[] = [ { eventType: "workshop.project_completed", badgeKey: "workshop_builder" }, { eventType: "arcade.streak_hit", badgeKey: "arcade_7_day_streak", condition: (p) => typeof p.streakLength === "number" && p.streakLength >= 7, }, ]; export async function emitMemberEvent( memberId: string, eventType: string, payload: Record<string, unknown> = {} ): Promise<void> { await db.insert(memberEvents).values({ memberId, eventType, payload }); const matching = BADGE_RULES.filter( (r) => r.eventType === eventType && (!r.condition || r.condition(payload)) ); for (const rule of matching) { const [badge] = await db.select().from(badges).where(eq(badges.key, rule.badgeKey)).limit(1); if (!badge) { // Loud, not silent: a rule referencing an unseeded badge is a config bug, // not something to swallow quietly. console.warn( `[roles/events] badge rule references unseeded badge key "${rule.badgeKey}" — seed it or remove the rule.` ); continue; } await db .insert(memberBadges) .values({ memberId, badgeId: badge.id, sourceEvent: eventType }) .onConflictDoNothing({ target: [memberBadges.memberId, memberBadges.badgeId] }); } }import { pgTable, uuid, text, timestamp, pgEnum } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const moderationFlagStatusEnum = pgEnum("moderation_flag_status", [ "pending", "reviewing", "actioned", "dismissed", ]); export const moderationFlags = pgTable("moderation_flags", { id: uuid("id").defaultRandom().primaryKey(), contentType: text("content_type").notNull(), // "block_post" | "block_reply" | future feature keys contentId: uuid("content_id").notNull(), reason: text("reason").notNull(), reportedBy: uuid("reported_by").references(() => blockMembers.id), status: moderationFlagStatusEnum("status").notNull().default("pending"), reviewerId: uuid("reviewer_id").references(() => blockMembers.id), reviewedAt: timestamp("reviewed_at", { withTimezone: true }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), });import { db } from "@/lib/db"; import { moderationFlags } from "@/lib/db/schema/moderation"; export type FlaggableContentType = "block_post" | "block_reply"; export async function flagForReview(params: { contentType: FlaggableContentType; contentId: string; reason: string; reportedBy?: string; }): Promise<void> { await db.insert(moderationFlags).values({ contentType: params.contentType, contentId: params.contentId, reason: params.reason, reportedBy: params.reportedBy ?? null, status: "pending", }); }import { pgTable, uuid, text, timestamp, integer, pgEnum, uniqueIndex, index, sql, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const blockPostStatusEnum = pgEnum("block_post_status", ["published", "flagged", "removed"]); export const blockPosts = pgTable( "block_posts", { id: uuid("id").defaultRandom().primaryKey(), authorId: uuid("author_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), sectionKey: text("section_key").notNull(), // validated against a fixed enum in the API layer title: text("title").notNull(), body: text("body").notNull(), tags: text("tags").array().notNull().default(sql`'{}'::text[]`), status: blockPostStatusEnum("status").notNull().default("published"), cheerCount: integer("cheer_count").notNull().default(0), replyCount: integer("reply_count").notNull().default(0), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), deletedAt: timestamp("deleted_at", { withTimezone: true }), }, (t) => ({ feedOrderIdx: index("block_posts_feed_idx").on(t.createdAt), sectionIdx: index("block_posts_section_idx").on(t.sectionKey, t.createdAt), }) ); export const blockPostReplies = pgTable( "block_post_replies", { id: uuid("id").defaultRandom().primaryKey(), postId: uuid("post_id").notNull().references(() => blockPosts.id, { onDelete: "cascade" }), authorId: uuid("author_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), body: text("body").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), deletedAt: timestamp("deleted_at", { withTimezone: true }), }, (t) => ({ postOrderIdx: index("block_post_replies_post_idx").on(t.postId, t.createdAt) }) ); export const blockPostCheers = pgTable( "block_post_cheers", { id: uuid("id").defaultRandom().primaryKey(), postId: uuid("post_id").notNull().references(() => blockPosts.id, { onDelete: "cascade" }), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ onePerMember: uniqueIndex("block_post_cheers_uq").on(t.postId, t.memberId) }) );import { z } from "zod"; export const ALLOWED_SECTION_KEYS = ["general", "prayer", "wins", "questions", "announcements"] as const; export const createPostSchema = z.object({ sectionKey: z.enum(ALLOWED_SECTION_KEYS), title: z.string().trim().min(1, "Title required").max(200, "200 characters max"), body: z.string().trim().min(1, "Body required").max(10_000, "10,000 characters max"), tags: z.array(z.string().trim().min(1).max(30)).max(5, "5 tags max").default([]), }); export const createReplySchema = z.object({ body: z.string().trim().min(1, "Reply required").max(4_000, "4,000 characters max"), }); export type CreatePostInput = z.infer<typeof createPostSchema>; export type CreateReplyInput = z.infer<typeof createReplySchema>;import { db } from "@/lib/db"; import { blockPosts, blockPostReplies } from "@/lib/db/schema/block-posts"; import { and, eq, gte, sql } from "drizzle-orm"; export class RateLimitedError extends Error { constructor(public retryAfterSeconds: number) { super(`Rate limit exceeded. Try again in ${retryAfterSeconds}s.`); this.name = "RateLimitedError"; } } const POST_WINDOW_MIN = 10; const POST_MAX = 5; const REPLY_WINDOW_MIN = 5; const REPLY_MAX = 10; export async function assertPostRateLimit(authorId: string): Promise<void> { const windowStart = new Date(Date.now() - POST_WINDOW_MIN * 60_000); const [{ count }] = await db .select({ count: sql<number>`count(*)::int` }) .from(blockPosts) .where(and(eq(blockPosts.authorId, authorId), gte(blockPosts.createdAt, windowStart))); if (count >= POST_MAX) throw new RateLimitedError(POST_WINDOW_MIN * 60); } export async function assertReplyRateLimit(authorId: string): Promise<void> { const windowStart = new Date(Date.now() - REPLY_WINDOW_MIN * 60_000); const [{ count }] = await db .select({ count: sql<number>`count(*)::int` }) .from(blockPostReplies) .where(and(eq(blockPostReplies.authorId, authorId), gte(blockPostReplies.createdAt, windowStart))); if (count >= REPLY_MAX) throw new RateLimitedError(REPLY_WINDOW_MIN * 60); }import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { createPostSchema } from "@/lib/block/post-schema"; import { assertPostRateLimit, RateLimitedError } from "@/lib/block/rate-limit"; import { and, desc, eq, isNull, lt } from "drizzle-orm"; import { z } from "zod"; const listQuerySchema = z.object({ cursor: z.string().datetime().optional(), sectionKey: z.string().optional(), limit: z.coerce.number().int().min(1).max(50).default(20), }); export async function GET(req: NextRequest) { const parsed = listQuerySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) { return NextResponse.json({ error: "invalid_query", details: parsed.error.flatten() }, { status: 400 }); } const { cursor, sectionKey, limit } = parsed.data; try { const conditions = [isNull(blockPosts.deletedAt), eq(blockPosts.status, "published")]; if (cursor) conditions.push(lt(blockPosts.createdAt, new Date(cursor))); if (sectionKey) conditions.push(eq(blockPosts.sectionKey, sectionKey)); const rows = await db .select() .from(blockPosts) .where(and(...conditions)) .orderBy(desc(blockPosts.createdAt)) .limit(limit); return NextResponse.json({ posts: rows, nextCursor: rows.length === limit ? rows[rows.length - 1].createdAt.toISOString() : null, }); } catch (err) { console.error("[block/posts:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } } export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const rawBody = await req.json().catch(() => null); const parsed = createPostSchema.safeParse(rawBody); if (!parsed.success) { return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); } try { await assertPostRateLimit(viewer.id); } catch (err) { if (err instanceof RateLimitedError) { return NextResponse.json( { error: "rate_limited" }, { status: 429, headers: { "Retry-After": String(err.retryAfterSeconds) } } ); } throw err; } try { const [post] = await db.insert(blockPosts).values({ authorId: viewer.id, sectionKey: parsed.data.sectionKey, title: parsed.data.title, body: parsed.data.body, tags: parsed.data.tags, }).returning(); return NextResponse.json({ post }, { status: 201 }); } catch (err) { console.error("[block/posts:POST]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { blockPosts, blockPostCheers } from "@/lib/db/schema/block-posts"; import { and, eq, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const result = await db.transaction(async (tx) => { const existing = await tx .select({ id: blockPostCheers.id }) .from(blockPostCheers) .where(and(eq(blockPostCheers.postId, params.id), eq(blockPostCheers.memberId, viewer.id))) .limit(1); if (existing.length > 0) { const deleted = await tx .delete(blockPostCheers) .where(eq(blockPostCheers.id, existing[0].id)) .returning({ id: blockPostCheers.id }); if (deleted.length === 0) return { cheered: true }; // already removed by a concurrent request await tx.update(blockPosts).set({ cheerCount: sql`${blockPosts.cheerCount} - 1` }).where(eq(blockPosts.id, params.id)); return { cheered: false }; } const inserted = await tx .insert(blockPostCheers) .values({ postId: params.id, memberId: viewer.id }) .onConflictDoNothing({ target: [blockPostCheers.postId, blockPostCheers.memberId] }) .returning({ id: blockPostCheers.id }); if (inserted.length === 0) return { cheered: true }; // already cheered by a concurrent request, no double count await tx.update(blockPosts).set({ cheerCount: sql`${blockPosts.cheerCount} + 1` }).where(eq(blockPosts.id, params.id)); return { cheered: true }; }); return NextResponse.json(result); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { createReplySchema } from "@/lib/block/post-schema"; import { assertReplyRateLimit, RateLimitedError } from "@/lib/block/rate-limit"; import { db } from "@/lib/db"; import { blockPosts, blockPostReplies } from "@/lib/db/schema/block-posts"; import { asc, eq, isNull } from "drizzle-orm"; export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { const rows = await db .select() .from(blockPostReplies) .where(and_(params.id)) .orderBy(asc(blockPostReplies.createdAt)); return NextResponse.json({ replies: rows }); function and_(postId: string) { return isNull(blockPostReplies.deletedAt), eq(blockPostReplies.postId, postId); } } export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const rawBody = await req.json().catch(() => null); const parsed = createReplySchema.safeParse(rawBody); if (!parsed.success) { return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); } try { await assertReplyRateLimit(viewer.id); } catch (err) { if (err instanceof RateLimitedError) { return NextResponse.json( { error: "rate_limited" }, { status: 429, headers: { "Retry-After": String(err.retryAfterSeconds) } } ); } throw err; } const [post] = await db.select({ id: blockPosts.id }).from(blockPosts).where(eq(blockPosts.id, params.id)).limit(1); if (!post) return NextResponse.json({ error: "not_found" }, { status: 404 }); const [reply] = await db.transaction(async (tx) => { const [r] = await tx .insert(blockPostReplies) .values({ postId: params.id, authorId: viewer.id, body: parsed.data.body }) .returning(); await tx.update(blockPosts).set({ replyCount: sql`${blockPosts.replyCount} + 1` }).where(eq(blockPosts.id, params.id)); return [r]; }); return NextResponse.json({ reply }, { status: 201 }); }export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { const rows = await db .select() .from(blockPostReplies) .where(and(eq(blockPostReplies.postId, params.id), isNull(blockPostReplies.deletedAt))) .orderBy(asc(blockPostReplies.createdAt)); return NextResponse.json({ replies: rows }); }import { and, asc, eq, isNull, sql } from "drizzle-orm";import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { flagForReview } from "@/lib/moderation/flag"; import { db } from "@/lib/db"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { eq } from "drizzle-orm"; import { z } from "zod"; const reportSchema = z.object({ reason: z.string().trim().min(1).max(500) }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const rawBody = await req.json().catch(() => null); const parsed = reportSchema.safeParse(rawBody); if (!parsed.success) { return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); } const [post] = await db.select({ id: blockPosts.id }).from(blockPosts).where(eq(blockPosts.id, params.id)).limit(1); if (!post) return NextResponse.json({ error: "not_found" }, { status: 404 }); await flagForReview({ contentType: "block_post", contentId: post.id, reason: parsed.data.reason, reportedBy: viewer.id, }); return NextResponse.json({ status: "flagged" }, { status: 202 }); }import { pgTable, uuid, text, timestamp, pgEnum } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const emailTypeEnum = pgEnum("email_type", ["verification", "digest", "alert"]); export const emailStatusEnum = pgEnum("email_status", ["sent", "failed"]); export const notificationsEmail = pgTable("notifications_email", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").references(() => blockMembers.id, { onDelete: "set null" }), type: emailTypeEnum("type").notNull(), toAddress: text("to_address").notNull(), subject: text("subject").notNull(), status: emailStatusEnum("status").notNull(), providerMessageId: text("provider_message_id"), error: text("error"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), });import { Resend } from "resend"; // Fails loudly at import time in production if the key is missing — this is // deliberately not a silent no-op, because a mis-set env var here means // nobody's verification email ever arrives and support has no idea why. const apiKey = process.env.RESEND_API_KEY; if (!apiKey && process.env.NODE_ENV === "production") { throw new Error("RESEND_API_KEY is not set. Refusing to boot without real email delivery in production."); } export const resend = apiKey ? new Resend(apiKey) : null; export const EMAIL_FROM = process.env.RESEND_FROM_EMAIL ?? "onboarding@resend.dev";import { db } from "@/lib/db"; import { notificationsEmail } from "@/lib/db/schema/notifications-email"; import { and, eq, gte, sql } from "drizzle-orm"; export class EmailRateLimitedError extends Error { constructor(public retryAfterSeconds: number) { super(`Too many emails sent to this address. Try again in ${retryAfterSeconds}s.`); this.name = "EmailRateLimitedError"; } } // Prevents email-bombing an address via repeated "resend code" clicks — // a real abuse vector, not a speculative one. const WINDOW_MIN = 10; const MAX_PER_WINDOW = 5; export async function assertEmailRateLimit(toAddress: string): Promise<void> { const windowStart = new Date(Date.now() - WINDOW_MIN * 60_000); const [{ count }] = await db .select({ count: sql<number>`count(*)::int` }) .from(notificationsEmail) .where(and(eq(notificationsEmail.toAddress, toAddress), gte(notificationsEmail.createdAt, windowStart))); if (count >= MAX_PER_WINDOW) throw new EmailRateLimitedError(WINDOW_MIN * 60); }import { resend, EMAIL_FROM } from "./client"; import { db } from "@/lib/db"; import { notificationsEmail } from "@/lib/db/schema/notifications-email"; import { assertEmailRateLimit, EmailRateLimitedError } from "./rate-limit"; type SendParams = { to: string; subject: string; html: string; type: "verification" | "digest" | "alert"; memberId?: string; }; export { EmailRateLimitedError }; export async function sendEmail(params: SendParams): Promise<{ delivered: boolean }> { await assertEmailRateLimit(params.to); // throws EmailRateLimitedError, caller decides how to surface it if (!resend) { // Dev-only fallback — explicit and logged, never silent, never runs in production // because client.ts already refuses to boot without a key in prod. console.warn(`[email:dev-fallback] RESEND_API_KEY not set. Would have sent to ${params.to}: "${params.subject}"`); await db.insert(notificationsEmail).values({ memberId: params.memberId ?? null, type: params.type, toAddress: params.to, subject: params.subject, status: "failed", error: "RESEND_API_KEY not set (dev fallback)", }); return { delivered: false }; } try { const result = await resend.emails.send({ from: EMAIL_FROM, to: params.to, subject: params.subject, html: params.html, }); if (result.error) { await db.insert(notificationsEmail).values({ memberId: params.memberId ?? null, type: params.type, toAddress: params.to, subject: params.subject, status: "failed", error: result.error.message, }); return { delivered: false }; } await db.insert(notificationsEmail).values({ memberId: params.memberId ?? null, type: params.type, toAddress: params.to, subject: params.subject, status: "sent", providerMessageId: result.data?.id ?? null, }); return { delivered: true }; } catch (err) { console.error("[email:send] unexpected failure", err); await db.insert(notificationsEmail).values({ memberId: params.memberId ?? null, type: params.type, toAddress: params.to, subject: params.subject, status: "failed", error: err instanceof Error ? err.message : "unknown error", }); return { delivered: false }; } } export async function sendVerificationCodeEmail(to: string, code: string): Promise<{ delivered: boolean }> { return sendEmail({ to, type: "verification", subject: "Your Misfit Social verification code", html: ` <div style="font-family: sans-serif; background:#0A0908; color:#F2EDE4; padding:24px;"> <p style="color:#A79E93;">Your verification code:</p> <p style="font-size:28px; letter-spacing:4px; color:#D4AF37; font-weight:bold;">${code}</p> <p style="color:#A79E93; font-size:13px;">This code expires in 10 minutes. If you didn't request this, ignore this email.</p> </div> `, }); }// wherever the code is generated today, replace the dev-only "return devCode in response" branch with: import { sendVerificationCodeEmail, EmailRateLimitedError } from "@/lib/email/send"; try { await sendVerificationCodeEmail(email, code); } catch (err) { if (err instanceof EmailRateLimitedError) { return NextResponse.json({ error: "rate_limited" }, { status: 429, headers: { "Retry-After": String(err.retryAfterSeconds) } }); } throw err; }import { pgTable, uuid, text, timestamp, pgEnum } from "drizzle-orm/pg-core"; export const quoteCategoryEnum = pgEnum("quote_category", ["scripture", "encouragement", "community"]); export const quotes = pgTable("quotes", { id: uuid("id").defaultRandom().primaryKey(), text: text("text").notNull(), source: text("source").notNull(), // attribution, e.g. "Joshua 1:9" or a member's name category: quoteCategoryEnum("category").notNull(), active: text("active").notNull().default("true"), // simple on/off switch for retiring a quote without deleting history createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), });import { db } from "@/lib/db"; import { quotes } from "@/lib/db/schema/quotes"; // Starter set only — scripture quotes are public domain (KJV). // "encouragement" and "community" categories intentionally left for real // community-submitted content, not invented copy standing in for it. const SEED_QUOTES = [ { text: "Be strong and of a good courage; be not afraid, neither be thou dismayed.", source: "Joshua 1:9", category: "scripture" as const }, { text: "I can do all things through Christ which strengtheneth me.", source: "Philippians 4:13", category: "scripture" as const }, { text: "The Lord is my shepherd; I shall not want.", source: "Psalm 23:1", category: "scripture" as const }, { text: "Come unto me, all ye that labour and are heavy laden, and I will give you rest.", source: "Matthew 11:28", category: "scripture" as const }, { text: "Weeping may endure for a night, but joy cometh in the morning.", source: "Psalm 30:5", category: "scripture" as const }, ]; export async function seedQuotes() { await db.insert(quotes).values(SEED_QUOTES).onConflictDoNothing(); console.log(`[seed:quotes] inserted up to ${SEED_QUOTES.length} starter quotes`); } if (require.main === module) { seedQuotes().then(() => process.exit(0)).catch((err) => { console.error(err); process.exit(1); }); }import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { quotes } from "@/lib/db/schema/quotes"; import { and, eq, sql } from "drizzle-orm"; import { z } from "zod"; const querySchema = z.object({ category: z.enum(["scripture", "encouragement", "community"]).optional(), }); export async function GET(req: NextRequest) { const parsed = querySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) { return NextResponse.json({ error: "invalid_query" }, { status: 400 }); } const conditions = [eq(quotes.active, "true")]; if (parsed.data.category) conditions.push(eq(quotes.category, parsed.data.category)); const [quote] = await db .select() .from(quotes) .where(and(...conditions)) .orderBy(sql`random()`) .limit(1); if (!quote) { // Empty state surfaced honestly, not a fabricated fallback quote return NextResponse.json({ quote: null, empty: true }, { status: 200 }); } return NextResponse.json({ quote }); }-- Run this ONCE, manually, after `drizzle-kit push` has created vault_teachings. -- Not run automatically — this is intentional, see README note. ALTER TABLE vault_teachings ADD COLUMN search_vector tsvector GENERATED ALWAYS AS ( setweight(to_tsvector('english', coalesce(title, '')), 'A') || setweight(to_tsvector('english', coalesce(description, '')), 'B') || setweight(to_tsvector('english', coalesce(transcript, '')), 'C') ) STORED; CREATE INDEX vault_teachings_search_idx ON vault_teachings USING GIN (search_vector);// Add to the existing roles.ts — exports TIER_RANK (was private) and adds // an anonymous-safe tier lookup other features can use directly. export const TIER_RANK: Record<MemberTier, number> = { street: 0, block: 1, house: 2 }; export function tierMeetsRequirement(tier: MemberTier, required: MemberTier): boolean { return TIER_RANK[tier] >= TIER_RANK[required]; } export async function getViewerTier(): Promise<MemberTier> { const viewer = await getBlockViewer(); if (!viewer) return "street"; // anonymous visitor — distinct from an authenticated member with no tier row return getMemberTier(viewer.id); }import { z } from "zod"; export const createTeachingSchema = z.object({ title: z.string().trim().min(1).max(200), description: z.string().trim().min(1).max(2000), transcript: z.string().trim().max(50_000).optional(), mediaType: z.enum(["video", "audio", "text", "study_guide"]), mediaUrl: z.string().url().max(2048).optional(), series: z.string().trim().max(100).optional(), topicTags: z.array(z.string().trim().min(1).max(30)).max(10).default([]), accessTier: z.enum(["street", "block", "house"]).default("block"), }); export const searchQuerySchema = z.object({ q: z.string().trim().max(200).optional(), series: z.string().trim().max(100).optional(), tag: z.string().trim().max(30).optional(), cursor: z.string().datetime().optional(), limit: z.coerce.number().int().min(1).max(50).default(20), });import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { vaultTeachings } from "@/lib/db/schema/vault"; import { and, arrayContains, eq, lt, sql } from "drizzle-orm"; import { getViewerTier, tierMeetsRequirement } from "@/lib/auth/roles"; import { searchQuerySchema } from "@/lib/vault/schema"; export async function GET(req: NextRequest) { const parsed = searchQuerySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) { return NextResponse.json({ error: "invalid_query", details: parsed.error.flatten() }, { status: 400 }); } const { q, series, tag, cursor, limit } = parsed.data; const viewerTier = await getViewerTier(); // Tier gate happens in SQL, not just in application code after the fact — // an anonymous visitor's query never even fetches house-only rows. const visibleTiers = (["street", "block", "house"] as const).filter((t) => tierMeetsRequirement(viewerTier, t) ? false : true // placeholder removed below ); try { const conditions = [ // A row is visible if its OWN required tier is <= viewer's tier — // i.e. viewer meets the row's requirement, not the other way around. sql` CASE ${vaultTeachings.accessTier} WHEN 'street' THEN true WHEN 'block' THEN ${viewerTier === "block" || viewerTier === "house"} WHEN 'house' THEN ${viewerTier === "house"} END `, ]; if (series) conditions.push(eq(vaultTeachings.series, series)); if (tag) conditions.push(arrayContains(vaultTeachings.topicTags, [tag])); if (cursor) conditions.push(lt(vaultTeachings.publishedAt, new Date(cursor))); if (q) conditions.push(sql`search_vector @@ plainto_tsquery('english', ${q})`); const rows = await db .select() .from(vaultTeachings) .where(and(...conditions)) .orderBy( q ? sql`ts_rank(search_vector, plainto_tsquery('english', ${q})) DESC` : sql`${vaultTeachings.publishedAt} DESC` ) .limit(limit); return NextResponse.json({ teachings: rows, nextCursor: rows.length === limit ? rows[rows.length - 1].publishedAt.toISOString() : null, }); } catch (err) { console.error("[vault/teachings:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }const visibleTiers = (["street", "block", "house"] as const).filter((t) => tierMeetsRequirement(viewerTier, t) ? false : true // placeholder removed below );export async function POST(req: NextRequest) { let viewer; try { viewer = await requireHouseAccess(); // ASSUMPTION FLAGGED ABOVE: swap to requireSiteRole("moderator") if upload should be staff-only } catch (err) { if (err instanceof AccessDeniedError) { return NextResponse.json({ error: err.reason }, { status: err.reason === "unauthenticated" ? 401 : 403 }); } throw err; } const rawBody = await req.json().catch(() => null); const parsed = createTeachingSchema.safeParse(rawBody); if (!parsed.success) { return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); } const [teaching] = await db.insert(vaultTeachings).values({ ...parsed.data, uploadedBy: viewer.id, }).returning(); return NextResponse.json({ teaching }, { status: 201 }); }import { pgTable, uuid, integer, timestamp, uniqueIndex } from "drizzle-orm/pg-core"; import { blockPosts } from "./block-posts"; import { blockMembers } from "./block-members"; export const streetCuratedPosts = pgTable( "street_curated_posts", { id: uuid("id").defaultRandom().primaryKey(), postId: uuid("post_id").notNull().references(() => blockPosts.id, { onDelete: "cascade" }), curatedBy: uuid("curated_by").notNull().references(() => blockMembers.id), sortOrder: integer("sort_order").notNull().default(0), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ onePerPost: uniqueIndex("street_curated_posts_post_uq").on(t.postId) }) );// EDITORIAL CONTENT — placeholder draft, not final copy. // Replace every field below with your actual mission language before launch. // This file is intentionally NOT database-backed: it's static site copy, // same as any other page's About-page text would be. export const STREET_COPY = { headline: "PLACEHOLDER: Your mission headline goes here", subheadline: "PLACEHOLDER: One sentence describing who this is for", missionBody: `PLACEHOLDER: The real explainer paragraph(s) about the ministry's mission, who it serves, and what The Block/House/Workshop actually are. This needs your real language, not mine.`, ctaLabel: "Join The Block", ctaHref: "/join", // ASSUMPTION: adjust to your real signup route };import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { streetCuratedPosts } from "@/lib/db/schema/street"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { blockMembers } from "@/lib/db/schema/block-members"; import { vaultTeachings } from "@/lib/db/schema/vault"; import { asc, desc, eq, and } from "drizzle-orm"; export async function GET() { try { // ASSUMPTION FLAGGED: blockMembers.displayName is guessed — adjust to your // real column name (could be `name`, `username`, etc.) before running this. const curatedPosts = await db .select({ id: blockPosts.id, title: blockPosts.title, body: blockPosts.body, authorName: blockMembers.displayName, createdAt: blockPosts.createdAt, }) .from(streetCuratedPosts) .innerJoin(blockPosts, eq(streetCuratedPosts.postId, blockPosts.id)) .innerJoin(blockMembers, eq(blockPosts.authorId, blockMembers.id)) .where(eq(blockPosts.status, "published")) // a curated post that's since been removed/flagged never surfaces publicly .orderBy(asc(streetCuratedPosts.sortOrder)) .limit(6); const featuredTeachings = await db .select({ id: vaultTeachings.id, title: vaultTeachings.title, description: vaultTeachings.description, mediaType: vaultTeachings.mediaType, }) .from(vaultTeachings) .where(and(eq(vaultTeachings.featured, true), eq(vaultTeachings.accessTier, "street"))) .orderBy(desc(vaultTeachings.publishedAt)) .limit(3); return NextResponse.json({ curatedPosts, featuredTeachings }); } catch (err) { console.error("[street/snapshot:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { streetCuratedPosts } from "@/lib/db/schema/street"; import { z } from "zod"; const curateSchema = z.object({ postId: z.string().uuid(), sortOrder: z.number().int().default(0) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = curateSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [row] = await db .insert(streetCuratedPosts) .values({ postId: parsed.data.postId, curatedBy: viewer.id, sortOrder: parsed.data.sortOrder }) .onConflictDoUpdate({ target: streetCuratedPosts.postId, set: { sortOrder: parsed.data.sortOrder }, }) .returning(); return NextResponse.json({ curated: row }, { status: 201 }); } export async function DELETE(req: NextRequest) { try { await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const { postId } = z.object({ postId: z.string().uuid() }).parse(await req.json()); await db.delete(streetCuratedPosts).where(eq(streetCuratedPosts.postId, postId)); return NextResponse.json({ status: "removed" }); }import { STREET_COPY } from "@/content/street-copy"; import { StreetCuratedFeed } from "@/components/street/curated-feed"; import styles from "./street.module.css"; // No auth check here at all — this route is explicitly public. That absence // is the correct behavior, not an oversight. export default function StreetPage() { return ( <main className={styles.page}> <section className={styles.hero}> <h1 className={styles.headline}>{STREET_COPY.headline}</h1> <p className={styles.subheadline}>{STREET_COPY.subheadline}</p> <a className={styles.cta} href={STREET_COPY.ctaHref}>{STREET_COPY.ctaLabel}</a> </section> <section className={styles.body}> <p>{STREET_COPY.missionBody}</p> </section> <StreetCuratedFeed /> </main> ); }"use client"; import { useEffect, useState } from "react"; import styles from "./curated-feed.module.css"; type Post = { id: string; title: string; body: string; authorName: string; createdAt: string }; type Teaching = { id: string; title: string; description: string; mediaType: string }; type ViewState = | { status: "loading" } | { status: "ready"; posts: Post[]; teachings: Teaching[] } | { status: "empty" } | { status: "error" }; export function StreetCuratedFeed() { const [state, setState] = useState<ViewState>({ status: "loading" }); useEffect(() => { let cancelled = false; fetch("/api/street/snapshot") .then((res) => { if (!res.ok) throw new Error(); return res.json(); }) .then((data) => { if (cancelled) return; const { curatedPosts, featuredTeachings } = data; if (curatedPosts.length === 0 && featuredTeachings.length === 0) { setState({ status: "empty" }); } else { setState({ status: "ready", posts: curatedPosts, teachings: featuredTeachings }); } }) .catch(() => { if (!cancelled) setState({ status: "error" }); }); return () => { cancelled = true; }; }, []); if (state.status === "loading") return <p className={styles.status}>Loading…</p>; if (state.status === "error") return <p className={styles.status}>Couldn't load community highlights right now.</p>; if (state.status === "empty") return <p className={styles.status}>Community highlights are coming soon.</p>; return ( <div className={styles.grid}> {state.posts.length > 0 && ( <section> <h2 className={styles.sectionTitle}>From The Block</h2> {state.posts.map((p) => ( <article key={p.id} className={styles.card}> <h3>{p.title}</h3> <p>{p.body.slice(0, 220)}{p.body.length > 220 ? "…" : ""}</p> <footer className={styles.meta}>— {p.authorName}</footer> </article> ))} </section> )} {state.teachings.length > 0 && ( <section> <h2 className={styles.sectionTitle}>Featured Teachings</h2> {state.teachings.map((t) => ( <article key={t.id} className={styles.card}> <h3>{t.title}</h3> <p>{t.description}</p> </article> ))} </section> )} </div> ); }import { pgTable, uuid, text, integer, timestamp, boolean, pgEnum, uniqueIndex, index, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const missionNeedCategoryEnum = pgEnum("mission_need_category", [ "service", "skills", "logistics", "administrative", "creative", "other", ]); export const missionNeedStatusEnum = pgEnum("mission_need_status", ["open", "cancelled"]); export const missionSignupStatusEnum = pgEnum("mission_signup_status", ["active", "cancelled"]); export const missionBoardNeeds = pgTable( "mission_board_needs", { id: uuid("id").defaultRandom().primaryKey(), title: text("title").notNull(), description: text("description").notNull(), category: missionNeedCategoryEnum("category").notNull(), isVirtual: boolean("is_virtual").notNull().default(false), location: text("location"), // null when isVirtual = true slotsNeeded: integer("slots_needed").notNull(), deadline: timestamp("deadline", { withTimezone: true }), // null = ongoing, no deadline status: missionNeedStatusEnum("status").notNull().default("open"), createdBy: uuid("created_by").notNull().references(() => blockMembers.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ statusIdx: index("mission_needs_status_idx").on(t.status, t.deadline) }) ); export const missionBoardSignups = pgTable( "mission_board_signups", { id: uuid("id").defaultRandom().primaryKey(), needId: uuid("need_id").notNull().references(() => missionBoardNeeds.id, { onDelete: "cascade" }), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), status: missionSignupStatusEnum("status").notNull().default("active"), signedUpAt: timestamp("signed_up_at", { withTimezone: true }).notNull().defaultNow(), cancelledAt: timestamp("cancelled_at", { withTimezone: true }), }, (t) => ({ needMemberIdx: index("mission_signups_need_member_idx").on(t.needId, t.memberId) }) );import { z } from "zod"; export const createNeedSchema = z.object({ title: z.string().trim().min(1).max(200), description: z.string().trim().min(1).max(3000), category: z.enum(["service", "skills", "logistics", "administrative", "creative", "other"]), isVirtual: z.boolean(), location: z.string().trim().max(200).optional(), slotsNeeded: z.number().int().min(1).max(500), deadline: z.string().datetime().optional(), }).refine((d) => d.isVirtual || !!d.location, { message: "location is required when isVirtual is false", path: ["location"], });import { db } from "@/lib/db"; import { missionBoardNeeds, missionBoardSignups } from "@/lib/db/schema/mission-board"; import { and, eq, sql } from "drizzle-orm"; export class MissionBoardError extends Error { constructor(public code: "not_found" | "closed" | "past_deadline" | "full" | "already_signed_up") { super(code); } } export async function signUpForNeed(needId: string, memberId: string): Promise<void> { await db.transaction(async (tx) => { // Row-level lock: every concurrent signup attempt for this need serializes here, // which is exactly what prevents both overselling slots and double-signup races. const [need] = await tx.execute( sql`SELECT id, status, slots_needed, deadline FROM mission_board_needs WHERE id = ${needId} FOR UPDATE` ) as unknown as [{ id: string; status: string; slots_needed: number; deadline: Date | null }]; if (!need) throw new MissionBoardError("not_found"); if (need.status !== "open") throw new MissionBoardError("closed"); if (need.deadline && new Date(need.deadline) < new Date()) throw new MissionBoardError("past_deadline"); const [existing] = await tx .select({ id: missionBoardSignups.id }) .from(missionBoardSignups) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "active") )) .limit(1); if (existing) throw new MissionBoardError("already_signed_up"); const [{ count }] = await tx .select({ count: sql<number>`count(*)::int` }) .from(missionBoardSignups) .where(and(eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.status, "active"))); if (count >= need.slots_needed) throw new MissionBoardError("full"); // Reactivate a prior cancelled signup rather than inserting a duplicate row const [priorCancelled] = await tx .select({ id: missionBoardSignups.id }) .from(missionBoardSignups) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "cancelled") )) .limit(1); if (priorCancelled) { await tx.update(missionBoardSignups) .set({ status: "active", signedUpAt: sql`now()`, cancelledAt: null }) .where(eq(missionBoardSignups.id, priorCancelled.id)); } else { await tx.insert(missionBoardSignups).values({ needId, memberId }); } }); } export async function cancelSignup(needId: string, memberId: string): Promise<void> { const result = await db .update(missionBoardSignups) .set({ status: "cancelled", cancelledAt: sql`now()` }) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "active") )) .returning({ id: missionBoardSignups.id }); if (result.length === 0) throw new MissionBoardError("not_found"); }-- Run this ONCE, manually, after `drizzle-kit push` has created vault_teachings. -- Not run automatically — this is intentional, see README note. ALTER TABLE vault_teachings ADD COLUMN search_vector tsvector GENERATED ALWAYS AS ( setweight(to_tsvector('english', coalesce(title, '')), 'A') || setweight(to_tsvector('english', coalesce(description, '')), 'B') || setweight(to_tsvector('english', coalesce(transcript, '')), 'C') ) STORED; CREATE INDEX vault_teachings_search_idx ON vault_teachings USING GIN (search_vector);// Add to the existing roles.ts — exports TIER_RANK (was private) and adds // an anonymous-safe tier lookup other features can use directly. export const TIER_RANK: Record<MemberTier, number> = { street: 0, block: 1, house: 2 }; export function tierMeetsRequirement(tier: MemberTier, required: MemberTier): boolean { return TIER_RANK[tier] >= TIER_RANK[required]; } export async function getViewerTier(): Promise<MemberTier> { const viewer = await getBlockViewer(); if (!viewer) return "street"; // anonymous visitor — distinct from an authenticated member with no tier row return getMemberTier(viewer.id); }import { z } from "zod"; export const createTeachingSchema = z.object({ title: z.string().trim().min(1).max(200), description: z.string().trim().min(1).max(2000), transcript: z.string().trim().max(50_000).optional(), mediaType: z.enum(["video", "audio", "text", "study_guide"]), mediaUrl: z.string().url().max(2048).optional(), series: z.string().trim().max(100).optional(), topicTags: z.array(z.string().trim().min(1).max(30)).max(10).default([]), accessTier: z.enum(["street", "block", "house"]).default("block"), }); export const searchQuerySchema = z.object({ q: z.string().trim().max(200).optional(), series: z.string().trim().max(100).optional(), tag: z.string().trim().max(30).optional(), cursor: z.string().datetime().optional(), limit: z.coerce.number().int().min(1).max(50).default(20), });import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { vaultTeachings } from "@/lib/db/schema/vault"; import { and, arrayContains, eq, lt, sql } from "drizzle-orm"; import { getViewerTier, tierMeetsRequirement } from "@/lib/auth/roles"; import { searchQuerySchema } from "@/lib/vault/schema"; export async function GET(req: NextRequest) { const parsed = searchQuerySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) { return NextResponse.json({ error: "invalid_query", details: parsed.error.flatten() }, { status: 400 }); } const { q, series, tag, cursor, limit } = parsed.data; const viewerTier = await getViewerTier(); // Tier gate happens in SQL, not just in application code after the fact — // an anonymous visitor's query never even fetches house-only rows. const visibleTiers = (["street", "block", "house"] as const).filter((t) => tierMeetsRequirement(viewerTier, t) ? false : true // placeholder removed below ); try { const conditions = [ // A row is visible if its OWN required tier is <= viewer's tier — // i.e. viewer meets the row's requirement, not the other way around. sql` CASE ${vaultTeachings.accessTier} WHEN 'street' THEN true WHEN 'block' THEN ${viewerTier === "block" || viewerTier === "house"} WHEN 'house' THEN ${viewerTier === "house"} END `, ]; if (series) conditions.push(eq(vaultTeachings.series, series)); if (tag) conditions.push(arrayContains(vaultTeachings.topicTags, [tag])); if (cursor) conditions.push(lt(vaultTeachings.publishedAt, new Date(cursor))); if (q) conditions.push(sql`search_vector @@ plainto_tsquery('english', ${q})`); const rows = await db .select() .from(vaultTeachings) .where(and(...conditions)) .orderBy( q ? sql`ts_rank(search_vector, plainto_tsquery('english', ${q})) DESC` : sql`${vaultTeachings.publishedAt} DESC` ) .limit(limit); return NextResponse.json({ teachings: rows, nextCursor: rows.length === limit ? rows[rows.length - 1].publishedAt.toISOString() : null, }); } catch (err) { console.error("[vault/teachings:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }const visibleTiers = (["street", "block", "house"] as const).filter((t) => tierMeetsRequirement(viewerTier, t) ? false : true // placeholder removed below );export async function POST(req: NextRequest) { let viewer; try { viewer = await requireHouseAccess(); // ASSUMPTION FLAGGED ABOVE: swap to requireSiteRole("moderator") if upload should be staff-only } catch (err) { if (err instanceof AccessDeniedError) { return NextResponse.json({ error: err.reason }, { status: err.reason === "unauthenticated" ? 401 : 403 }); } throw err; } const rawBody = await req.json().catch(() => null); const parsed = createTeachingSchema.safeParse(rawBody); if (!parsed.success) { return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); } const [teaching] = await db.insert(vaultTeachings).values({ ...parsed.data, uploadedBy: viewer.id, }).returning(); return NextResponse.json({ teaching }, { status: 201 }); }import { pgTable, uuid, integer, timestamp, uniqueIndex } from "drizzle-orm/pg-core"; import { blockPosts } from "./block-posts"; import { blockMembers } from "./block-members"; export const streetCuratedPosts = pgTable( "street_curated_posts", { id: uuid("id").defaultRandom().primaryKey(), postId: uuid("post_id").notNull().references(() => blockPosts.id, { onDelete: "cascade" }), curatedBy: uuid("curated_by").notNull().references(() => blockMembers.id), sortOrder: integer("sort_order").notNull().default(0), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ onePerPost: uniqueIndex("street_curated_posts_post_uq").on(t.postId) }) );// EDITORIAL CONTENT — placeholder draft, not final copy. // Replace every field below with your actual mission language before launch. // This file is intentionally NOT database-backed: it's static site copy, // same as any other page's About-page text would be. export const STREET_COPY = { headline: "PLACEHOLDER: Your mission headline goes here", subheadline: "PLACEHOLDER: One sentence describing who this is for", missionBody: `PLACEHOLDER: The real explainer paragraph(s) about the ministry's mission, who it serves, and what The Block/House/Workshop actually are. This needs your real language, not mine.`, ctaLabel: "Join The Block", ctaHref: "/join", // ASSUMPTION: adjust to your real signup route };import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { streetCuratedPosts } from "@/lib/db/schema/street"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { blockMembers } from "@/lib/db/schema/block-members"; import { vaultTeachings } from "@/lib/db/schema/vault"; import { asc, desc, eq, and } from "drizzle-orm"; export async function GET() { try { // ASSUMPTION FLAGGED: blockMembers.displayName is guessed — adjust to your // real column name (could be `name`, `username`, etc.) before running this. const curatedPosts = await db .select({ id: blockPosts.id, title: blockPosts.title, body: blockPosts.body, authorName: blockMembers.displayName, createdAt: blockPosts.createdAt, }) .from(streetCuratedPosts) .innerJoin(blockPosts, eq(streetCuratedPosts.postId, blockPosts.id)) .innerJoin(blockMembers, eq(blockPosts.authorId, blockMembers.id)) .where(eq(blockPosts.status, "published")) // a curated post that's since been removed/flagged never surfaces publicly .orderBy(asc(streetCuratedPosts.sortOrder)) .limit(6); const featuredTeachings = await db .select({ id: vaultTeachings.id, title: vaultTeachings.title, description: vaultTeachings.description, mediaType: vaultTeachings.mediaType, }) .from(vaultTeachings) .where(and(eq(vaultTeachings.featured, true), eq(vaultTeachings.accessTier, "street"))) .orderBy(desc(vaultTeachings.publishedAt)) .limit(3); return NextResponse.json({ curatedPosts, featuredTeachings }); } catch (err) { console.error("[street/snapshot:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { streetCuratedPosts } from "@/lib/db/schema/street"; import { z } from "zod"; const curateSchema = z.object({ postId: z.string().uuid(), sortOrder: z.number().int().default(0) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = curateSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [row] = await db .insert(streetCuratedPosts) .values({ postId: parsed.data.postId, curatedBy: viewer.id, sortOrder: parsed.data.sortOrder }) .onConflictDoUpdate({ target: streetCuratedPosts.postId, set: { sortOrder: parsed.data.sortOrder }, }) .returning(); return NextResponse.json({ curated: row }, { status: 201 }); } export async function DELETE(req: NextRequest) { try { await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const { postId } = z.object({ postId: z.string().uuid() }).parse(await req.json()); await db.delete(streetCuratedPosts).where(eq(streetCuratedPosts.postId, postId)); return NextResponse.json({ status: "removed" }); }import { STREET_COPY } from "@/content/street-copy"; import { StreetCuratedFeed } from "@/components/street/curated-feed"; import styles from "./street.module.css"; // No auth check here at all — this route is explicitly public. That absence // is the correct behavior, not an oversight. export default function StreetPage() { return ( <main className={styles.page}> <section className={styles.hero}> <h1 className={styles.headline}>{STREET_COPY.headline}</h1> <p className={styles.subheadline}>{STREET_COPY.subheadline}</p> <a className={styles.cta} href={STREET_COPY.ctaHref}>{STREET_COPY.ctaLabel}</a> </section> <section className={styles.body}> <p>{STREET_COPY.missionBody}</p> </section> <StreetCuratedFeed /> </main> ); }"use client"; import { useEffect, useState } from "react"; import styles from "./curated-feed.module.css"; type Post = { id: string; title: string; body: string; authorName: string; createdAt: string }; type Teaching = { id: string; title: string; description: string; mediaType: string }; type ViewState = | { status: "loading" } | { status: "ready"; posts: Post[]; teachings: Teaching[] } | { status: "empty" } | { status: "error" }; export function StreetCuratedFeed() { const [state, setState] = useState<ViewState>({ status: "loading" }); useEffect(() => { let cancelled = false; fetch("/api/street/snapshot") .then((res) => { if (!res.ok) throw new Error(); return res.json(); }) .then((data) => { if (cancelled) return; const { curatedPosts, featuredTeachings } = data; if (curatedPosts.length === 0 && featuredTeachings.length === 0) { setState({ status: "empty" }); } else { setState({ status: "ready", posts: curatedPosts, teachings: featuredTeachings }); } }) .catch(() => { if (!cancelled) setState({ status: "error" }); }); return () => { cancelled = true; }; }, []); if (state.status === "loading") return <p className={styles.status}>Loading…</p>; if (state.status === "error") return <p className={styles.status}>Couldn't load community highlights right now.</p>; if (state.status === "empty") return <p className={styles.status}>Community highlights are coming soon.</p>; return ( <div className={styles.grid}> {state.posts.length > 0 && ( <section> <h2 className={styles.sectionTitle}>From The Block</h2> {state.posts.map((p) => ( <article key={p.id} className={styles.card}> <h3>{p.title}</h3> <p>{p.body.slice(0, 220)}{p.body.length > 220 ? "…" : ""}</p> <footer className={styles.meta}>— {p.authorName}</footer> </article> ))} </section> )} {state.teachings.length > 0 && ( <section> <h2 className={styles.sectionTitle}>Featured Teachings</h2> {state.teachings.map((t) => ( <article key={t.id} className={styles.card}> <h3>{t.title}</h3> <p>{t.description}</p> </article> ))} </section> )} </div> ); }import { pgTable, uuid, text, integer, timestamp, boolean, pgEnum, uniqueIndex, index, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const missionNeedCategoryEnum = pgEnum("mission_need_category", [ "service", "skills", "logistics", "administrative", "creative", "other", ]); export const missionNeedStatusEnum = pgEnum("mission_need_status", ["open", "cancelled"]); export const missionSignupStatusEnum = pgEnum("mission_signup_status", ["active", "cancelled"]); export const missionBoardNeeds = pgTable( "mission_board_needs", { id: uuid("id").defaultRandom().primaryKey(), title: text("title").notNull(), description: text("description").notNull(), category: missionNeedCategoryEnum("category").notNull(), isVirtual: boolean("is_virtual").notNull().default(false), location: text("location"), // null when isVirtual = true slotsNeeded: integer("slots_needed").notNull(), deadline: timestamp("deadline", { withTimezone: true }), // null = ongoing, no deadline status: missionNeedStatusEnum("status").notNull().default("open"), createdBy: uuid("created_by").notNull().references(() => blockMembers.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ statusIdx: index("mission_needs_status_idx").on(t.status, t.deadline) }) ); export const missionBoardSignups = pgTable( "mission_board_signups", { id: uuid("id").defaultRandom().primaryKey(), needId: uuid("need_id").notNull().references(() => missionBoardNeeds.id, { onDelete: "cascade" }), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), status: missionSignupStatusEnum("status").notNull().default("active"), signedUpAt: timestamp("signed_up_at", { withTimezone: true }).notNull().defaultNow(), cancelledAt: timestamp("cancelled_at", { withTimezone: true }), }, (t) => ({ needMemberIdx: index("mission_signups_need_member_idx").on(t.needId, t.memberId) }) );import { z } from "zod"; export const createNeedSchema = z.object({ title: z.string().trim().min(1).max(200), description: z.string().trim().min(1).max(3000), category: z.enum(["service", "skills", "logistics", "administrative", "creative", "other"]), isVirtual: z.boolean(), location: z.string().trim().max(200).optional(), slotsNeeded: z.number().int().min(1).max(500), deadline: z.string().datetime().optional(), }).refine((d) => d.isVirtual || !!d.location, { message: "location is required when isVirtual is false", path: ["location"], });import { db } from "@/lib/db"; import { missionBoardNeeds, missionBoardSignups } from "@/lib/db/schema/mission-board"; import { and, eq, sql } from "drizzle-orm"; export class MissionBoardError extends Error { constructor(public code: "not_found" | "closed" | "past_deadline" | "full" | "already_signed_up") { super(code); } } export async function signUpForNeed(needId: string, memberId: string): Promise<void> { await db.transaction(async (tx) => { // Row-level lock: every concurrent signup attempt for this need serializes here, // which is exactly what prevents both overselling slots and double-signup races. const [need] = await tx.execute( sql`SELECT id, status, slots_needed, deadline FROM mission_board_needs WHERE id = ${needId} FOR UPDATE` ) as unknown as [{ id: string; status: string; slots_needed: number; deadline: Date | null }]; if (!need) throw new MissionBoardError("not_found"); if (need.status !== "open") throw new MissionBoardError("closed"); if (need.deadline && new Date(need.deadline) < new Date()) throw new MissionBoardError("past_deadline"); const [existing] = await tx .select({ id: missionBoardSignups.id }) .from(missionBoardSignups) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "active") )) .limit(1); if (existing) throw new MissionBoardError("already_signed_up"); const [{ count }] = await tx .select({ count: sql<number>`count(*)::int` }) .from(missionBoardSignups) .where(and(eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.status, "active"))); if (count >= need.slots_needed) throw new MissionBoardError("full"); // Reactivate a prior cancelled signup rather than inserting a duplicate row const [priorCancelled] = await tx .select({ id: missionBoardSignups.id }) .from(missionBoardSignups) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "cancelled") )) .limit(1); if (priorCancelled) { await tx.update(missionBoardSignups) .set({ status: "active", signedUpAt: sql`now()`, cancelledAt: null }) .where(eq(missionBoardSignups.id, priorCancelled.id)); } else { await tx.insert(missionBoardSignups).values({ needId, memberId }); } }); } export async function cancelSignup(needId: string, memberId: string): Promise<void> { const result = await db .update(missionBoardSignups) .set({ status: "cancelled", cancelledAt: sql`now()` }) .where(and( eq(missionBoardSignups.needId, needId), eq(missionBoardSignups.memberId, memberId), eq(missionBoardSignups.status, "active") )) .returning({ id: missionBoardSignups.id }); if (result.length === 0) throw new MissionBoardError("not_found"); }import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { missionBoardNeeds, missionBoardSignups } from "@/lib/db/schema/mission-board"; import { requireHouseAccess, AccessDeniedError } from "@/lib/auth/roles"; import { createNeedSchema } from "@/lib/mission-board/schema"; import { and, eq, desc, lt, sql } from "drizzle-orm"; import { z } from "zod"; const listQuerySchema = z.object({ category: z.string().optional(), cursor: z.string().datetime().optional(), limit: z.coerce.number().int().min(1).max(50).default(20), }); export async function GET(req: NextRequest) { const parsed = listQuerySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) return NextResponse.json({ error: "invalid_query" }, { status: 400 }); const { category, cursor, limit } = parsed.data; const conditions = [eq(missionBoardNeeds.status, "open")]; if (category) conditions.push(eq(missionBoardNeeds.category, category as any)); if (cursor) conditions.push(lt(missionBoardNeeds.createdAt, new Date(cursor))); const needs = await db .select({ id: missionBoardNeeds.id, title: missionBoardNeeds.title, description: missionBoardNeeds.description, category: missionBoardNeeds.category, isVirtual: missionBoardNeeds.isVirtual, location: missionBoardNeeds.location, slotsNeeded: missionBoardNeeds.slotsNeeded, deadline: missionBoardNeeds.deadline, createdAt: missionBoardNeeds.createdAt, slotsFilled: sql<number>`( SELECT count(*)::int FROM mission_board_signups WHERE need_id = ${missionBoardNeeds.id} AND status = 'active' )`, }) .from(missionBoardNeeds) .where(and(...conditions)) .orderBy(desc(missionBoardNeeds.createdAt)) .limit(limit); return NextResponse.json({ needs, nextCursor: needs.length === limit ? needs[needs.length - 1].createdAt.toISOString() : null, }); } export async function POST(req: NextRequest) { let viewer; try { viewer = await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) { return NextResponse.json({ error: err.reason }, { status: err.reason === "unauthenticated" ? 401 : 403 }); } throw err; } const parsed = createNeedSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const [need] = await db.insert(missionBoardNeeds).values({ ...parsed.data, deadline: parsed.data.deadline ? new Date(parsed.data.deadline) : null, createdBy: viewer.id, }).returning(); return NextResponse.json({ need }, { status: 201 }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { signUpForNeed, cancelSignup, MissionBoardError } from "@/lib/mission-board/signup"; const STATUS_BY_CODE: Record<MissionBoardError["code"], number> = { not_found: 404, closed: 409, past_deadline: 409, full: 409, already_signed_up: 409, }; export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } try { await signUpForNeed(params.id, viewer.id); return NextResponse.json({ status: "signed_up" }, { status: 201 }); } catch (err) { if (err instanceof MissionBoardError) { return NextResponse.json({ error: err.code }, { status: STATUS_BY_CODE[err.code] }); } console.error("[mission-board/signup:POST]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } } export async function DELETE(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } try { await cancelSignup(params.id, viewer.id); return NextResponse.json({ status: "cancelled" }); } catch (err) { if (err instanceof MissionBoardError) { return NextResponse.json({ error: err.code }, { status: STATUS_BY_CODE[err.code] }); } console.error("[mission-board/signup:DELETE]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }"use client"; import { useState } from "react"; import styles from "./need-card.module.css"; type Need = { id: string; title: string; description: string; category: string; isVirtual: boolean; location: string | null; slotsNeeded: number; slotsFilled: number; deadline: string | null; }; export function NeedCard({ need, viewerSignedUp }: { need: Need; viewerSignedUp: boolean }) { const [signedUp, setSignedUp] = useState(viewerSignedUp); const [pending, setPending] = useState(false); const [error, setError] = useState<string | null>(null); const isFull = need.slotsFilled >= need.slotsNeeded; async function toggleSignup() { setPending(true); setError(null); try { const res = await fetch(`/api/mission-board/needs/${need.id}/signup`, { method: signedUp ? "DELETE" : "POST", }); if (!res.ok) { const body = await res.json().catch(() => ({})); throw new Error(body.error ?? "request_failed"); } setSignedUp(!signedUp); } catch (e) { setError(e instanceof Error ? e.message : "request_failed"); } finally { setPending(false); } } return ( <article className={styles.card}> <span className={styles.category}>{need.category}</span> <h3 className={styles.title}>{need.title}</h3> <p className={styles.description}>{need.description}</p> <div className={styles.meta}> <span>{need.isVirtual ? "Virtual" : need.location}</span> <span className={styles.slots}>{need.slotsFilled}/{need.slotsNeeded} filled</span> </div> <div className={styles.progressTrack}> <div className={styles.progressFill} style={{ width: `${Math.min(100, (need.slotsFilled / need.slotsNeeded) * 100)}%` }} /> </div> <button className={signedUp ? styles.leaveBtn : styles.joinBtn} onClick={toggleSignup} disabled={pending || (!signedUp && isFull)} > {pending ? "…" : signedUp ? "Cancel signup" : isFull ? "Full" : "Sign up"} </button> {error && <p className={styles.error}>{error.replaceAll("_", " ")}</p>} </article> ); }.card { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); padding: 16px; } .category { font-family: var(--font-mono); font-size: 0.7rem; text-transform: uppercase; color: var(--accent-silver); } .title { font-family: var(--font-display); color: var(--text-primary); margin: 6px 0; } .description { color: var(--text-secondary); font-size: 0.9rem; } .meta { display: flex; justify-content: space-between; color: var(--text-secondary); font-size: 0.8rem; margin: 10px 0 6px; } .progressTrack { height: 6px; background: var(--surface-2); border-radius: 3px; overflow: hidden; } .progressFill { height: 100%; background: var(--accent-gold); transition: width var(--motion-slow) ease; } .joinBtn { margin-top: 12px; background: var(--accent-gold); color: #14100F; border: none; border-radius: var(--radius-sm); padding: 8px 14px; font-weight: 600; cursor: pointer; } .joinBtn:disabled { background: var(--surface-2); color: var(--text-secondary); cursor: not-allowed; } .leaveBtn { margin-top: 12px; background: transparent; color: var(--accent-silver); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 8px 14px; cursor: pointer; } .error { color: var(--danger-text); font-size: 0.8rem; margin-top: 6px; }import { pgTable, uuid, text, integer, timestamp, pgEnum, uniqueIndex, index, sql, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const workshopProjectStatusEnum = pgEnum("workshop_project_status", [ "planning", "active", "completed", "paused", ]); export const workshopVolunteerStatusEnum = pgEnum("workshop_volunteer_status", ["active", "left"]); export const workshopProjects = pgTable( "workshop_projects", { id: uuid("id").defaultRandom().primaryKey(), title: text("title").notNull(), missionStatement: text("mission_statement").notNull(), description: text("description").notNull(), status: workshopProjectStatusEnum("status").notNull().default("planning"), progressPercent: integer("progress_percent").notNull().default(0), coverPhotoUrls: text("cover_photo_urls").array().notNull().default(sql`'{}'::text[]`), createdBy: uuid("created_by").notNull().references(() => blockMembers.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), completedAt: timestamp("completed_at", { withTimezone: true }), }, (t) => ({ statusIdx: index("workshop_projects_status_idx").on(t.status, t.createdAt) }) ); export const workshopVolunteers = pgTable( "workshop_volunteers", { id: uuid("id").defaultRandom().primaryKey(), projectId: uuid("project_id").notNull().references(() => workshopProjects.id, { onDelete: "cascade" }), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), role: text("role"), status: workshopVolunteerStatusEnum("status").notNull().default("active"), joinedAt: timestamp("joined_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ oneRowPerMember: uniqueIndex("workshop_volunteers_project_member_uq").on(t.projectId, t.memberId) }) ); export const workshopUpdates = pgTable( "workshop_updates", { id: uuid("id").defaultRandom().primaryKey(), projectId: uuid("project_id").notNull().references(() => workshopProjects.id, { onDelete: "cascade" }), authorId: uuid("author_id").notNull().references(() => blockMembers.id), body: text("body").notNull(), photoUrls: text("photo_urls").array().notNull().default(sql`'{}'::text[]`), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ projectOrderIdx: index("workshop_updates_project_idx").on(t.projectId, t.createdAt) }) ); export const workshopDiscussionComments = pgTable( "workshop_discussion_comments", { id: uuid("id").defaultRandom().primaryKey(), projectId: uuid("project_id").notNull().references(() => workshopProjects.id, { onDelete: "cascade" }), authorId: uuid("author_id").notNull().references(() => blockMembers.id), body: text("body").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), deletedAt: timestamp("deleted_at", { withTimezone: true }), }, (t) => ({ projectOrderIdx: index("workshop_comments_project_idx").on(t.projectId, t.createdAt) }) );import { db } from "@/lib/db"; import { badges } from "@/lib/db/schema/member-roles"; // These keys MUST match BADGE_RULES in src/lib/roles/events.ts exactly. // This file exists specifically to close a gap flagged when events.ts was built: // rules referenced badge keys that had never been seeded. const SEED_BADGES = [ { key: "workshop_builder", name: "Builder", description: "Completed a Workshop project as an active volunteer." }, { key: "arcade_7_day_streak", name: "7-Day Streak", description: "Hit a 7-day activity streak in The Arcade." }, ]; export async function seedBadges() { await db.insert(badges).values(SEED_BADGES).onConflictDoNothing({ target: badges.key }); console.log(`[seed:badges] ensured ${SEED_BADGES.length} badge definitions exist`); } if (require.main === module) { seedBadges().then(() => process.exit(0)).catch((err) => { console.error(err); process.exit(1); }); }import { db } from "@/lib/db"; import { memberEvents, memberBadges, badges } from "@/lib/db/schema/member-roles"; import { eq } from "drizzle-orm"; type DbOrTx = typeof db; // drizzle transactions expose the same query-builder surface as `db` // ... BADGE_RULES unchanged from earlier pass ... export async function emitMemberEvent( memberId: string, eventType: string, payload: Record<string, unknown> = {}, executor: DbOrTx = db // callers inside a transaction pass `tx` here to keep this atomic with their own writes ): Promise<void> { await executor.insert(memberEvents).values({ memberId, eventType, payload }); const matching = BADGE_RULES.filter( (r) => r.eventType === eventType && (!r.condition || r.condition(payload)) ); for (const rule of matching) { const [badge] = await executor.select().from(badges).where(eq(badges.key, rule.badgeKey)).limit(1); if (!badge) { console.warn(`[roles/events] badge rule references unseeded badge key "${rule.badgeKey}"`); continue; } await executor .insert(memberBadges) .values({ memberId, badgeId: badge.id, sourceEvent: eventType }) .onConflictDoNothing({ target: [memberBadges.memberId, memberBadges.badgeId] }); } }import { db } from "@/lib/db"; import { workshopVolunteers, workshopProjects } from "@/lib/db/schema/workshop"; import { emitMemberEvent } from "@/lib/roles/events"; import { and, eq } from "drizzle-orm"; export class WorkshopError extends Error { constructor(public code: "not_found" | "already_active" | "not_a_volunteer") { super(code); } } export async function joinProject(projectId: string, memberId: string, role?: string): Promise<void> { const [existing] = await db .select() .from(workshopVolunteers) .where(and(eq(workshopVolunteers.projectId, projectId), eq(workshopVolunteers.memberId, memberId))) .limit(1); if (existing?.status === "active") throw new WorkshopError("already_active"); if (existing) { await db.update(workshopVolunteers) .set({ status: "active", role: role ?? existing.role }) .where(eq(workshopVolunteers.id, existing.id)); } else { await db.insert(workshopVolunteers).values({ projectId, memberId, role: role ?? null }); } } export async function leaveProject(projectId: string, memberId: string): Promise<void> { const result = await db.update(workshopVolunteers) .set({ status: "left" }) .where(and( eq(workshopVolunteers.projectId, projectId), eq(workshopVolunteers.memberId, memberId), eq(workshopVolunteers.status, "active") )) .returning({ id: workshopVolunteers.id }); if (result.length === 0) throw new WorkshopError("not_a_volunteer"); } // Only path that awards the completion badge — marking a project complete // through any other route (there is none) would silently skip badge awarding, // which is exactly why this logic lives in one place, not duplicated per-caller. export async function completeProject(projectId: string): Promise<void> { await db.transaction(async (tx) => { const [project] = await tx.select().from(workshopProjects).where(eq(workshopProjects.id, projectId)).limit(1); if (!project) throw new WorkshopError("not_found"); await tx.update(workshopProjects) .set({ status: "completed", completedAt: new Date(), progressPercent: 100 }) .where(eq(workshopProjects.id, projectId)); const activeVolunteers = await tx .select({ memberId: workshopVolunteers.memberId }) .from(workshopVolunteers) .where(and(eq(workshopVolunteers.projectId, projectId), eq(workshopVolunteers.status, "active"))); for (const v of activeVolunteers) { await emitMemberEvent(v.memberId, "workshop.project_completed", { projectId }, tx); } }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { joinProject, leaveProject, WorkshopError } from "@/lib/workshop/volunteers"; import { z } from "zod"; const joinSchema = z.object({ role: z.string().trim().max(60).optional() }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = joinSchema.safeParse(await req.json().catch(() => ({}))); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); try { await joinProject(params.id, viewer.id, parsed.data.role); return NextResponse.json({ status: "joined" }, { status: 201 }); } catch (err) { if (err instanceof WorkshopError) return NextResponse.json({ error: err.code }, { status: 409 }); throw err; } } export async function DELETE(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } try { await leaveProject(params.id, viewer.id); return NextResponse.json({ status: "left" }); } catch (err) { if (err instanceof WorkshopError) return NextResponse.json({ error: err.code }, { status: 409 }); throw err; } }import { NextRequest, NextResponse } from "next/server"; import { requireHouseAccess, AccessDeniedError } from "@/lib/auth/roles"; import { completeProject, WorkshopError } from "@/lib/workshop/volunteers"; // Gated to House tier: marking a project complete triggers real badge awards // to every active volunteer — this should not be an open-member action. export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { try { await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } try { await completeProject(params.id); return NextResponse.json({ status: "completed" }); } catch (err) { if (err instanceof WorkshopError) return NextResponse.json({ error: err.code }, { status: 404 }); console.error("[workshop/complete:POST]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { workshopDiscussionComments } from "@/lib/db/schema/workshop"; import { flagForReview } from "@/lib/moderation/flag"; import { asc, and, eq, isNull } from "drizzle-orm"; import { z } from "zod"; const commentSchema = z.object({ body: z.string().trim().min(1).max(2000) }); export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { const rows = await db.select().from(workshopDiscussionComments) .where(and(eq(workshopDiscussionComments.projectId, params.id), isNull(workshopDiscussionComments.deletedAt))) .orderBy(asc(workshopDiscussionComments.createdAt)); return NextResponse.json({ comments: rows }); } export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = commentSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [comment] = await db.insert(workshopDiscussionComments) .values({ projectId: params.id, authorId: viewer.id, body: parsed.data.body }) .returning(); return NextResponse.json({ comment }, { status: 201 }); }export type FlaggableContentType = "block_post" | "block_reply" | "workshop_update" | "workshop_comment";import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { flagForReview, FlaggableContentType } from "@/lib/moderation/flag"; import { z } from "zod"; const reportSchema = z.object({ contentType: z.enum(["block_post", "block_reply", "workshop_update", "workshop_comment"]), contentId: z.string().uuid(), reason: z.string().trim().min(1).max(500), }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = reportSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); await flagForReview({ ...parsed.data, reportedBy: viewer.id } as { contentType: FlaggableContentType; contentId: string; reason: string; reportedBy: string; }); return NextResponse.json({ status: "flagged" }, { status: 202 }); }import { pgTable, uuid, text, integer, timestamp, jsonb, pgEnum, primaryKey, index, date, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const arcadeGameKindEnum = pgEnum("arcade_game_kind", ["solo_score", "head_to_head"]); export const arcadeScoreDirectionEnum = pgEnum("arcade_score_direction", ["higher_better", "lower_better"]); export const arcadeMatchStatusEnum = pgEnum("arcade_match_status", ["pending", "active", "completed"]); // The registry every other arcade table's gameKey points at. Adding a new // game means adding one row here — nothing else needs to know about it. export const arcadeGames = pgTable("arcade_games", { key: text("key").primaryKey(), // e.g. "tetris", "war", "trivia" name: text("name").notNull(), kind: arcadeGameKindEnum("kind").notNull(), scoreDirection: arcadeScoreDirectionEnum("score_direction").notNull().default("higher_better"), active: text("active").notNull().default("true"), }); export const arcadeRatings = pgTable( "arcade_ratings", { memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), gameKey: text("game_key").notNull().references(() => arcadeGames.key), rating: integer("rating").notNull().default(1200), gamesPlayed: integer("games_played").notNull().default(0), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ pk: primaryKey({ columns: [t.memberId, t.gameKey] }), leaderboardIdx: index("arcade_ratings_leaderboard_idx").on(t.gameKey, t.rating), }) ); // Best-score-per-attempt log for solo_score games. Leaderboards read this, // not a mutable "best score" column, so history is never silently overwritten. export const arcadeScores = pgTable( "arcade_scores", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), gameKey: text("game_key").notNull().references(() => arcadeGames.key), score: integer("score").notNull(), metadata: jsonb("metadata").notNull().default({}), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ leaderboardIdx: index("arcade_scores_leaderboard_idx").on(t.gameKey, t.score) }) ); export const arcadeMatches = pgTable("arcade_matches", { id: uuid("id").defaultRandom().primaryKey(), gameKey: text("game_key").notNull().references(() => arcadeGames.key), playerAId: uuid("player_a_id").notNull().references(() => blockMembers.id), playerBId: uuid("player_b_id").notNull().references(() => blockMembers.id), status: arcadeMatchStatusEnum("status").notNull().default("active"), winnerId: uuid("winner_id").references(() => blockMembers.id), // null = draw or in progress state: jsonb("state").notNull(), // game-specific engine state, opaque to this table on purpose createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), completedAt: timestamp("completed_at", { withTimezone: true }), }); // Real queue-consumption pattern: FOR UPDATE SKIP LOCKED, not a naive SELECT. // Two members joining the queue in the same instant must never both grab // the same waiting opponent. export const arcadeMatchmakingQueue = pgTable( "arcade_matchmaking_queue", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), gameKey: text("game_key").notNull().references(() => arcadeGames.key), division: text("division").notNull(), queuedAt: timestamp("queued_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ queueIdx: index("arcade_queue_idx").on(t.gameKey, t.division, t.queuedAt) }) ); // Site-wide, not per-game — deliberately, see design note above. export const arcadeDailyStreaks = pgTable("arcade_daily_streaks", { memberId: uuid("member_id").primaryKey().references(() => blockMembers.id, { onDelete: "cascade" }), currentStreak: integer("current_streak").notNull().default(0), longestStreak: integer("longest_streak").notNull().default(0), totalActiveDays: integer("total_active_days").notNull().default(0), lastActiveDate: date("last_active_date"), });export const DIVISIONS = [ { name: "bronze", minRating: 0 }, { name: "silver", minRating: 1200 }, { name: "gold", minRating: 1400 }, { name: "platinum", minRating: 1600 }, { name: "elite", minRating: 1800 }, ] as const; export type Division = (typeof DIVISIONS)[number]["name"]; export function divisionForRating(rating: number): Division { let current: Division = "bronze"; for (const d of DIVISIONS) if (rating >= d.minRating) current = d.name; return current; }// Standard Elo rating formula (the same math chess federations use). // K=32 is a conventional choice for volatile, low-game-count populations — // a community arcade, not professional chess — so ratings move meaningfully // after a handful of games instead of taking hundreds to settle. const K_FACTOR = 32; export function computeEloUpdate( ratingA: number, ratingB: number, outcomeForA: 1 | 0.5 | 0 // 1 = A won, 0.5 = draw, 0 = A lost ): { newRatingA: number; newRatingB: number } { const expectedA = 1 / (1 + 10 ** ((ratingB - ratingA) / 400)); const expectedB = 1 - expectedA; const outcomeForB = 1 - outcomeForA; return { newRatingA: Math.round(ratingA + K_FACTOR * (outcomeForA - expectedA)), newRatingB: Math.round(ratingB + K_FACTOR * (outcomeForB - expectedB)), }; }import { db } from "@/lib/db"; import { arcadeDailyStreaks } from "@/lib/db/schema/arcade-core"; import { emitMemberEvent } from "@/lib/roles/events"; import { eq, sql } from "drizzle-orm"; function isoDate(d: Date): string { return d.toISOString().slice(0, 10); } // Called once per arcade activity (score submission or match completion). // Idempotent for repeat calls on the same calendar day — playing 10 games // in one day counts as one day of activity, not ten. export async function recordArcadeActivity(memberId: string): Promise<void> { const today = isoDate(new Date()); const [existing] = await db .select() .from(arcadeDailyStreaks) .where(eq(arcadeDailyStreaks.memberId, memberId)) .limit(1); if (!existing) { await db.insert(arcadeDailyStreaks).values({ memberId, currentStreak: 1, longestStreak: 1, totalActiveDays: 1, lastActiveDate: today, }); return; } if (existing.lastActiveDate === today) return; // already counted today, no-op const yesterday = isoDate(new Date(Date.now() - 86_400_000)); const newCurrent = existing.lastActiveDate === yesterday ? existing.currentStreak + 1 : 1; const newLongest = Math.max(existing.longestStreak, newCurrent); await db.update(arcadeDailyStreaks).set({ currentStreak: newCurrent, longestStreak: newLongest, totalActiveDays: sql`${arcadeDailyStreaks.totalActiveDays} + 1`, lastActiveDate: today, }).where(eq(arcadeDailyStreaks.memberId, memberId)); if (newCurrent > 0 && newCurrent % 7 === 0) { await emitMemberEvent(memberId, "arcade.streak_hit", { streakLength: newCurrent }); } }import { db } from "@/lib/db"; import { arcadeRatings, arcadeMatches } from "@/lib/db/schema/arcade-core"; import { divisionForRating } from "./divisions"; import { eq, and, sql } from "drizzle-orm"; export async function getOrCreateRating(memberId: string, gameKey: string): Promise<number> { const [row] = await db .select({ rating: arcadeRatings.rating }) .from(arcadeRatings) .where(and(eq(arcadeRatings.memberId, memberId), eq(arcadeRatings.gameKey, gameKey))) .limit(1); if (row) return row.rating; await db.insert(arcadeRatings).values({ memberId, gameKey }) .onConflictDoNothing({ target: [arcadeRatings.memberId, arcadeRatings.gameKey] }); return 1200; } type QueueResult = | { status: "matched"; matchId: string; opponentId: string } | { status: "queued" }; export async function joinQueueOrMatch( memberId: string, gameKey: string, initialState: unknown ): Promise<QueueResult> { const rating = await getOrCreateRating(memberId, gameKey); const division = divisionForRating(rating); return db.transaction(async (tx) => { // FOR UPDATE SKIP LOCKED: the correct Postgres idiom for a work queue — // if two members join in the same instant, they can never both claim the // same waiting opponent row. This is the pattern the Mission Board's // plain FOR UPDATE lock was a simpler stand-in for; this one is the // right tool specifically because it's a queue, not a single resource. const waiting = await tx.execute(sql` SELECT id, member_id FROM arcade_matchmaking_queue WHERE game_key = ${gameKey} AND division = ${division} AND member_id != ${memberId} ORDER BY queued_at ASC FOR UPDATE SKIP LOCKED LIMIT 1 `) as unknown as [{ id: string; member_id: string }]; const opponent = waiting[0]; if (!opponent) { await tx.execute(sql` INSERT INTO arcade_matchmaking_queue (member_id, game_key, division) VALUES (${memberId}, ${gameKey}, ${division}) `); return { status: "queued" as const }; } await tx.execute(sql`DELETE FROM arcade_matchmaking_queue WHERE id = ${opponent.id}`); const [match] = await tx.insert(arcadeMatches).values({ gameKey, playerAId: opponent.member_id, playerBId: memberId, state: initialState, }).returning({ id: arcadeMatches.id }); return { status: "matched" as const, matchId: match.id, opponentId: opponent.member_id }; }); }import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { arcadeGames, arcadeRatings, arcadeScores } from "@/lib/db/schema/arcade-core"; import { blockMembers } from "@/lib/db/schema/block-members"; import { DIVISIONS, Division } from "@/lib/arcade/divisions"; import { and, eq, desc, asc, gte, lt, sql } from "drizzle-orm"; import { z } from "zod"; const querySchema = z.object({ division: z.enum(["bronze", "silver", "gold", "platinum", "elite"]).optional(), limit: z.coerce.number().int().min(1).max(100).default(20), }); export async function GET(req: NextRequest, { params }: { params: { gameKey: string } }) { const parsed = querySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) return NextResponse.json({ error: "invalid_query" }, { status: 400 }); const { division, limit } = parsed.data; const [game] = await db.select().from(arcadeGames).where(eq(arcadeGames.key, params.gameKey)).limit(1); if (!game) return NextResponse.json({ error: "not_found" }, { status: 404 }); try { if (game.kind === "head_to_head") { const conditions = [eq(arcadeRatings.gameKey, params.gameKey)]; if (division) { const idx = DIVISIONS.findIndex((d) => d.name === division); conditions.push(gte(arcadeRatings.rating, DIVISIONS[idx].minRating)); if (idx < DIVISIONS.length - 1) conditions.push(lt(arcadeRatings.rating, DIVISIONS[idx + 1].minRating)); } const rows = await db .select({ memberId: arcadeRatings.memberId, name: blockMembers.displayName, rating: arcadeRatings.rating }) .from(arcadeRatings) .innerJoin(blockMembers, eq(arcadeRatings.memberId, blockMembers.id)) .where(and(...conditions)) .orderBy(desc(arcadeRatings.rating)) .limit(limit); return NextResponse.json({ kind: "rating", entries: rows }); } // solo_score: best score per member, respecting direction const orderFn = game.scoreDirection === "higher_better" ? desc : asc; const rows = await db .select({ memberId: arcadeScores.memberId, name: blockMembers.displayName, bestScore: sql<number>`${game.scoreDirection === "higher_better" ? sql`max` : sql`min`}(${arcadeScores.score})`, }) .from(arcadeScores) .innerJoin(blockMembers, eq(arcadeScores.memberId, blockMembers.id)) .where(eq(arcadeScores.gameKey, params.gameKey)) .groupBy(arcadeScores.memberId, blockMembers.displayName) .orderBy(orderFn(sql`bestScore`)) .limit(limit); return NextResponse.json({ kind: "score", entries: rows }); } catch (err) { console.error("[arcade/leaderboard:GET]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } } const isHigherBetter = game.scoreDirection === "higher_better"; const aggFn = isHigherBetter ? sql`MAX(${arcadeScores.score})` : sql`MIN(${arcadeScores.score})`; const rows = await db .select({ memberId: arcadeScores.memberId, name: blockMembers.displayName, bestScore: aggFn.as("best_score"), }) .from(arcadeScores) .innerJoin(blockMembers, eq(arcadeScores.memberId, blockMembers.id)) .where(eq(arcadeScores.gameKey, params.gameKey)) .groupBy(arcadeScores.memberId, blockMembers.displayName) .orderBy(isHigherBetter ? desc(sql`best_score`) : asc(sql`best_score`)) .limit(limit); return NextResponse.json({ kind: "score", entries: rows });"use client"; import { useEffect, useState } from "react"; import styles from "./leaderboard-widget.module.css"; type Entry = { memberId: string; name: string; rating?: number; bestScore?: number }; type ViewState = | { status: "loading" } | { status: "ready"; entries: Entry[]; kind: "rating" | "score" } | { status: "empty" } | { status: "error" }; // Embeddable anywhere on the site — Street, House, Workshop sidebars, etc. // "on every floor" is satisfied by this being a drop-in widget, not a page. export function ArcadeLeaderboardWidget({ gameKey, division }: { gameKey: string; division?: string }) { const [state, setState] = useState<ViewState>({ status: "loading" }); useEffect(() => { let cancelled = false; const qs = division ? `?division=${division}` : ""; fetch(`/api/arcade/leaderboard/${gameKey}${qs}`) .then((res) => { if (!res.ok) throw new Error(); return res.json(); }) .then((data) => { if (cancelled) return; setState(data.entries.length === 0 ? { status: "empty" } : { status: "ready", entries: data.entries, kind: data.kind }); }) .catch(() => { if (!cancelled) setState({ status: "error" }); }); return () => { cancelled = true; }; }, [gameKey, division]); if (state.status === "loading") return <p className={styles.status}>Loading standings…</p>; if (state.status === "error") return <p className={styles.status}>Leaderboard unavailable.</p>; if (state.status === "empty") return <p className={styles.status}>No scores yet — be the first.</p>; return ( <ol className={styles.list}> {state.entries.map((e, i) => ( <li key={e.memberId} className={styles.row}> <span className={styles.rank}>{i + 1}</span> <span className={styles.name}>{e.name}</span> <span className={styles.value}>{state.kind === "rating" ? e.rating : e.bestScore}</span> </li> ))} </ol> ); }.list { list-style: none; padding: 0; margin: 0; background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); } .row { display: flex; align-items: center; gap: 10px; padding: 8px 12px; border-bottom: 1px solid var(--border); } .row:last-child { border-bottom: none; } .rank { font-family: var(--font-mono); color: var(--accent-silver); width: 20px; } .name { flex: 1; color: var(--text-primary); } .value { font-family: var(--font-mono); color: var(--accent-gold); font-weight: 600; } .status { color: var(--text-secondary); font-style: italic; }import { db } from "@/lib/db"; import { arcadeGames } from "@/lib/db/schema/arcade-core"; const GAMES = [ { key: "trivia", name: "Daily Trivia", kind: "solo_score" as const, scoreDirection: "higher_better" as const }, { key: "tetris", name: "Tetris", kind: "solo_score" as const, scoreDirection: "higher_better" as const }, { key: "war", name: "War", kind: "head_to_head" as const, scoreDirection: "higher_better" as const }, { key: "word_scramble", name: "Word Scramble", kind: "solo_score" as const, scoreDirection: "higher_better" as const }, { key: "reaction_timer", name: "Reaction Timer", kind: "solo_score" as const, scoreDirection: "lower_better" as const }, { key: "coin_flip_streak", name: "Coin Flip Streak", kind: "solo_score" as const, scoreDirection: "higher_better" as const }, ]; export async function seedArcadeGames() { await db.insert(arcadeGames).values(GAMES).onConflictDoNothing({ target: arcadeGames.key }); console.log(`[seed:arcade] ensured ${GAMES.length} games registered`); } if (require.main === module) { seedArcadeGames().then(() => process.exit(0)).catch((e) => { console.error(e); process.exit(1); }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeGames, arcadeScores } from "@/lib/db/schema/arcade-core"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq } from "drizzle-orm"; import { z } from "zod"; const submitSchema = z.object({ score: z.number().int().min(0).max(10_000_000), // ceiling guards against a corrupted client submitting garbage metadata: z.record(z.unknown()).default({}), }); export async function POST(req: NextRequest, { params }: { params: { gameKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [game] = await db.select().from(arcadeGames).where(eq(arcadeGames.key, params.gameKey)).limit(1); if (!game || game.kind !== "solo_score") return NextResponse.json({ error: "not_found" }, { status: 404 }); const parsed = submitSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const [score] = await db.insert(arcadeScores).values({ memberId: viewer.id, gameKey: params.gameKey, score: parsed.data.score, metadata: parsed.data.metadata, }).returning(); await recordArcadeActivity(viewer.id); return NextResponse.json({ score }, { status: 201 }); }import { pgTable, uuid, text, integer, date, timestamp, uniqueIndex } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const triviaQuestions = pgTable("trivia_questions", { id: uuid("id").defaultRandom().primaryKey(), question: text("question").notNull(), choices: text("choices").array().notNull(), // exactly 4, validated at insert time in application code correctIndex: integer("correct_index").notNull(), // 0-3 category: text("category").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }); // One row per calendar day — deterministic daily question, same for everyone. export const triviaDailyRotation = pgTable("trivia_daily_rotation", { date: date("date").primaryKey(), questionId: uuid("question_id").notNull().references(() => triviaQuestions.id), }); export const triviaAttempts = pgTable( "trivia_attempts", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), date: date("date").notNull(), correct: text("correct").notNull(), // "true"/"false" — see note below createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ onePerDay: uniqueIndex("trivia_attempts_member_date_uq").on(t.memberId, t.date) }) );export type Suit = "♠" | "♥" | "♦" | "♣"; export type Card = { rank: number; suit: Suit }; // rank: 2-14, 14 = Ace export type WarState = { deckA: Card[]; deckB: Card[]; round: number; log: string[]; winnerId: "A" | "B" | "draw" | null; }; const SUITS: Suit[] = ["♠", "♥", "♦", "♣"]; const MAX_ROUNDS = 1000; // named safeguard against the real, documented infinite-War edge case export function buildShuffledDecks(): { deckA: Card[]; deckB: Card[] } { const deck: Card[] = []; for (const suit of SUITS) for (let rank = 2; rank <= 14; rank++) deck.push({ rank, suit }); // Fisher-Yates, using Math.random — fine for a card game with no money on // the line; NOT the choice you'd make if this ever became a real-money // gambling feature (it explicitly must not, per the marketplace scoping rule). for (let i = deck.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [deck[i], deck[j]] = [deck[j], deck[i]]; } return { deckA: deck.slice(0, 26), deckB: deck.slice(26) }; } function playWarChain(deckA: Card[], deckB: Card[], pile: Card[]): "A" | "B" | "exhausted-A" | "exhausted-B" { // Real edge case: a player with fewer than 4 cards left can't do a full // war chain (1 face-down x3 + 1 face-up). House rule applied here: they // stake everything they have except their final card, which is their // face-up. If they have 0 cards at all, they lose immediately. const stakeCount = (deck: Card[]) => Math.max(0, Math.min(3, deck.length - 1)); if (deckA.length === 0) return "exhausted-A"; if (deckB.length === 0) return "exhausted-B"; const stakeA = stakeCount(deckA); const stakeB = stakeCount(deckB); for (let i = 0; i < stakeA; i++) pile.push(deckA.shift()!); for (let i = 0; i < stakeB; i++) pile.push(deckB.shift()!); if (deckA.length === 0) return "exhausted-A"; if (deckB.length === 0) return "exhausted-B"; const faceUpA = deckA.shift()!; const faceUpB = deckB.shift()!; pile.push(faceUpA, faceUpB); if (faceUpA.rank > faceUpB.rank) return "A"; if (faceUpB.rank > faceUpA.rank) return "B"; return playWarChain(deckA, deckB, pile); // another tie — recurse into a deeper war } export function playRound(state: WarState): WarState { if (state.winnerId) return state; // game already decided, no-op const { deckA, deckB } = state; if (state.round >= MAX_ROUNDS) { // Named safeguard triggered: declare by card count rather than hang forever. const winnerId = deckA.length === deckB.length ? "draw" : deckA.length > deckB.length ? "A" : "B"; return { ...state, winnerId, log: [...state.log, `Round cap (${MAX_ROUNDS}) reached — decided by card count.`] }; } const cardA = deckA.shift(); const cardB = deckB.shift(); if (!cardA && !cardB) return { ...state, winnerId: "draw", log: [...state.log, "Both decks exhausted simultaneously."] }; if (!cardA) return { ...state, winnerId: "B", log: [...state.log, "Player A ran out of cards."] }; if (!cardB) return { ...state, winnerId: "A", log: [...state.log, "Player B ran out of cards."] }; const pile: Card[] = [cardA, cardB]; let roundLog = `Round ${state.round + 1}: A=${cardA.rank}${cardA.suit} vs B=${cardB.rank}${cardB.suit}`; let roundWinner: "A" | "B" | "exhausted-A" | "exhausted-B"; if (cardA.rank > cardB.rank) roundWinner = "A"; else if (cardB.rank > cardA.rank) roundWinner = "B"; else { roundLog += " — WAR!"; roundWinner = playWarChain(deckA, deckB, pile); } const newDeckA = [...deckA]; const newDeckB = [...deckB]; if (roundWinner === "A") newDeckA.push(...pile); else if (roundWinner === "B") newDeckB.push(...pile); // "exhausted-A"/"exhausted-B" fall through to the game-over check below, // pile cards are irrelevant since the game ends here. const nextState: WarState = { deckA: newDeckA, deckB: newDeckB, round: state.round + 1, log: [...state.log, roundLog], winnerId: null, }; if (roundWinner === "exhausted-A" || newDeckA.length === 0 && newDeckB.length > 0) return { ...nextState, winnerId: "B" }; if (roundWinner === "exhausted-B" || newDeckB.length === 0 && newDeckA.length > 0) return { ...nextState, winnerId: "A" }; return nextState; }// Run with: node scripts/test-war-engine.mjs // This is the concrete, executable check for the riskiest logic in this pass — // per the Witness principle, this is what turns "I traced it by hand" into // an actual recorded observation once you run it. import { buildShuffledDecks, playRound } from "../src/lib/arcade/war/engine.ts"; let { deckA, deckB } = buildShuffledDecks(); let state = { deckA, deckB, round: 0, log: [], winnerId: null }; let iterations = 0; while (!state.winnerId && iterations < 2000) { state = playRound(state); iterations++; } console.log(`Finished after ${iterations} rounds. Winner: ${state.winnerId}`); console.log(`Final card counts — A: ${state.deckA.length}, B: ${state.deckB.length}`); if (!state.winnerId) { console.error("FAIL: game did not terminate within 2000 iterations — the round cap safeguard did not work."); process.exit(1); } if (state.deckA.length + state.deckB.length !== 52 && state.winnerId !== "draw") { console.error(`FAIL: card count doesn't sum to 52 — cards were lost or duplicated. Total: ${state.deckA.length + state.deckB.length}`); process.exit(1); } console.log("PASS: game terminated and card accounting is consistent.");import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { joinQueueOrMatch } from "@/lib/arcade/matchmaking"; import { buildShuffledDecks } from "@/lib/arcade/war/engine"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const { deckA, deckB } = buildShuffledDecks(); const result = await joinQueueOrMatch(viewer.id, "war", { deckA, deckB, round: 0, log: [], winnerId: null }); return NextResponse.json(result); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches, arcadeRatings } from "@/lib/db/schema/arcade-core"; import { playRound, WarState } from "@/lib/arcade/war/engine"; import { computeEloUpdate } from "@/lib/arcade/elo"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, or, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [match] = await db.select().from(arcadeMatches).where(eq(arcadeMatches.id, params.matchId)).limit(1); if (!match) return NextResponse.json({ error: "not_found" }, { status: 404 }); if (match.playerAId !== viewer.id && match.playerBId !== viewer.id) { return NextResponse.json({ error: "forbidden" }, { status: 403 }); } if (match.status === "completed") return NextResponse.json({ state: match.state, status: "completed" }); const newState = playRound(match.state as WarState); const isDone = newState.winnerId !== null; await db.update(arcadeMatches).set({ state: newState, status: isDone ? "completed" : "active", completedAt: isDone ? new Date() : null, winnerId: isDone && newState.winnerId !== "draw" ? (newState.winnerId === "A" ? match.playerAId : match.playerBId) : null, }).where(eq(arcadeMatches.id, params.matchId)); if (isDone && newState.winnerId !== "draw") { const ratingA = await getOrCreateRating(match.playerAId, "war"); const ratingB = await getOrCreateRating(match.playerBId, "war"); const outcomeForA = newState.winnerId === "A" ? 1 : 0; const { newRatingA, newRatingB } = computeEloUpdate(ratingA, ratingB, outcomeForA); await db.update(arcadeRatings).set({ rating: newRatingA, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.playerAId), eq(arcadeRatings.gameKey, "war"))); await db.update(arcadeRatings).set({ rating: newRatingB, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.playerBId), eq(arcadeRatings.gameKey, "war"))); await recordArcadeActivity(match.playerAId); await recordArcadeActivity(match.playerBId); } return NextResponse.json({ state: newState, status: isDone ? "completed" : "active" }); }// Small real word list — deliberately not a huge dictionary; this is meant // to be a 30-second daily distraction, not a competitive word game. export const SCRAMBLE_WORDS = [ "MERCY", "FAITH", "HARBOR", "COURAGE", "SHELTER", "WITNESS", "COVENANT", "REFUGE", "STEADFAST", "KINDNESS", "RENEWAL", "PURPOSE", ]; export function scramble(word: string): string { const letters = word.split(""); for (let i = letters.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [letters[i], letters[j]] = [letters[j], letters[i]]; } // Guard against a scramble that happens to equal the original word — // rare with longer words but a real bug if left unhandled on a 4-letter word. const result = letters.join(""); return result === word ? scramble(word) : result; }import { NextResponse } from "next/server"; import { SCRAMBLE_WORDS, scramble } from "@/lib/arcade/word-scramble/words"; export async function GET() { const word = SCRAMBLE_WORDS[Math.floor(Math.random() * SCRAMBLE_WORDS.length)]; // The answer is never sent — only the scrambled letters and a length hint. // Verification happens server-side in the submit route below. return NextResponse.json({ scrambled: scramble(word), length: word.length, roundId: word }); // NOTE: using the plaintext word as roundId is a real, named shortcut — // it's trivially crackable by reading network traffic, but this is a // zero-stakes word game with no leaderboard integrity requirement beyond // "did you actually unscramble it," so this doesn't rise to a security concern. }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeScores } from "@/lib/db/schema/arcade-core"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { z } from "zod"; const schema = z.object({ roundId: z.string(), guess: z.string().max(50) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const correct = parsed.data.guess.trim().toUpperCase() === parsed.data.roundId.toUpperCase(); if (correct) { await db.insert(arcadeScores).values({ memberId: viewer.id, gameKey: "word_scramble", score: 1 }); await recordArcadeActivity(viewer.id); } return NextResponse.json({ correct }); }// src/app/api/arcade/games/reaction-timer/score/route.ts // Reuses the shared solo-score endpoint entirely — reaction time IS the score, // with scoreDirection: "lower_better" already configured in the games registry. // No separate route needed; the client just POSTs its measured ms to // /api/arcade/games/reaction_timer/score. Documented here instead of a // duplicate file that would just re-wrap the shared handler for no reason.import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeScores } from "@/lib/db/schema/arcade-core"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { z } from "zod"; const schema = z.object({ guess: z.enum(["heads", "tails"]), currentStreak: z.number().int().min(0).max(1000) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); // Server flips the coin — the client cannot influence or predict this. const result: "heads" | "tails" = Math.random() < 0.5 ? "heads" : "tails"; const won = result === parsed.data.guess; const newStreak = won ? parsed.data.currentStreak + 1 : 0; if (!won && parsed.data.currentStreak > 0) { await db.insert(arcadeScores).values({ memberId: viewer.id, gameKey: "coin_flip_streak", score: parsed.data.currentStreak }); } await recordArcadeActivity(viewer.id); return NextResponse.json({ result, won, newStreak }); }import { db } from "@/lib/db"; import { arcadeRatings, arcadeMatches } from "@/lib/db/schema/arcade-core"; import { divisionForRating } from "./divisions"; import { eq, and, sql } from "drizzle-orm"; export async function getOrCreateRating(memberId: string, gameKey: string): Promise<number> { const [row] = await db .select({ rating: arcadeRatings.rating }) .from(arcadeRatings) .where(and(eq(arcadeRatings.memberId, memberId), eq(arcadeRatings.gameKey, gameKey))) .limit(1); if (row) return row.rating; await db.insert(arcadeRatings).values({ memberId, gameKey }) .onConflictDoNothing({ target: [arcadeRatings.memberId, arcadeRatings.gameKey] }); return 1200; } type QueueResult = | { status: "matched"; matchId: string; opponentId: string } | { status: "queued" }; /** * CHANGED this pass: `buildInitialState` is now a function invoked with * (opponentId, selfId) once a match is confirmed, not a plain object built * blind beforehand. This lets asymmetric games (Chess: who's white/black) * assign roles correctly, and removes a placeholder-state race window. * War's caller was updated to match — see below. */ export async function joinQueueOrMatch( memberId: string, gameKey: string, buildInitialState: (opponentId: string, selfId: string) => unknown ): Promise<QueueResult> { const rating = await getOrCreateRating(memberId, gameKey); const division = divisionForRating(rating); return db.transaction(async (tx) => { const waiting = await tx.execute(sql` SELECT id, member_id FROM arcade_matchmaking_queue WHERE game_key = ${gameKey} AND division = ${division} AND member_id != ${memberId} ORDER BY queued_at ASC FOR UPDATE SKIP LOCKED LIMIT 1 `) as unknown as [{ id: string; member_id: string }]; const opponent = waiting[0]; if (!opponent) { // ON CONFLICT: repeated join-clicks refresh queue position instead of // stacking duplicate rows (closed by the new unique index below). await tx.execute(sql` INSERT INTO arcade_matchmaking_queue (member_id, game_key, division) VALUES (${memberId}, ${gameKey}, ${division}) ON CONFLICT (member_id, game_key) DO UPDATE SET queued_at = now(), division = EXCLUDED.division `); return { status: "queued" as const }; } await tx.execute(sql`DELETE FROM arcade_matchmaking_queue WHERE id = ${opponent.id}`); const initialState = buildInitialState(opponent.member_id, memberId); const [match] = await tx.insert(arcadeMatches).values({ gameKey, playerAId: opponent.member_id, // whoever was already waiting playerBId: memberId, // whoever just joined and triggered the match state: initialState, }).returning({ id: arcadeMatches.id }); return { status: "matched" as const, matchId: match.id, opponentId: opponent.member_id }; }); }import { uniqueIndex } from "drizzle-orm/pg-core"; // add to existing import line export const arcadeMatchmakingQueue = pgTable( "arcade_matchmaking_queue", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), gameKey: text("game_key").notNull().references(() => arcadeGames.key), division: text("division").notNull(), queuedAt: timestamp("queued_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ queueIdx: index("arcade_queue_idx").on(t.gameKey, t.division, t.queuedAt), onePerMemberPerGame: uniqueIndex("arcade_queue_member_game_uq").on(t.memberId, t.gameKey), }) );import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { joinQueueOrMatch } from "@/lib/arcade/matchmaking"; import { buildShuffledDecks } from "@/lib/arcade/war/engine"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const result = await joinQueueOrMatch(viewer.id, "war", () => { const { deckA, deckB } = buildShuffledDecks(); return { deckA, deckB, round: 0, log: [], winnerId: null }; }); return NextResponse.json(result); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches, arcadeRatings } from "@/lib/db/schema/arcade-core"; import { playRound, WarState } from "@/lib/arcade/war/engine"; import { computeEloUpdate } from "@/lib/arcade/elo"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const outcome = await db.transaction(async (tx) => { const locked = await tx.execute( sql`SELECT * FROM arcade_matches WHERE id = ${params.matchId} FOR UPDATE` ) as unknown as [any]; const match = locked[0]; if (!match) return { httpStatus: 404 as const, body: { error: "not_found" } }; if (match.player_a_id !== viewer.id && match.player_b_id !== viewer.id) { return { httpStatus: 403 as const, body: { error: "forbidden" } }; } if (match.status === "completed") { return { httpStatus: 200 as const, body: { state: match.state, status: "completed" } }; } const newState = playRound(match.state as WarState); const isDone = newState.winnerId !== null; await tx.update(arcadeMatches).set({ state: newState, status: isDone ? "completed" : "active", completedAt: isDone ? new Date() : null, winnerId: isDone && newState.winnerId !== "draw" ? (newState.winnerId === "A" ? match.player_a_id : match.player_b_id) : null, }).where(eq(arcadeMatches.id, params.matchId)); return { httpStatus: 200 as const, body: { state: newState, status: isDone ? "completed" : "active" }, isDone, newState, match, }; }); if (outcome.httpStatus !== 200 || !("isDone" in outcome)) { return NextResponse.json(outcome.body, { status: outcome.httpStatus }); } // Elo/streak writes happen after the transaction commits, outside the row // lock — keeps the lock's duration limited to the deck mutation itself. if (outcome.isDone && outcome.newState.winnerId !== "draw") { const { match, newState } = outcome; const ratingA = await getOrCreateRating(match.player_a_id, "war"); const ratingB = await getOrCreateRating(match.player_b_id, "war"); const outcomeForA = newState.winnerId === "A" ? 1 : 0; const { newRatingA, newRatingB } = computeEloUpdate(ratingA, ratingB, outcomeForA); await db.update(arcadeRatings).set({ rating: newRatingA, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_a_id), eq(arcadeRatings.gameKey, "war"))); await db.update(arcadeRatings).set({ rating: newRatingB, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_b_id), eq(arcadeRatings.gameKey, "war"))); await recordArcadeActivity(match.player_a_id); await recordArcadeActivity(match.player_b_id); } return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches } from "@/lib/db/schema/arcade-core"; import { and, eq, or, desc } from "drizzle-orm"; export async function GET(_req: NextRequest, { params }: { params: { gameKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [match] = await db .select() .from(arcadeMatches) .where(and( eq(arcadeMatches.gameKey, params.gameKey), or(eq(arcadeMatches.playerAId, viewer.id), eq(arcadeMatches.playerBId, viewer.id)), eq(arcadeMatches.status, "active") )) .orderBy(desc(arcadeMatches.createdAt)) .limit(1); return NextResponse.json({ match: match ?? null }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { gameKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } await db.execute(sql`DELETE FROM arcade_matchmaking_queue WHERE member_id = ${viewer.id} AND game_key = ${params.gameKey}`); return NextResponse.json({ status: "left_queue" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches } from "@/lib/db/schema/arcade-core"; import { eq } from "drizzle-orm"; // Generic across every head_to_head arcade game. This exists because two // games (War, Chess) needed opponent-state sync before Section 16 (the real // notifications/real-time layer) has been built. It is deliberately a // stand-in — POLLING, not WebSockets/SSE — so that when Section 16 lands, // there is exactly one call site to redirect, not three separate ad-hoc // mechanisms invented per-game. export async function GET(_req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [match] = await db.select().from(arcadeMatches).where(eq(arcadeMatches.id, params.matchId)).limit(1); if (!match) return NextResponse.json({ error: "not_found" }, { status: 404 }); if (match.playerAId !== viewer.id && match.playerBId !== viewer.id) { return NextResponse.json({ error: "forbidden" }, { status: 403 }); } return NextResponse.json({ match }); }"use client"; import { useEffect, useState, useCallback, useRef } from "react"; export function useMatchPolling<TState>(matchId: string, intervalMs = 1500) { const [match, setMatch] = useState<{ id: string; state: TState; status: string; winnerId: string | null } | null>(null); const [error, setError] = useState<string | null>(null); const statusRef = useRef<string | undefined>(undefined); const fetchOnce = useCallback(async () => { try { const res = await fetch(`/api/arcade/matches/${matchId}`); if (!res.ok) throw new Error(`status ${res.status}`); const data = await res.json(); setMatch(data.match); statusRef.current = data.match?.status; setError(null); } catch { setError("Couldn't sync match state."); } }, [matchId]); useEffect(() => { fetchOnce(); const interval = setInterval(() => { if (statusRef.current !== "completed") fetchOnce(); }, intervalMs); return () => clearInterval(interval); }, [fetchOnce, intervalMs]); return { match, error, refetch: fetchOnce }; }npm install chess.jsimport { Chess } from "chess.js"; export type ChessMatchState = { fen: string; pgn: string; whitePlayerId: string; blackPlayerId: string; drawOfferedBy: string | null; lastMoveAt: string; }; export type MoveResult = | { ok: true; state: ChessMatchState; gameOver: false } | { ok: true; state: ChessMatchState; gameOver: true; result: "checkmate" | "stalemate" | "draw"; winnerColor: "w" | "b" | null } | { ok: false; error: "illegal_move" | "not_your_turn" | "game_over" }; // Invariant this whole build depends on: whoever was ALREADY WAITING in the // matchmaking queue is assigned white (playerA); whoever just joined and // triggered the match is black (playerB). Set once, here, and never // reassigned — every downstream Elo/winnerId calculation assumes it holds. export function createInitialChessState(whitePlayerId: string, blackPlayerId: string): ChessMatchState { const chess = new Chess(); return { fen: chess.fen(), pgn: "", whitePlayerId, blackPlayerId, drawOfferedBy: null, lastMoveAt: new Date().toISOString(), }; } function colorForPlayer(state: ChessMatchState, playerId: string): "w" | "b" | null { if (playerId === state.whitePlayerId) return "w"; if (playerId === state.blackPlayerId) return "b"; return null; } export function applyMove( state: ChessMatchState, playerId: string, move: { from: string; to: string; promotion?: string } ): MoveResult { const chess = new Chess(state.fen); if (chess.isGameOver()) return { ok: false, error: "game_over" }; const playerColor = colorForPlayer(state, playerId); if (!playerColor || chess.turn() !== playerColor) return { ok: false, error: "not_your_turn" }; let result; try { // v1.x throws on illegal moves; older versions return null. Handling // both means this file degrades gracefully instead of crashing the // route if the installed version differs from what's targeted above. result = chess.move({ from: move.from, to: move.to, promotion: move.promotion ?? "q" }); } catch { result = null; } if (!result) return { ok: false, error: "illegal_move" }; const newState: ChessMatchState = { ...state, fen: chess.fen(), pgn: chess.pgn(), drawOfferedBy: null, // any completed move cancels a pending draw offer lastMoveAt: new Date().toISOString(), }; if (chess.isGameOver()) { if (chess.isCheckmate()) { return { ok: true, state: newState, gameOver: true, result: "checkmate", winnerColor: playerColor }; } return { ok: true, state: newState, gameOver: true, result: chess.isStalemate() ? "stalemate" : "draw", winnerColor: null, }; } return { ok: true, state: newState, gameOver: false }; }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { joinQueueOrMatch } from "@/lib/arcade/matchmaking"; import { createInitialChessState } from "@/lib/arcade/chess/engine"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const result = await joinQueueOrMatch(viewer.id, "chess", (opponentId, selfId) => createInitialChessState(opponentId, selfId) // opponent (already queued) = white, self (new joiner) = black ); if (result.status === "matched") { return NextResponse.json({ status: "matched", matchId: result.matchId, yourColor: "b" }); } return NextResponse.json({ status: "queued" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches, arcadeRatings } from "@/lib/db/schema/arcade-core"; import { applyMove, ChessMatchState } from "@/lib/arcade/chess/engine"; import { computeEloUpdate } from "@/lib/arcade/elo"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, sql } from "drizzle-orm"; import { z } from "zod"; const moveSchema = z.object({ from: z.string().regex(/^[a-h][1-8]$/), to: z.string().regex(/^[a-h][1-8]$/), promotion: z.enum(["q", "r", "b", "n"]).optional(), }); export async function POST(req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = moveSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const outcome = await db.transaction(async (tx) => { const locked = await tx.execute( sql`SELECT * FROM arcade_matches WHERE id = ${params.matchId} FOR UPDATE` ) as unknown as [any]; const match = locked[0]; if (!match) return { httpStatus: 404 as const, body: { error: "not_found" } }; if (match.player_a_id !== viewer.id && match.player_b_id !== viewer.id) { return { httpStatus: 403 as const, body: { error: "forbidden" } }; } if (match.status === "completed") return { httpStatus: 409 as const, body: { error: "game_over" } }; const result = applyMove(match.state as ChessMatchState, viewer.id, parsed.data); if (!result.ok) { const statusMap = { illegal_move: 400, not_your_turn: 403, game_over: 409 } as const; return { httpStatus: statusMap[result.error], body: { error: result.error } }; } await tx.update(arcadeMatches).set({ state: result.state, status: result.gameOver ? "completed" : "active", completedAt: result.gameOver ? new Date() : null, winnerId: result.gameOver && result.winnerColor ? (result.winnerColor === "w" ? match.player_a_id : match.player_b_id) : null, }).where(eq(arcadeMatches.id, params.matchId)); return { httpStatus: 200 as const, body: { state: result.state, gameOver: result.gameOver }, gameOver: result.gameOver, winnerColor: result.gameOver ? result.winnerColor : undefined, match, }; }); if (outcome.httpStatus !== 200 || !("gameOver" in outcome) || !outcome.gameOver) { return NextResponse.json(outcome.body, { status: outcome.httpStatus }); } // Correct Elo handling for all three real outcomes — win, loss, AND draw // (draws use outcomeForA = 0.5, the standard chess-rating treatment). // White is always playerA by the invariant set in createInitialChessState. const { match, winnerColor } = outcome; const ratingA = await getOrCreateRating(match.player_a_id, "chess"); const ratingB = await getOrCreateRating(match.player_b_id, "chess"); const outcomeForA = winnerColor === "w" ? 1 : winnerColor === "b" ? 0 : 0.5; const { newRatingA, newRatingB } = computeEloUpdate(ratingA, ratingB, outcomeForA); await db.update(arcadeRatings).set({ rating: newRatingA, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_a_id), eq(arcadeRatings.gameKey, "chess"))); await db.update(arcadeRatings).set({ rating: newRatingB, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_b_id), eq(arcadeRatings.gameKey, "chess"))); await recordArcadeActivity(match.player_a_id); await recordArcadeActivity(match.player_b_id); return NextResponse.json(outcome.body); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches, arcadeRatings } from "@/lib/db/schema/arcade-core"; import { computeEloUpdate } from "@/lib/arcade/elo"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM arcade_matches WHERE id = ${params.matchId} FOR UPDATE`) as unknown as [any]; const match = locked[0]; if (!match) return null; if (match.player_a_id !== viewer.id && match.player_b_id !== viewer.id) return null; if (match.status === "completed") return { alreadyDone: true as const }; const winnerId = match.player_a_id === viewer.id ? match.player_b_id : match.player_a_id; await tx.update(arcadeMatches).set({ status: "completed", completedAt: new Date(), winnerId }).where(eq(arcadeMatches.id, params.matchId)); return { match, winnerId, resignedId: viewer.id }; }); if (!outcome) return NextResponse.json({ error: "not_found_or_forbidden" }, { status: 404 }); if ("alreadyDone" in outcome) return NextResponse.json({ status: "already_completed" }); const { winnerId, resignedId } = outcome; const ratingWinner = await getOrCreateRating(winnerId, "chess"); const ratingLoser = await getOrCreateRating(resignedId, "chess"); const { newRatingA, newRatingB } = computeEloUpdate(ratingWinner, ratingLoser, 1); await db.update(arcadeRatings).set({ rating: newRatingA, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, winnerId), eq(arcadeRatings.gameKey, "chess"))); await db.update(arcadeRatings).set({ rating: newRatingB, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, resignedId), eq(arcadeRatings.gameKey, "chess"))); await recordArcadeActivity(winnerId); await recordArcadeActivity(resignedId); return NextResponse.json({ status: "resigned", winnerId }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { arcadeMatches, arcadeRatings } from "@/lib/db/schema/arcade-core"; import { ChessMatchState } from "@/lib/arcade/chess/engine"; import { computeEloUpdate } from "@/lib/arcade/elo"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, sql } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ action: z.enum(["offer", "accept", "decline"]) }); export async function POST(req: NextRequest, { params }: { params: { matchId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM arcade_matches WHERE id = ${params.matchId} FOR UPDATE`) as unknown as [any]; const match = locked[0]; if (!match) return { httpStatus: 404 as const, body: { error: "not_found" } }; if (match.player_a_id !== viewer.id && match.player_b_id !== viewer.id) return { httpStatus: 403 as const, body: { error: "forbidden" } }; if (match.status === "completed") return { httpStatus: 409 as const, body: { error: "game_over" } }; const state = match.state as ChessMatchState; if (parsed.data.action === "offer") { await tx.update(arcadeMatches).set({ state: { ...state, drawOfferedBy: viewer.id } }).where(eq(arcadeMatches.id, params.matchId)); return { httpStatus: 200 as const, body: { status: "offered" } }; } if (parsed.data.action === "decline") { await tx.update(arcadeMatches).set({ state: { ...state, drawOfferedBy: null } }).where(eq(arcadeMatches.id, params.matchId)); return { httpStatus: 200 as const, body: { status: "declined" } }; } if (!state.drawOfferedBy || state.drawOfferedBy === viewer.id) { return { httpStatus: 409 as const, body: { error: "no_pending_offer" } }; } await tx.update(arcadeMatches).set({ state: { ...state, drawOfferedBy: null }, status: "completed", completedAt: new Date(), winnerId: null, }).where(eq(arcadeMatches.id, params.matchId)); return { httpStatus: 200 as const, body: { status: "drawn" }, drawn: true as const, match }; }); if (outcome.httpStatus !== 200) return NextResponse.json(outcome.body, { status: outcome.httpStatus }); if ("drawn" in outcome && outcome.drawn) { const { match } = outcome; const ratingA = await getOrCreateRating(match.player_a_id, "chess"); const ratingB = await getOrCreateRating(match.player_b_id, "chess"); const { newRatingA, newRatingB } = computeEloUpdate(ratingA, ratingB, 0.5); await db.update(arcadeRatings).set({ rating: newRatingA, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_a_id), eq(arcadeRatings.gameKey, "chess"))); await db.update(arcadeRatings).set({ rating: newRatingB, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, match.player_b_id), eq(arcadeRatings.gameKey, "chess"))); await recordArcadeActivity(match.player_a_id); await recordArcadeActivity(match.player_b_id); } return NextResponse.json(outcome.body); }"use client"; import { useState, useMemo } from "react"; import { Chess } from "chess.js"; import { useMatchPolling } from "@/hooks/use-match-polling"; import type { ChessMatchState } from "@/lib/arcade/chess/engine"; import styles from "./chess-board.module.css"; const FILES = ["a", "b", "c", "d", "e", "f", "g", "h"]; const RANKS = ["8", "7", "6", "5", "4", "3", "2", "1"]; const PIECE_GLYPHS: Record<string, string> = { wp: "♙", wr: "♖", wn: "♘", wb: "♗", wq: "♕", wk: "♔", bp: "♟", br: "♜", bn: "♞", bb: "♝", bq: "♛", bk: "♚", }; export function ChessBoard({ matchId, viewerId }: { matchId: string; viewerId: string }) { const { match, error } = useMatchPolling<ChessMatchState>(matchId); const [selected, setSelected] = useState<string | null>(null); const [pending, setPending] = useState(false); const [moveError, setMoveError] = useState<string | null>(null); const chess = useMemo(() => (match ? new Chess(match.state.fen) : null), [match?.state.fen]); if (error) return <p className={styles.status}>{error}</p>; if (!match || !chess) return <p className={styles.status}>Loading match…</p>; const state = match.state; const myColor = viewerId === state.whitePlayerId ? "w" : "b"; const isMyTurn = chess.turn() === myColor && match.status !== "completed"; const legalTargets = selected ? chess.moves({ square: selected as any, verbose: true }).map((m: any) => m.to) : []; async function handleSquareClick(square: string) { if (!isMyTurn || pending || !chess) return; setMoveError(null); if (selected && legalTargets.includes(square)) { setPending(true); try { const res = await fetch(`/api/arcade/games/chess/${matchId}/move`, { method: "POST", headers: { "Content-Type": "application/json" }, // Promotion hardcoded to queen — a promotion-choice UI is a real, // named follow-up polish item, not built in this pass. body: JSON.stringify({ from: selected, to: square, promotion: "q" }), }); if (!res.ok) { const body = await res.json().catch(() => ({})); setMoveError(body.error ?? "move_failed"); } } finally { setSelected(null); setPending(false); } return; } const piece = chess.get(square as any); setSelected(piece && piece.color === myColor ? square : null); } async function respondDraw(action: "offer" | "accept" | "decline") { await fetch(`/api/arcade/games/chess/${matchId}/draw-offer`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action }), }); } async function resign() { if (!confirm("Resign this game?")) return; await fetch(`/api/arcade/games/chess/${matchId}/resign`, { method: "POST" }); } return ( <div className={styles.wrapper}> <div className={styles.board}> {RANKS.map((rank) => FILES.map((file) => { const square = `${file}${rank}`; const piece = chess.get(square as any); const isDark = (FILES.indexOf(file) + RANKS.indexOf(rank)) % 2 === 1; return ( <button key={square} className={[ styles.square, isDark ? styles.dark : styles.light, selected === square ? styles.selected : "", legalTargets.includes(square) ? styles.legalTarget : "", ].join(" ")} onClick={() => handleSquareClick(square)} disabled={!isMyTurn} aria-label={square} > {piece && ( <span className={piece.color === "w" ? styles.whitePiece : styles.blackPiece}> {PIECE_GLYPHS[`${piece.color}${piece.type}`]} </span> )} </button> ); }) )} </div> <div className={styles.sidebar}> <p>You are: <strong>{myColor === "w" ? "White" : "Black"}</strong></p> <p>{match.status === "completed" ? "Game over" : isMyTurn ? "Your move" : "Opponent's move"}</p> {state.drawOfferedBy && state.drawOfferedBy !== viewerId && match.status !== "completed" && ( <div className={styles.drawOffer}> <p>Opponent offers a draw.</p> <button onClick={() => respondDraw("accept")}>Accept</button> <button onClick={() => respondDraw("decline")}>Decline</button> </div> )} {match.status !== "completed" && ( <div className={styles.actions}> <button onClick={() => respondDraw("offer")}>Offer draw</button> <button onClick={resign} className={styles.resignBtn}>Resign</button> </div> )} {moveError && <p className={styles.error}>{moveError.replaceAll("_", " ")}</p>} </div> </div> ); }.wrapper { display: flex; gap: 24px; align-items: flex-start; } .board { display: grid; grid-template-columns: repeat(8, 48px); grid-template-rows: repeat(8, 48px); border: 2px solid var(--border); border-radius: var(--radius-sm); overflow: hidden; } .square { width: 48px; height: 48px; display: flex; align-items: center; justify-content: center; font-size: 30px; border: none; cursor: pointer; padding: 0; } .square:disabled { cursor: default; } .light { background: #2A241E; } .dark { background: #14100F; } .selected { outline: 2px solid var(--accent-gold); outline-offset: -2px; } .legalTarget { box-shadow: inset 0 0 0 8px rgba(212, 175, 55, 0.25); } .whitePiece { color: var(--accent-silver); } .blackPiece { color: #0A0908; -webkit-text-stroke: 1px var(--accent-gold); } .sidebar { font-family: var(--font-mono); color: var(--text-primary); min-width: 180px; } .drawOffer, .actions { margin-top: 12px; display: flex; flex-direction: column; gap: 6px; } .resignBtn { color: var(--danger-text); border-color: var(--danger); } .status { color: var(--text-secondary); font-style: italic; } .error { color: var(--danger-text); font-size: 0.8rem; }{ key: "chess", name: "Chess", kind: "head_to_head" as const, scoreDirection: "higher_better" as const },npm install -D tsx npx tsx scripts/test-war-engine.mjs npx tsx scripts/test-mystery-redaction.mjs # new script, belowimport { pgTable, uuid, text, integer, timestamp, jsonb, pgEnum, uniqueIndex, index, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const mysteryLobbyStatusEnum = pgEnum("mystery_lobby_status", ["waiting", "active", "completed"]); export const mysteryLobbies = pgTable("mystery_lobbies", { id: uuid("id").defaultRandom().primaryKey(), status: mysteryLobbyStatusEnum("status").notNull().default("waiting"), createdBy: uuid("created_by").notNull().references(() => blockMembers.id), minPlayers: integer("min_players").notNull().default(3), maxPlayers: integer("max_players").notNull().default(6), // Full server-authoritative state (solution, all hands, log) once active. // NEVER sent to a client unredacted — see lib/arcade/mystery/redact.ts. state: jsonb("state"), winnerId: uuid("winner_id").references(() => blockMembers.id), // null = unsolved OR still in progress createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), startedAt: timestamp("started_at", { withTimezone: true }), completedAt: timestamp("completed_at", { withTimezone: true }), }, (t) => ({ statusIdx: index("mystery_lobbies_status_idx").on(t.status) })); export const mysteryLobbyPlayers = pgTable("mystery_lobby_players", { id: uuid("id").defaultRandom().primaryKey(), lobbyId: uuid("lobby_id").notNull().references(() => mysteryLobbies.id, { onDelete: "cascade" }), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), seatOrder: integer("seat_order"), // assigned at start(); null while waiting joinedAt: timestamp("joined_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ oneRowPerMember: uniqueIndex("mystery_lobby_players_uq").on(t.lobbyId, t.memberId), })); // Deliberately its OWN table, not a field inside the jsonb state blob. // A private reveal is the single most sensitive piece of data in this // entire feature — separating it structurally means the redaction function // has nothing to accidentally leak, because it never even queries this table // for anyone except the entitled suggester. export const mysteryPrivateReveals = pgTable("mystery_private_reveals", { id: uuid("id").defaultRandom().primaryKey(), lobbyId: uuid("lobby_id").notNull().references(() => mysteryLobbies.id, { onDelete: "cascade" }), suggestionIndex: integer("suggestion_index").notNull(), suggesterId: uuid("suggester_id").notNull().references(() => blockMembers.id), disproverId: uuid("disprover_id").notNull().references(() => blockMembers.id), cardId: text("card_id").notNull(), revealedAt: timestamp("revealed_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ suggesterIdx: index("mystery_reveals_suggester_idx").on(t.lobbyId, t.suggesterId), }));export const SUSPECTS = [ "Mr. Alaric Finch", "Ms. Odette Reyes", "Professor Callum Wren", "Dame Sylvie Okafor", "Captain Rian Blackwood", "Ms. Priya Vantage", ] as const; export const TOOLS = [ "Lockpick", "Grappling Hook", "Master Key", "Smoke Bomb", "Crowbar", "Forged Documents", ] as const; export const LOCATIONS = [ "The Archive", "The Gallery", "The Rooftop", "The Server Room", "The Wine Cellar", "The Security Office", ] as const; type CardType = "suspect" | "tool" | "location"; function cardId(type: CardType, name: string): string { return `${type}:${name}`; } function shuffle<T>(arr: readonly T[]): T[] { const copy = [...arr]; for (let i = copy.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [copy[i], copy[j]] = [copy[j], copy[i]]; } return copy; } export type MysteryPlayerState = { memberId: string; seatOrder: number; hand: string[]; status: "active" | "eliminated"; }; export type PendingDisprove = { suggestionIndex: number; suggesterId: string; disproverId: string; candidateCardIds: string[]; }; export type MysteryLogEntry = | { type: "suggestion"; turn: number; suggesterId: string; suspect: string; tool: string; location: string; disprovedBy: string | null } | { type: "accusation"; turn: number; accuserId: string; suspect: string; tool: string; location: string; correct: boolean }; export type MysteryEngineState = { solution: { suspect: string; tool: string; location: string }; players: MysteryPlayerState[]; currentTurnSeat: number; pendingDisprove: PendingDisprove | null; log: MysteryLogEntry[]; turnCount: number; gameOver: boolean; winnerId: string | null; }; export function initializeGame(memberIds: string[]): MysteryEngineState { if (memberIds.length < 3 || memberIds.length > 6) { throw new Error("Mystery requires 3-6 players"); } const suspects = shuffle(SUSPECTS); const tools = shuffle(TOOLS); const locations = shuffle(LOCATIONS); const solution = { suspect: suspects[0], tool: tools[0], location: locations[0] }; const remainingCards = [ ...suspects.slice(1).map((n) => cardId("suspect", n)), ...tools.slice(1).map((n) => cardId("tool", n)), ...locations.slice(1).map((n) => cardId("location", n)), ]; const deck = shuffle(remainingCards); const seated = shuffle(memberIds); const hands: string[][] = seated.map(() => []); deck.forEach((card, i) => hands[i % seated.length].push(card)); const players: MysteryPlayerState[] = seated.map((memberId, i) => ({ memberId, seatOrder: i, hand: hands[i], status: "active", })); return { solution, players, currentTurnSeat: 0, pendingDisprove: null, log: [], turnCount: 0, gameOver: false, winnerId: null }; } export type MysteryActionResult = | { ok: true; state: MysteryEngineState } | { ok: false; error: "not_found" | "not_your_turn" | "eliminated" | "disprove_pending" | "invalid_card" | "game_over" }; function findPlayer(state: MysteryEngineState, memberId: string) { return state.players.find((p) => p.memberId === memberId); } // Bound is `step <= n` INTENTIONALLY (not `< n`) — this allows wraparound // back to the caller's own seat, which is the correct behavior when they are // the sole remaining active player: the turn legitimately returns to them. // Do not "fix" this to match the disprove loop below, which uses `< n` for // a different, also-intentional reason (a suggester can never disprove // themselves). function nextActiveSeat(state: MysteryEngineState, fromSeat: number): number | null { const n = state.players.length; for (let step = 1; step <= n; step++) { const seat = (fromSeat + step) % n; const player = state.players.find((p) => p.seatOrder === seat); if (player && player.status === "active") return seat; } return null; } export function makeSuggestion( state: MysteryEngineState, suggesterId: string, suspect: string, tool: string, location: string ): MysteryActionResult { if (state.gameOver) return { ok: false, error: "game_over" }; if (state.pendingDisprove) return { ok: false, error: "disprove_pending" }; const suggester = findPlayer(state, suggesterId); if (!suggester) return { ok: false, error: "not_found" }; if (suggester.seatOrder !== state.currentTurnSeat) return { ok: false, error: "not_your_turn" }; if (suggester.status === "eliminated") return { ok: false, error: "eliminated" }; if (!SUSPECTS.includes(suspect as any) || !TOOLS.includes(tool as any) || !LOCATIONS.includes(location as any)) { return { ok: false, error: "invalid_card" }; } const suggestedIds = [cardId("suspect", suspect), cardId("tool", tool), cardId("location", location)]; const n = state.players.length; let disprover: MysteryPlayerState | null = null; let candidateCardIds: string[] = []; // `step < n`: excludes the suggester's own seat. Eliminated players are // NOT skipped here — they still hold real cards and must still disprove. // Elimination only revokes the right to suggest/accuse, never to disprove. for (let step = 1; step < n; step++) { const seat = (suggester.seatOrder + step) % n; const player = state.players.find((p) => p.seatOrder === seat)!; const matches = player.hand.filter((c) => suggestedIds.includes(c)); if (matches.length > 0) { disprover = player; candidateCardIds = matches; break; } } const newLog: MysteryLogEntry = { type: "suggestion", turn: state.turnCount, suggesterId, suspect, tool, location, disprovedBy: disprover ? disprover.memberId : null, }; if (!disprover) { const nextSeat = nextActiveSeat(state, suggester.seatOrder); return { ok: true, state: { ...state, log: [...state.log, newLog], turnCount: state.turnCount + 1, currentTurnSeat: nextSeat ?? state.currentTurnSeat, gameOver: nextSeat === null, winnerId: null, }, }; } return { ok: true, state: { ...state, log: [...state.log, newLog], pendingDisprove: { suggestionIndex: state.log.length, suggesterId, disproverId: disprover.memberId, candidateCardIds }, }, }; } export function resolveDisprove(state: MysteryEngineState, disproverId: string, chosenCardId: string): MysteryActionResult { if (state.gameOver) return { ok: false, error: "game_over" }; if (!state.pendingDisprove) return { ok: false, error: "not_found" }; if (state.pendingDisprove.disproverId !== disproverId) return { ok: false, error: "not_your_turn" }; if (!state.pendingDisprove.candidateCardIds.includes(chosenCardId)) return { ok: false, error: "invalid_card" }; const suggester = findPlayer(state, state.pendingDisprove.suggesterId)!; const nextSeat = nextActiveSeat(state, suggester.seatOrder); return { ok: true, state: { ...state, pendingDisprove: null, turnCount: state.turnCount + 1, currentTurnSeat: nextSeat ?? state.currentTurnSeat, gameOver: nextSeat === null, }, }; } export function makeAccusation( state: MysteryEngineState, accuserId: string, suspect: string, tool: string, location: string ): MysteryActionResult { if (state.gameOver) return { ok: false, error: "game_over" }; if (state.pendingDisprove) return { ok: false, error: "disprove_pending" }; const accuser = findPlayer(state, accuserId); if (!accuser) return { ok: false, error: "not_found" }; if (accuser.seatOrder !== state.currentTurnSeat) return { ok: false, error: "not_your_turn" }; if (accuser.status === "eliminated") return { ok: false, error: "eliminated" }; if (!SUSPECTS.includes(suspect as any) || !TOOLS.includes(tool as any) || !LOCATIONS.includes(location as any)) { return { ok: false, error: "invalid_card" }; } const correct = state.solution.suspect === suspect && state.solution.tool === tool && state.solution.location === location; const newLog: MysteryLogEntry = { type: "accusation", turn: state.turnCount, accuserId, suspect, tool, location, correct }; if (correct) { return { ok: true, state: { ...state, log: [...state.log, newLog], gameOver: true, winnerId: accuserId, turnCount: state.turnCount + 1 } }; } const updatedPlayers = state.players.map((p) => (p.memberId === accuserId ? { ...p, status: "eliminated" as const } : p)); const stillActive = updatedPlayers.some((p) => p.status === "active"); const nextSeat = stillActive ? nextActiveSeat({ ...state, players: updatedPlayers }, accuser.seatOrder) : null; return { ok: true, state: { ...state, players: updatedPlayers, log: [...state.log, newLog], turnCount: state.turnCount + 1, currentTurnSeat: nextSeat ?? state.currentTurnSeat, gameOver: !stillActive || nextSeat === null, winnerId: null, }, }; }import { MysteryEngineState, MysteryLogEntry } from "./engine"; export type RedactedMysteryState = { players: { memberId: string; seatOrder: number; handSize: number; status: "active" | "eliminated" }[]; yourHand: string[]; currentTurnSeat: number; pendingDisprove: { suggesterId: string; disproverId: string; candidateCardIds: string[] } | null; log: MysteryLogEntry[]; turnCount: number; gameOver: boolean; winnerId: string | null; solution: { suspect: string; tool: string; location: string } | null; }; export function redactStateForViewer(state: MysteryEngineState, viewerId: string): RedactedMysteryState { const you = state.players.find((p) => p.memberId === viewerId); return { // NOTE: intentionally NOT spreading `p` here — building this object // field-by-field means an unrelated future field added to // MysteryPlayerState can never silently leak through an accidental // `...p` spread. This is slower to write and exactly as slow to audit, // which is the point. players: state.players.map((p) => ({ memberId: p.memberId, seatOrder: p.seatOrder, handSize: p.hand.length, status: p.status, })), yourHand: you ? you.hand : [], currentTurnSeat: state.currentTurnSeat, pendingDisprove: state.pendingDisprove ? { suggesterId: state.pendingDisprove.suggesterId, disproverId: state.pendingDisprove.disproverId, candidateCardIds: state.pendingDisprove.disproverId === viewerId ? state.pendingDisprove.candidateCardIds : [], } : null, log: state.log, // suggestions/accusations are PUBLIC by the actual rules of the genre — only the disprove card itself is private turnCount: state.turnCount, gameOver: state.gameOver, winnerId: state.winnerId, solution: state.gameOver ? state.solution : null, }; }// Run with: npx tsx scripts/test-mystery-redaction.mjs // This is the concrete, automated check that the redaction function actually // does what the comments above claim. Per the Witness principle: reasoning // through this by hand is not evidence. This script is the evidence. import { initializeGame, makeSuggestion, SUSPECTS, TOOLS, LOCATIONS } from "../src/lib/arcade/mystery/engine.ts"; import { redactStateForViewer } from "../src/lib/arcade/mystery/redact.ts"; const players = ["p1", "p2", "p3", "p4"]; let state = initializeGame(players); let found = false; outer: for (const s of SUSPECTS) for (const t of TOOLS) for (const l of LOCATIONS) { const result = makeSuggestion(state, "p1", s, t, l); if (result.ok && result.state.pendingDisprove) { state = result.state; found = true; break outer; } } if (!found) { console.error("FAIL: could not reach a pendingDisprove state — engine logic problem, test inconclusive."); process.exit(1); } const disproverId = state.pendingDisprove.disproverId; const bystanderId = players.find((p) => p !== disproverId && p !== "p1"); const forBystander = redactStateForViewer(state, bystanderId); const forDisprover = redactStateForViewer(state, disproverId); let failed = false; const fail = (msg) => { console.error(`FAIL: ${msg}`); failed = true; }; if (forBystander.pendingDisprove.candidateCardIds.length !== 0) fail("candidate cards leaked to a bystander"); if (forDisprover.pendingDisprove.candidateCardIds.length === 0) fail("disprover couldn't see their OWN legitimate candidates"); for (const p of forBystander.players) if ("hand" in p) fail("raw hand field present in redacted players array"); if (forBystander.solution !== null) fail("solution exposed before game over"); const serialized = JSON.stringify(forBystander); for (const p of state.players) { if (p.memberId === bystanderId) continue; for (const card of p.hand) { if (serialized.includes(card)) fail(`card "${card}" belonging to another player leaked into bystander's payload`); } } if (failed) process.exit(1); console.log("PASS: 5 leak checks passed — no hidden information crossed a viewer boundary.");// Free-for-all rating approximation: treat the result as N-1 independent // pairwise comparisons between the winner and each other player, using a // reduced K-factor (32 / opponent count) so a 6-player win doesn't move the // winner's rating 5x further than a 3-player win. This is a stated // simplification for a community leaderboard — NOT a validated multiplayer // rating system like TrueSkill, and shouldn't be presented as one. export function computeMysteryRatingUpdates( winnerId: string | null, playerRatings: { memberId: string; rating: number }[] ): { memberId: string; newRating: number }[] { if (!winnerId) { // Unsolved game: nobody's rating moves — an unattributed shared loss // isn't meaningful evidence about any individual's skill. return playerRatings.map((p) => ({ memberId: p.memberId, newRating: p.rating })); } const winner = playerRatings.find((p) => p.memberId === winnerId); if (!winner) return playerRatings.map((p) => ({ memberId: p.memberId, newRating: p.rating })); const others = playerRatings.filter((p) => p.memberId !== winnerId); const reducedK = 32 / Math.max(1, others.length); const originalWinnerRating = winner.rating; let winnerDeltaSum = 0; const otherResults = others.map((opponent) => { // Computed against the ORIGINAL winner rating for every opponent // simultaneously — order-independent, zero-sum per pair. const expectedWinner = 1 / (1 + 10 ** ((opponent.rating - originalWinnerRating) / 400)); const delta = reducedK * (1 - expectedWinner); winnerDeltaSum += delta; return { memberId: opponent.memberId, newRating: Math.round(opponent.rating - delta) }; }); return [{ memberId: winnerId, newRating: Math.round(originalWinnerRating + winnerDeltaSum) }, ...otherResults]; }import { db } from "@/lib/db"; import { arcadeRatings } from "@/lib/db/schema/arcade-core"; import { getOrCreateRating } from "@/lib/arcade/matchmaking"; import { computeMysteryRatingUpdates } from "./rating"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { MysteryEngineState } from "./engine"; import { eq, and, sql } from "drizzle-orm"; export async function finalizeIfGameOver(state: MysteryEngineState): Promise<void> { if (!state.gameOver) return; const ratings = await Promise.all( state.players.map(async (p) => ({ memberId: p.memberId, rating: await getOrCreateRating(p.memberId, "mystery") })) ); const updates = computeMysteryRatingUpdates(state.winnerId, ratings); for (const u of updates) { await db.update(arcadeRatings).set({ rating: u.newRating, gamesPlayed: sql`${arcadeRatings.gamesPlayed} + 1` }) .where(and(eq(arcadeRatings.memberId, u.memberId), eq(arcadeRatings.gameKey, "mystery"))); } for (const p of state.players) await recordArcadeActivity(p.memberId); }export const arcadeGameKindEnum = pgEnum("arcade_game_kind", ["solo_score", "head_to_head", "multiplayer"]);if (game.kind === "head_to_head" || game.kind === "multiplayer") {{ key: "mystery", name: "The Heist Files", kind: "multiplayer" as const, scoreDirection: "higher_better" as const },import { db } from "@/lib/db"; import { mysteryLobbies } from "@/lib/db/schema/mystery"; import { MysteryEngineState } from "./engine"; import { sql } from "drizzle-orm"; type LobbyRow = { id: string; status: string; state: MysteryEngineState | null; min_players: number; max_players: number; created_by: string }; export async function withLockedActiveLobby( lobbyId: string, fn: (tx: any, lobby: LobbyRow) => Promise<{ httpStatus: number; body: unknown; finalState?: MysteryEngineState }> ) { return db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM mystery_lobbies WHERE id = ${lobbyId} FOR UPDATE`) as unknown as [LobbyRow]; const lobby = locked[0]; if (!lobby) return { httpStatus: 404, body: { error: "not_found" } }; if (lobby.status !== "active" || !lobby.state) return { httpStatus: 409, body: { error: "not_active" } }; return fn(tx, lobby); }); }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { mysteryLobbies, mysteryLobbyPlayers } from "@/lib/db/schema/mystery"; import { eq, desc } from "drizzle-orm"; export async function GET() { const lobbies = await db.select().from(mysteryLobbies).where(eq(mysteryLobbies.status, "waiting")).orderBy(desc(mysteryLobbies.createdAt)).limit(20); return NextResponse.json({ lobbies }); } export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const lobby = await db.transaction(async (tx) => { const [row] = await tx.insert(mysteryLobbies).values({ createdBy: viewer.id }).returning(); await tx.insert(mysteryLobbyPlayers).values({ lobbyId: row.id, memberId: viewer.id }); return row; }); return NextResponse.json({ lobby }, { status: 201 }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { mysteryLobbyPlayers } from "@/lib/db/schema/mystery"; import { eq, and, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM mystery_lobbies WHERE id = ${params.id} FOR UPDATE`) as unknown as [any]; const lobby = locked[0]; if (!lobby) return { httpStatus: 404, body: { error: "not_found" } }; if (lobby.status !== "waiting") return { httpStatus: 409, body: { error: "not_waiting" } }; const existing = await tx.select().from(mysteryLobbyPlayers) .where(and(eq(mysteryLobbyPlayers.lobbyId, params.id), eq(mysteryLobbyPlayers.memberId, viewer.id))).limit(1); if (existing.length > 0) return { httpStatus: 409, body: { error: "already_joined" } }; const [{ count }] = await tx.select({ count: sql<number>`count(*)::int` }).from(mysteryLobbyPlayers).where(eq(mysteryLobbyPlayers.lobbyId, params.id)); if (count >= lobby.max_players) return { httpStatus: 409, body: { error: "lobby_full" } }; await tx.insert(mysteryLobbyPlayers).values({ lobbyId: params.id, memberId: viewer.id }); return { httpStatus: 200, body: { status: "joined" } }; }); return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { mysteryLobbies, mysteryLobbyPlayers } from "@/lib/db/schema/mystery"; import { initializeGame } from "@/lib/arcade/mystery/engine"; import { eq, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM mystery_lobbies WHERE id = ${params.id} FOR UPDATE`) as unknown as [any]; const lobby = locked[0]; if (!lobby) return { httpStatus: 404, body: { error: "not_found" } }; if (lobby.created_by !== viewer.id) return { httpStatus: 403, body: { error: "forbidden" } }; if (lobby.status !== "waiting") return { httpStatus: 409, body: { error: "not_waiting" } }; const players = await tx.select().from(mysteryLobbyPlayers).where(eq(mysteryLobbyPlayers.lobbyId, params.id)); if (players.length < lobby.min_players) return { httpStatus: 409, body: { error: "not_enough_players" } }; let state; try { state = initializeGame(players.map((p) => p.memberId)); } catch { return { httpStatus: 500, body: { error: "init_failed" } }; } await tx.update(mysteryLobbies).set({ status: "active", state, startedAt: new Date() }).where(eq(mysteryLobbies.id, params.id)); for (const p of state.players) { await tx.update(mysteryLobbyPlayers).set({ seatOrder: p.seatOrder }) .where(sql`lobby_id = ${params.id} AND member_id = ${p.memberId}`); } return { httpStatus: 200, body: { status: "started" } }; }); return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { mysteryLobbies, mysteryPrivateReveals } from "@/lib/db/schema/mystery"; import { withLockedActiveLobby } from "@/lib/arcade/mystery/lobby-tx"; import { resolveDisprove } from "@/lib/arcade/mystery/engine"; import { finalizeIfGameOver } from "@/lib/arcade/mystery/finalize"; import { eq } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ cardId: z.string().min(1).max(60) }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const outcome = await withLockedActiveLobby(params.id, async (tx, lobby) => { const pending = lobby.state!.pendingDisprove; const result = resolveDisprove(lobby.state!, viewer.id, parsed.data.cardId); if (!result.ok) return { httpStatus: 409, body: { error: result.error } }; if (pending) { await tx.insert(mysteryPrivateReveals).values({ lobbyId: params.id, suggestionIndex: pending.suggestionIndex, suggesterId: pending.suggesterId, disproverId: pending.disproverId, cardId: parsed.data.cardId, }); } await tx.update(mysteryLobbies).set({ state: result.state, status: result.state.gameOver ? "completed" : "active", winnerId: result.state.winnerId, completedAt: result.state.gameOver ? new Date() : null, }).where(eq(mysteryLobbies.id, params.id)); return { httpStatus: 200, body: { status: "ok" }, finalState: result.state }; }); if (outcome.finalState?.gameOver) await finalizeIfGameOver(outcome.finalState); return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { mysteryLobbies } from "@/lib/db/schema/mystery"; import { withLockedActiveLobby } from "@/lib/arcade/mystery/lobby-tx"; import { makeAccusation, SUSPECTS, TOOLS, LOCATIONS } from "@/lib/arcade/mystery/engine"; import { finalizeIfGameOver } from "@/lib/arcade/mystery/finalize"; import { eq } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ suspect: z.enum(SUSPECTS), tool: z.enum(TOOLS), location: z.enum(LOCATIONS) }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const outcome = await withLockedActiveLobby(params.id, async (tx, lobby) => { const result = makeAccusation(lobby.state!, viewer.id, parsed.data.suspect, parsed.data.tool, parsed.data.location); if (!result.ok) return { httpStatus: 409, body: { error: result.error } }; await tx.update(mysteryLobbies).set({ state: result.state, status: result.state.gameOver ? "completed" : "active", winnerId: result.state.winnerId, completedAt: result.state.gameOver ? new Date() : null, }).where(eq(mysteryLobbies.id, params.id)); const lastEntry = result.state.log[result.state.log.length - 1]; const correct = lastEntry.type === "accusation" ? lastEntry.correct : undefined; return { httpStatus: 200, body: { correct }, finalState: result.state }; }); if (outcome.finalState?.gameOver) await finalizeIfGameOver(outcome.finalState); return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { mysteryLobbies, mysteryLobbyPlayers, mysteryPrivateReveals } from "@/lib/db/schema/mystery"; import { redactStateForViewer } from "@/lib/arcade/mystery/redact"; import { eq, and } from "drizzle-orm"; export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [lobby] = await db.select().from(mysteryLobbies).where(eq(mysteryLobbies.id, params.id)).limit(1); if (!lobby) return NextResponse.json({ error: "not_found" }, { status: 404 }); const players = await db.select().from(mysteryLobbyPlayers).where(eq(mysteryLobbyPlayers.lobbyId, params.id)); if (!players.some((p) => p.memberId === viewer.id)) return NextResponse.json({ error: "forbidden" }, { status: 403 }); if (lobby.status === "waiting") { return NextResponse.json({ status: "waiting", players, minPlayers: lobby.minPlayers, maxPlayers: lobby.maxPlayers, isHost: lobby.createdBy === viewer.id }); } const redacted = redactStateForViewer(lobby.state as any, viewer.id); // Only ever queried for THIS viewer as suggester — never fetched broadly and filtered client-side. const myReveals = await db.select().from(mysteryPrivateReveals) .where(and(eq(mysteryPrivateReveals.lobbyId, params.id), eq(mysteryPrivateReveals.suggesterId, viewer.id))); return NextResponse.json({ status: lobby.status, state: redacted, myReveals }); }"use client"; import { useEffect, useState } from "react"; import { SUSPECTS, TOOLS, LOCATIONS } from "@/lib/arcade/mystery/engine"; import styles from "./mystery-app.module.css"; type LobbySummary = { id: string; minPlayers: number; maxPlayers: number }; export function MysteryLobbyBrowser({ viewerId }: { viewerId: string }) { const [lobbies, setLobbies] = useState<LobbySummary[] | null>(null); const [activeLobbyId, setActiveLobbyId] = useState<string | null>(null); useEffect(() => { fetch("/api/arcade/games/mystery/lobbies").then((r) => r.json()).then((d) => setLobbies(d.lobbies)); }, []); async function createLobby() { const res = await fetch("/api/arcade/games/mystery/lobbies", { method: "POST" }); const data = await res.json(); setActiveLobbyId(data.lobby.id); } async function joinLobby(id: string) { const res = await fetch(`/api/arcade/games/mystery/lobbies/${id}/join`, { method: "POST" }); if (res.ok) setActiveLobbyId(id); } if (activeLobbyId) return <MysteryLobbyRoom lobbyId={activeLobbyId} viewerId={viewerId} />; return ( <div className={styles.browser}> <button className={styles.createBtn} onClick={createLobby}>Start a new case</button> {lobbies === null && <p className={styles.status}>Loading open cases…</p>} {lobbies?.length === 0 && <p className={styles.status}>No open cases — start one.</p>} {lobbies?.map((l) => ( <div key={l.id} className={styles.lobbyRow}> <span>Case #{l.id.slice(0, 8)}</span> <button onClick={() => joinLobby(l.id)}>Join</button> </div> ))} </div> ); } function MysteryLobbyRoom({ lobbyId, viewerId }: { lobbyId: string; viewerId: string }) { const [data, setData] = useState<any>(null); useEffect(() => { let cancelled = false; async function poll() { const res = await fetch(`/api/arcade/games/mystery/lobbies/${lobbyId}`); if (!cancelled && res.ok) setData(await res.json()); } poll(); const interval = setInterval(poll, 2000); return () => { cancelled = true; clearInterval(interval); }; }, [lobbyId]); if (!data) return <p className={styles.status}>Loading case…</p>; if (data.status === "waiting") { return ( <div className={styles.waitingRoom}> <p>{data.players.length}/{data.maxPlayers} detectives joined (minimum {data.minPlayers} to start)</p> <ul>{data.players.map((p: any) => <li key={p.memberId}>{p.memberId === viewerId ? "You" : p.memberId}</li>)}</ul> {data.isHost && data.players.length >= data.minPlayers && ( <button onClick={async () => fetch(`/api/arcade/games/mystery/lobbies/${lobbyId}/start`, { method: "POST" })}> Start the case </button> )} </div> ); } return <MysteryBoard lobbyId={lobbyId} viewerId={viewerId} state={data.state} myReveals={data.myReveals} />; } function MysteryBoard({ lobbyId, viewerId, state, myReveals }: any) { const [suspect, setSuspect] = useState(SUSPECTS[0]); const [tool, setTool] = useState(TOOLS[0]); const [location, setLocation] = useState(LOCATIONS[0]); const [actionError, setActionError] = useState<string | null>(null); const you = state.players.find((p: any) => p.memberId === viewerId); const isMyTurn = you && you.seatOrder === state.currentTurnSeat && you.status === "active" && !state.pendingDisprove; const iMustDisprove = state.pendingDisprove?.disproverId === viewerId; async function act(path: string, body: unknown) { setActionError(null); const res = await fetch(`/api/arcade/games/mystery/lobbies/${lobbyId}/${path}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body), }); if (!res.ok) { const err = await res.json().catch(() => ({})); setActionError(err.error ?? "action_failed"); } } return ( <div className={styles.board}> <div className={styles.mainPanel}> <p className={styles.turnStatus}> {state.gameOver ? state.winnerId === viewerId ? "You solved the case!" : state.winnerId ? "Case solved by someone else." : "The case goes unsolved." : iMustDisprove ? "You must respond to a suggestion." : isMyTurn ? "Your turn." : "Waiting on another detective."} </p> {state.gameOver && state.solution && ( <p className={styles.solution}>The truth: {state.solution.suspect} used the {state.solution.tool} in {state.solution.location}.</p> )} {iMustDisprove && ( <div className={styles.disproveBox}> <p>Choose a card to show privately to the suggester:</p> {state.pendingDisprove.candidateCardIds.map((c: string) => ( <button key={c} onClick={() => act("reveal", { cardId: c })}>{c.split(":")[1]}</button> ))} </div> )} {isMyTurn && !iMustDisprove && ( <div className={styles.actionForms}> <div className={styles.formRow}> <select value={suspect} onChange={(e) => setSuspect(e.target.value)}>{SUSPECTS.map((s) => <option key={s}>{s}</option>)}</select> <select value={tool} onChange={(e) => setTool(e.target.value)}>{TOOLS.map((t) => <option key={t}>{t}</option>)}</select> <select value={location} onChange={(e) => setLocation(e.target.value)}>{LOCATIONS.map((l) => <option key={l}>{l}</option>)}</select> </div> <button onClick={() => act("suggest", { suspect, tool, location })}>Suggest</button> <button onClick={() => act("accuse", { suspect, tool, location })} className={styles.accuseBtn}>Accuse (final — wrong guess eliminates you)</button> </div> )} {actionError && <p className={styles.error}>{actionError.replaceAll("_", " ")}</p>} </div> <div className={styles.sidebar}> <h4>Your hand</h4> <ul>{you.hand.map((c: string) => <li key={c}>{c.split(":")[1]}</li>)}</ul> <h4>Detectives</h4> <ul>{state.players.map((p: any) => ( <li key={p.memberId} className={p.status === "eliminated" ? styles.eliminated : ""}> {p.memberId === viewerId ? "You" : p.memberId} — {p.handSize} cards {p.status === "eliminated" ? "(out)" : ""} </li> ))}</ul> <h4>Case log</h4> <ul className={styles.log}> {state.log.map((entry: any, i: number) => ( <li key={i}> {entry.type === "suggestion" ? `Suggested ${entry.suspect} / ${entry.tool} / ${entry.location}${entry.disprovedBy ? " — disproven" : " — no one could disprove"}` : `Accused ${entry.suspect} / ${entry.tool} / ${entry.location} — ${entry.correct ? "CORRECT" : "wrong"}`} </li> ))} </ul> {myReveals.length > 0 && ( <> <h4>Cards shown to you</h4> <ul>{myReveals.map((r: any, i: number) => <li key={i}>{r.cardId.split(":")[1]}</li>)}</ul> </> )} </div> </div> ); }.browser { display: flex; flex-direction: column; gap: 12px; } .createBtn { background: var(--accent-gold); color: #14100F; border: none; border-radius: var(--radius-sm); padding: 10px 16px; font-weight: 600; cursor: pointer; } .lobbyRow { display: flex; justify-content: space-between; background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 10px 14px; } .status { color: var(--text-secondary); font-style: italic; } .waitingRoom { color: var(--text-primary); } .board { display: flex; gap: 24px; color: var(--text-primary); } .mainPanel { flex: 1; } .turnStatus { font-family: var(--font-display); color: var(--accent-gold); } .solution { color: var(--accent-silver); font-weight: 600; } .disproveBox button, .actionForms button { margin: 4px; background: var(--surface-2); border: 1px solid var(--border); color: var(--text-primary); border-radius: var(--radius-sm); padding: 6px 12px; cursor: pointer; } .accuseBtn { border-color: var(--danger); color: var(--danger-text); } .formRow { display: flex; gap: 8px; margin-bottom: 8px; } .sidebar { min-width: 240px; font-size: 0.85rem; color: var(--text-secondary); } .sidebar h4 { color: var(--text-primary); margin-bottom: 4px; } .eliminated { opacity: 0.5; text-decoration: line-through; } .log { max-height: 200px; overflow-y: auto; } .error { color: var(--danger-text); font-size: 0.8rem; }export type EnemyKind = "grunt" | "shooter"; export type Bullet = { id: number; x: number; y: number; vy: number; owner: "player" | "enemy" }; export type Enemy = { id: number; x: number; y: number; vy: number; hp: number; kind: EnemyKind; lastFiredAt: number }; export type ShooterInput = { left: boolean; right: boolean; shoot: boolean }; export type ShooterState = { playerX: number; lives: number; invulnerableUntil: number; // measured in engine elapsedMs, not wall-clock bullets: Bullet[]; enemies: Enemy[]; score: number; level: number; elapsedMs: number; lastEnemySpawnAt: number; lastPlayerFireAt: number; gameOver: boolean; }; export const CANVAS_WIDTH = 480; export const CANVAS_HEIGHT = 640; export const PLAYER_WIDTH = 36; export const PLAYER_HEIGHT = 20; export const PLAYER_Y = CANVAS_HEIGHT - 50; export const ENEMY_WIDTH = 28; export const ENEMY_HEIGHT = 20; export const BULLET_WIDTH = 4; export const BULLET_HEIGHT = 10; const PLAYER_SPEED = 260; // px/s const PLAYER_BULLET_SPEED = 460; // px/s, upward const ENEMY_BULLET_SPEED = 200; // px/s, downward const FIRE_COOLDOWN_MS = 220; const INVULNERABILITY_MS = 1200; const MAX_PLAYER_BULLETS = 12; // defense-in-depth cap, independent of the cooldown const MAX_DT_MS = 100; // clamp — the fix for the backgrounded-tab tunneling bug const LEVEL_SCORE_STEP = 500; function spawnIntervalMs(level: number): number { return Math.max(280, 1100 - (level - 1) * 90); } function enemySpeedForLevel(level: number): number { return 55 + (level - 1) * 10; } function enemyFireIntervalMs(level: number): number { return Math.max(900, 2200 - (level - 1) * 120); } function aabbOverlap( ax: number, ay: number, aw: number, ah: number, bx: number, by: number, bw: number, bh: number ): boolean { return ax < bx + bw && ax + aw > bx && ay < by + bh && ay + ah > by; } let idCounter = 0; function nextId(): number { return ++idCounter; } export class ShooterEngine { state: ShooterState; private rng: () => number; // rng is injectable specifically so the verification script below can // force deterministic enemy spawns instead of fighting Math.random flakiness. constructor(rng: () => number = Math.random) { this.rng = rng; this.state = { playerX: CANVAS_WIDTH / 2 - PLAYER_WIDTH / 2, lives: 3, invulnerableUntil: 0, bullets: [], enemies: [], score: 0, level: 1, elapsedMs: 0, lastEnemySpawnAt: 0, lastPlayerFireAt: -Infinity, gameOver: false, }; } // Every life-loss path (bullet hit, body collision, enemy invasion) routes // through here, so the invulnerability window protects against ALL of them // uniformly — not just projectiles. private takeHit(): void { const s = this.state; if (s.elapsedMs < s.invulnerableUntil) return; // absorbed by i-frames, no-op s.lives -= 1; s.invulnerableUntil = s.elapsedMs + INVULNERABILITY_MS; if (s.lives <= 0) s.gameOver = true; } tick(rawDtMs: number, input: ShooterInput): void { const s = this.state; if (s.gameOver) return; const dtMs = Math.min(rawDtMs, MAX_DT_MS); // second clamp point, see class comment above const dt = dtMs / 1000; s.elapsedMs += dtMs; if (input.left) s.playerX -= PLAYER_SPEED * dt; if (input.right) s.playerX += PLAYER_SPEED * dt; s.playerX = Math.max(0, Math.min(CANVAS_WIDTH - PLAYER_WIDTH, s.playerX)); const playerBulletCount = s.bullets.filter((b) => b.owner === "player").length; if (input.shoot && s.elapsedMs - s.lastPlayerFireAt >= FIRE_COOLDOWN_MS && playerBulletCount < MAX_PLAYER_BULLETS) { s.bullets.push({ id: nextId(), x: s.playerX + PLAYER_WIDTH / 2 - BULLET_WIDTH / 2, y: PLAYER_Y, vy: -PLAYER_BULLET_SPEED, owner: "player", }); s.lastPlayerFireAt = s.elapsedMs; } for (const b of s.bullets) b.y += b.vy * dt; s.bullets = s.bullets.filter((b) => b.y > -BULLET_HEIGHT && b.y < CANVAS_HEIGHT + BULLET_HEIGHT); const survivingEnemies: Enemy[] = []; for (const e of s.enemies) { e.y += e.vy * dt; if (e.kind === "shooter" && s.elapsedMs - e.lastFiredAt >= enemyFireIntervalMs(s.level)) { s.bullets.push({ id: nextId(), x: e.x + ENEMY_WIDTH / 2 - BULLET_WIDTH / 2, y: e.y + ENEMY_HEIGHT, vy: ENEMY_BULLET_SPEED, owner: "enemy", }); e.lastFiredAt = s.elapsedMs; } if (e.y > CANVAS_HEIGHT) { this.takeHit(); // unopposed invasion costs a life, same i-frame rule as everything else continue; } survivingEnemies.push(e); } s.enemies = survivingEnemies; if (s.elapsedMs - s.lastEnemySpawnAt >= spawnIntervalMs(s.level)) { const kind: EnemyKind = s.level >= 2 && this.rng() < 0.35 ? "shooter" : "grunt"; s.enemies.push({ id: nextId(), x: this.rng() * (CANVAS_WIDTH - ENEMY_WIDTH), y: -ENEMY_HEIGHT, vy: enemySpeedForLevel(s.level), hp: kind === "shooter" ? 2 : 1, kind, lastFiredAt: s.elapsedMs, }); s.lastEnemySpawnAt = s.elapsedMs; } // Player bullets vs enemies. Non-piercing by design: a bullet is // consumed by the FIRST enemy (in array order) it overlaps this tick, // never damages two enemies at once even if their hitboxes overlap. const bulletsToRemove = new Set<number>(); const enemiesAfterHits: Enemy[] = []; for (const e of s.enemies) { let hp = e.hp; for (const b of s.bullets) { if (b.owner !== "player" || bulletsToRemove.has(b.id)) continue; if (aabbOverlap(e.x, e.y, ENEMY_WIDTH, ENEMY_HEIGHT, b.x, b.y, BULLET_WIDTH, BULLET_HEIGHT)) { hp -= 1; bulletsToRemove.add(b.id); if (hp <= 0) break; } } if (hp <= 0) s.score += (e.kind === "shooter" ? 25 : 10) * s.level; else enemiesAfterHits.push({ ...e, hp }); } s.enemies = enemiesAfterHits; s.bullets = s.bullets.filter((b) => !bulletsToRemove.has(b.id)); // Enemy bullets vs player. A bullet that touches the player is consumed // whether or not i-frames absorbed the damage — the incoming shot still // visually resolves, it just doesn't cost a life during invulnerability. const remainingBullets: Bullet[] = []; for (const b of s.bullets) { if (b.owner === "enemy" && aabbOverlap(s.playerX, PLAYER_Y, PLAYER_WIDTH, PLAYER_HEIGHT, b.x, b.y, BULLET_WIDTH, BULLET_HEIGHT)) { this.takeHit(); continue; } remainingBullets.push(b); } s.bullets = remainingBullets; // Enemy bodies vs player — same "consumed on contact regardless of i-frames" rule. const enemiesAfterBodyHits: Enemy[] = []; for (const e of s.enemies) { if (aabbOverlap(s.playerX, PLAYER_Y, PLAYER_WIDTH, PLAYER_HEIGHT, e.x, e.y, ENEMY_WIDTH, ENEMY_HEIGHT)) { this.takeHit(); continue; } enemiesAfterBodyHits.push(e); } s.enemies = enemiesAfterBodyHits; s.level = 1 + Math.floor(s.score / LEVEL_SCORE_STEP); } }// Run with: npx tsx scripts/test-shooter-engine.mjs // Targets the three hazards named above: tunneling via unclamped dt, // stacked-hit invulnerability, and unbounded entity growth. import { ShooterEngine, CANVAS_WIDTH } from "../src/lib/arcade/shooter/engine.ts"; let failed = false; const fail = (msg) => { console.error(`FAIL: ${msg}`); failed = true; }; // --- Test 1: dt clamp prevents an oversized jump after a backgrounded tab --- { const engine = new ShooterEngine(); engine.state.playerX = CANVAS_WIDTH / 2 - 18; // centered, away from boundary clamp const startX = engine.state.playerX; engine.tick(5000, { left: true, right: false, shoot: false }); // simulate a 5s tab-away gap const moved = startX - engine.state.playerX; if (moved > 30) fail(`dt clamp did not hold — player moved ${moved.toFixed(1)}px on a single 5000ms tick`); else console.log(`Test 1 OK: movement clamped to ${moved.toFixed(1)}px despite a 5000ms input tick`); } // --- Test 2: invulnerability prevents multiple simultaneous hits in one tick --- { const engine = new ShooterEngine(); const { playerX } = engine.state; const { PLAYER_Y } = await import("../src/lib/arcade/shooter/engine.ts"); engine.state.bullets.push( { id: 901, x: playerX + 5, y: PLAYER_Y + 2, vy: 0, owner: "enemy" }, { id: 902, x: playerX + 15, y: PLAYER_Y + 2, vy: 0, owner: "enemy" } ); engine.tick(1, { left: false, right: false, shoot: false }); // dt=1ms — bullets barely move, overlap holds if (engine.state.lives !== 2) fail(`expected exactly 1 life lost from 2 simultaneous overlapping bullets, lives=${engine.state.lives}`); else console.log("Test 2 OK: 2 simultaneous overlapping hits cost exactly 1 life"); } // --- Test 3: bounded memory under sustained continuous fire + spawns --- { const engine = new ShooterEngine(() => 0.5); // deterministic rng for spawn kind/position for (let i = 0; i < 3000; i++) { engine.tick(16, { left: false, right: false, shoot: true }); if (engine.state.gameOver) break; // game ending early is fine, not what this test checks } if (engine.state.bullets.length > 200) fail(`bullet array grew unbounded: ${engine.state.bullets.length} after 3000 ticks`); else console.log(`Test 3 OK: bullets bounded at ${engine.state.bullets.length} after 3000 ticks of continuous fire`); } if (failed) process.exit(1); console.log("PASS: all shooter engine invariants held.");"use client"; import { useEffect, useRef, useState, useCallback } from "react"; import { ShooterEngine, CANVAS_WIDTH, CANVAS_HEIGHT, PLAYER_WIDTH, PLAYER_HEIGHT, PLAYER_Y, ENEMY_WIDTH, ENEMY_HEIGHT, BULLET_WIDTH, BULLET_HEIGHT, } from "@/lib/arcade/shooter/engine"; import styles from "./shooter-game.module.css"; const MAX_FRAME_DT_MS = 100; // component-level clamp, mirrors the engine's own — belt and suspenders, not redundant export function ShooterGame() { const engineRef = useRef(new ShooterEngine()); const canvasRef = useRef<HTMLCanvasElement>(null); const keysRef = useRef({ left: false, right: false, shoot: false }); const lastFrameTimeRef = useRef<number | null>(null); const rafRef = useRef<number | null>(null); const [, setTick] = useState(0); const [submitted, setSubmitted] = useState(false); const rerender = useCallback(() => setTick((t) => t + 1), []); const draw = useCallback(() => { const canvas = canvasRef.current; const ctx = canvas?.getContext("2d"); if (!canvas || !ctx) return; const s = engineRef.current.state; ctx.fillStyle = "#0A0908"; ctx.fillRect(0, 0, canvas.width, canvas.height); // Flashes during invulnerability — visible feedback for a state that // would otherwise be an invisible internal flag the player can't sense. const isInvulnerable = s.elapsedMs < s.invulnerableUntil; if (!isInvulnerable || Math.floor(s.elapsedMs / 100) % 2 === 0) { ctx.fillStyle = "#C7CBD1"; // silver ctx.fillRect(s.playerX, PLAYER_Y, PLAYER_WIDTH, PLAYER_HEIGHT); } for (const b of s.bullets) { ctx.fillStyle = b.owner === "player" ? "#D4AF37" : "#7A1F1F"; // gold vs. deep red — danger used functionally, not decoratively ctx.fillRect(b.x, b.y, BULLET_WIDTH, BULLET_HEIGHT); } for (const e of s.enemies) { ctx.fillStyle = e.kind === "shooter" ? "#8F2626" : "#5A1818"; ctx.fillRect(e.x, e.y, ENEMY_WIDTH, ENEMY_HEIGHT); } }, []); const submitScore = useCallback(async () => { const s = engineRef.current.state; try { await fetch("/api/arcade/games/shooter/score", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ score: s.score, metadata: { level: s.level } }), }); } finally { setSubmitted(true); } }, []); useEffect(() => { function onKeyDown(e: KeyboardEvent) { if (["ArrowLeft", "a", "A"].includes(e.key)) keysRef.current.left = true; else if (["ArrowRight", "d", "D"].includes(e.key)) keysRef.current.right = true; else if (e.key === " ") { e.preventDefault(); keysRef.current.shoot = true; } } function onKeyUp(e: KeyboardEvent) { if (["ArrowLeft", "a", "A"].includes(e.key)) keysRef.current.left = false; else if (["ArrowRight", "d", "D"].includes(e.key)) keysRef.current.right = false; else if (e.key === " ") keysRef.current.shoot = false; } window.addEventListener("keydown", onKeyDown); window.addEventListener("keyup", onKeyUp); return () => { window.removeEventListener("keydown", onKeyDown); window.removeEventListener("keyup", onKeyUp); }; }, []); useEffect(() => { // On regaining visibility, reset the frame-time baseline instead of // relying on the dt clamp alone — this is the cleaner fix (near-zero // first-frame dt) with the clamp as a backstop, not the only safeguard. function onVisibilityChange() { if (!document.hidden) lastFrameTimeRef.current = null; } document.addEventListener("visibilitychange", onVisibilityChange); return () => document.removeEventListener("visibilitychange", onVisibilityChange); }, []); useEffect(() => { function step(now: number) { const engine = engineRef.current; if (!engine.state.gameOver && !document.hidden) { const last = lastFrameTimeRef.current; const rawDt = last === null ? 0 : now - last; lastFrameTimeRef.current = now; engine.tick(Math.min(rawDt, MAX_FRAME_DT_MS), keysRef.current); draw(); rerender(); if (engine.state.gameOver) { submitScore(); return; } } rafRef.current = requestAnimationFrame(step); } rafRef.current = requestAnimationFrame(step); return () => { if (rafRef.current !== null) cancelAnimationFrame(rafRef.current); }; }, [draw, rerender, submitScore]); const s = engineRef.current.state; return ( <div className={styles.wrapper}> <canvas ref={canvasRef} width={CANVAS_WIDTH} height={CANVAS_HEIGHT} className={styles.canvas} /> <div className={styles.hud}> <p>Score: <strong>{s.score}</strong></p> <p>Level: <strong>{s.level}</strong></p> <p>Lives: <strong>{"●".repeat(Math.max(0, s.lives))}{"○".repeat(Math.max(0, 3 - s.lives))}</strong></p> <p className={styles.controls}>Arrows/WASD to move, Space to fire</p> {s.gameOver && <p className={styles.gameOver}>{submitted ? "Score submitted." : "Submitting…"}</p>} </div> </div> ); }.wrapper { display: flex; gap: 20px; align-items: flex-start; } .canvas { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); } .hud { font-family: var(--font-mono); color: var(--text-primary); } .controls { color: var(--text-secondary); font-size: 0.75rem; margin-top: 10px; } .gameOver { color: var(--accent-gold); font-weight: 600; }{ key: "shooter", name: "Skyline Defender", kind: "solo_score" as const, scoreDirection: "higher_better" as const },import { pgTable, uuid, text, integer, timestamp, jsonb, pgEnum, primaryKey, doublePrecision, boolean, index, } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const rpgTargetTypeEnum = pgEnum("rpg_target_type", ["enemy", "self", "all_enemies"]); export const rpgEffectTypeEnum = pgEnum("rpg_effect_type", ["physical_damage", "magic_damage", "heal", "status"]); export const rpgItemTypeEnum = pgEnum("rpg_item_type", ["weapon", "armor", "accessory", "consumable", "material"]); export const rpgItemSlotEnum = pgEnum("rpg_item_slot", ["weapon", "armor", "accessory"]); export const rpgObjectiveTypeEnum = pgEnum("rpg_objective_type", ["kill", "collect", "talk"]); export const rpgQuestStatusEnum = pgEnum("rpg_quest_status", ["not_started", "active", "ready_to_turn_in", "completed"]); export const rpgCombatStatusEnum = pgEnum("rpg_combat_status", ["active", "victory", "defeat", "fled"]); // ---- Registries (seeded content, not user data) ---- export const rpgClasses = pgTable("rpg_classes", { key: text("key").primaryKey(), name: text("name").notNull(), description: text("description").notNull(), baseStr: integer("base_str").notNull(), baseDex: integer("base_dex").notNull(), baseInt: integer("base_int").notNull(), baseVit: integer("base_vit").notNull(), baseWis: integer("base_wis").notNull(), }); export const rpgAbilities = pgTable("rpg_abilities", { key: text("key").primaryKey(), name: text("name").notNull(), description: text("description").notNull(), classKey: text("class_key").notNull().references(() => rpgClasses.key), unlockLevel: integer("unlock_level").notNull().default(1), mpCost: integer("mp_cost").notNull(), targetType: rpgTargetTypeEnum("target_type").notNull(), effectType: rpgEffectTypeEnum("effect_type").notNull(), powerMultiplier: doublePrecision("power_multiplier").notNull().default(1), statusKind: text("status_kind"), // "poison" | "stun" | "buff_atk" | "debuff_def" | "regen" — nullable statusDuration: integer("status_duration"), statusMagnitude: integer("status_magnitude"), }); export const rpgItems = pgTable("rpg_items", { key: text("key").primaryKey(), name: text("name").notNull(), description: text("description").notNull(), itemType: rpgItemTypeEnum("item_type").notNull(), slot: rpgItemSlotEnum("slot"), // null for consumable/material statBonuses: jsonb("stat_bonuses").notNull().default({}), // e.g. { "str": 2, "vit": 1 } healAmount: integer("heal_amount"), // consumables only value: integer("value").notNull().default(0), }); export const rpgZones = pgTable("rpg_zones", { key: text("key").primaryKey(), name: text("name").notNull(), description: text("description").notNull(), requiredLevel: integer("required_level").notNull().default(1), }); export const rpgEnemyTemplates = pgTable("rpg_enemy_templates", { key: text("key").primaryKey(), name: text("name").notNull(), zoneKey: text("zone_key").notNull().references(() => rpgZones.key), level: integer("level").notNull(), str: integer("str").notNull(), dex: integer("dex").notNull(), int: integer("int").notNull(), vit: integer("vit").notNull(), wis: integer("wis").notNull(), abilityKeys: text("ability_keys").array().notNull().default([]), xpReward: integer("xp_reward").notNull(), goldReward: integer("gold_reward").notNull(), unfleeable: boolean("unfleeable").notNull().default(false), }); // A specific fight setup (may spawn multiple enemy templates together — this // is what makes the Warden fight a real multi-enemy encounter, not just a // single enemy template reused). export const rpgEncounters = pgTable("rpg_encounters", { key: text("key").primaryKey(), zoneKey: text("zone_key").notNull().references(() => rpgZones.key), name: text("name").notNull(), enemyTemplateKeys: text("enemy_template_keys").array().notNull(), }); export const rpgQuests = pgTable("rpg_quests", { key: text("key").primaryKey(), title: text("title").notNull(), description: text("description").notNull(), zoneKey: text("zone_key").notNull().references(() => rpgZones.key), giverName: text("giver_name").notNull(), prerequisiteQuestKeys: text("prerequisite_quest_keys").array().notNull().default([]), rewardXp: integer("reward_xp").notNull(), rewardGold: integer("reward_gold").notNull(), rewardItemKeys: text("reward_item_keys").array().notNull().default([]), }); export const rpgQuestStages = pgTable("rpg_quest_stages", { id: uuid("id").defaultRandom().primaryKey(), questKey: text("quest_key").notNull().references(() => rpgQuests.key, { onDelete: "cascade" }), stageOrder: integer("stage_order").notNull(), objectiveType: rpgObjectiveTypeEnum("objective_type").notNull(), objectiveTarget: text("objective_target").notNull(), // enemy key, item key, or NPC name depending on type objectiveCount: integer("objective_count").notNull().default(1), description: text("description").notNull(), }, (t) => ({ questStageIdx: index("rpg_quest_stages_quest_idx").on(t.questKey, t.stageOrder) })); // ---- Player-owned data ---- export const rpgCharacters = pgTable("rpg_characters", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }).unique(), name: text("name").notNull(), classKey: text("class_key").notNull().references(() => rpgClasses.key), level: integer("level").notNull().default(1), xp: integer("xp").notNull().default(0), str: integer("str").notNull(), dex: integer("dex").notNull(), int: integer("int").notNull(), vit: integer("vit").notNull(), wis: integer("wis").notNull(), unspentStatPoints: integer("unspent_stat_points").notNull().default(0), currentHp: integer("current_hp").notNull(), currentMp: integer("current_mp").notNull(), gold: integer("gold").notNull().default(0), currentZoneKey: text("current_zone_key").references(() => rpgZones.key), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }); export const rpgInventory = pgTable("rpg_inventory", { characterId: uuid("character_id").notNull().references(() => rpgCharacters.id, { onDelete: "cascade" }), itemKey: text("item_key").notNull().references(() => rpgItems.key), quantity: integer("quantity").notNull().default(0), }, (t) => ({ pk: primaryKey({ columns: [t.characterId, t.itemKey] }) })); export const rpgEquipment = pgTable("rpg_equipment", { characterId: uuid("character_id").primaryKey().references(() => rpgCharacters.id, { onDelete: "cascade" }), weaponKey: text("weapon_key").references(() => rpgItems.key), armorKey: text("armor_key").references(() => rpgItems.key), accessoryKey: text("accessory_key").references(() => rpgItems.key), }); export const rpgCharacterQuestProgress = pgTable("rpg_character_quest_progress", { characterId: uuid("character_id").notNull().references(() => rpgCharacters.id, { onDelete: "cascade" }), questKey: text("quest_key").notNull().references(() => rpgQuests.key), status: rpgQuestStatusEnum("status").notNull().default("active"), currentStageOrder: integer("current_stage_order").notNull().default(1), stageProgressCount: integer("stage_progress_count").notNull().default(0), }, (t) => ({ pk: primaryKey({ columns: [t.characterId, t.questKey] }) })); export const rpgCombatSessions = pgTable("rpg_combat_sessions", { id: uuid("id").defaultRandom().primaryKey(), characterId: uuid("character_id").notNull().references(() => rpgCharacters.id, { onDelete: "cascade" }), encounterKey: text("encounter_key").notNull().references(() => rpgEncounters.key), state: jsonb("state").notNull(), // full CombatSessionState — see engine below status: rpgCombatStatusEnum("status").notNull().default("active"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), completedAt: timestamp("completed_at", { withTimezone: true }), }, (t) => ({ characterIdx: index("rpg_combat_sessions_character_idx").on(t.characterId, t.status) }));export type BaseStats = { str: number; dex: number; int: number; vit: number; wis: number; level: number }; // The core formula set. Every number here is a real, stated design choice — // not tuned against playtesting data that doesn't exist yet, but internally // consistent and hand-verifiable (see the verification script below). export function baseDerivedStats(s: BaseStats) { return { maxHp: 20 + s.vit * 8 + s.level * 5, maxMp: 10 + s.int * 4 + s.wis * 2 + s.level * 2, physicalAttack: s.str * 2 + s.level, physicalDefense: Math.round(s.vit * 1.5 + s.level * 0.5), magicAttack: s.int * 2 + s.level, magicDefense: Math.round(s.wis * 1.5 + s.level * 0.5), speed: s.dex * 1.5, critChance: Math.min(0.35, 0.05 + s.dex * 0.01), dodgeChance: Math.min(0.25, s.dex * 0.008), }; } export type EffectiveStats = ReturnType<typeof baseDerivedStats>; export type StatusKind = "poison" | "stun" | "buff_atk" | "debuff_def" | "regen"; export type StatusEffect = { kind: StatusKind; remainingTurns: number; magnitude: number }; // Uniform application point for every buff/debuff, regardless of whether it // originated from a player ability or an enemy ability — one code path // means player-cast and enemy-cast status effects can never silently diverge. export function withStatusModifiers(base: EffectiveStats, statuses: StatusEffect[]): EffectiveStats { let { physicalAttack, physicalDefense, magicAttack, magicDefense } = base; for (const s of statuses) { if (s.kind === "buff_atk") { physicalAttack += s.magnitude; magicAttack += s.magnitude; } if (s.kind === "debuff_def") { physicalDefense = Math.max(0, physicalDefense - s.magnitude); magicDefense = Math.max(0, magicDefense - s.magnitude); } } return { ...base, physicalAttack, physicalDefense, magicAttack, magicDefense }; } export function xpToNextLevel(level: number): number { return level * 100; }import { BaseStats, StatusEffect, baseDerivedStats, withStatusModifiers } from "./stats"; export type PlayerCombatState = { currentHp: number; maxHp: number; currentMp: number; maxMp: number; stats: BaseStats; statuses: StatusEffect[]; defending: boolean; }; export type EnemyCombatState = { instanceId: number; templateKey: string; name: string; currentHp: number; maxHp: number; stats: BaseStats; statuses: StatusEffect[]; abilityKeys: string[]; xpReward: number; goldReward: number; unfleeable: boolean; }; export type CombatLogEntry = { actor: string; text: string }; export type CombatSessionState = { player: PlayerCombatState; enemies: EnemyCombatState[]; log: CombatLogEntry[]; round: number; status: "active" | "victory" | "defeat" | "fled"; }; export type AbilityDef = { key: string; name: string; mpCost: number; targetType: "enemy" | "self" | "all_enemies"; effectType: "physical_damage" | "magic_damage" | "heal" | "status"; powerMultiplier: number; statusEffect: { kind: StatusEffect["kind"]; duration: number; magnitude: number } | null; }; export type PlayerAction = | { type: "basic_attack"; targetInstanceId: number } | { type: "ability"; abilityKey: string; targetInstanceId?: number } | { type: "defend" } | { type: "use_item"; healAmount: number } | { type: "flee" }; function rollDamage( atk: ReturnType<typeof baseDerivedStats>, def: ReturnType<typeof baseDerivedStats>, rng: () => number, powerMultiplier: number, school: "physical" | "magic" ): { damage: number; dodged: boolean; crit: boolean } { if (rng() < def.dodgeChance) return { damage: 0, dodged: true, crit: false }; const isCrit = rng() < atk.critChance; const variance = 0.9 + rng() * 0.2; const rawAtk = school === "physical" ? atk.physicalAttack : atk.magicAttack; const rawDef = school === "physical" ? def.physicalDefense : def.magicDefense; const base = Math.max(1, rawAtk - rawDef); return { damage: Math.round(base * variance * powerMultiplier * (isCrit ? 1.5 : 1)), dodged: false, crit: isCrit }; } function applyDamage(target: PlayerCombatState | EnemyCombatState, result: ReturnType<typeof rollDamage>, log: CombatLogEntry[], actor: string, name: string) { if (result.dodged) { log.push({ actor, text: `${name} dodges the attack!` }); return; } let dmg = result.damage; if ("defending" in target && target.defending) dmg = Math.round(dmg * 0.5); target.currentHp = Math.max(0, target.currentHp - dmg); log.push({ actor, text: `${name} takes ${dmg} damage${result.crit ? " (critical hit!)" : ""}.` }); } function sortBySpeed(entries: { key: string; speed: number }[], rng: () => number) { return entries.map((e) => ({ e, jitter: rng() })).sort((a, b) => b.e.speed - a.e.speed || b.jitter - a.jitter).map((x) => x.e); } function removeDead(enemies: EnemyCombatState[], log: CombatLogEntry[]): { alive: EnemyCombatState[]; deadKeys: string[] } { const alive: EnemyCombatState[] = []; const deadKeys: string[] = []; for (const e of enemies) { if (e.currentHp <= 0) { log.push({ actor: "system", text: `${e.name} is defeated.` }); deadKeys.push(e.templateKey); } else alive.push(e); } return { alive, deadKeys }; } function tickStatuses(entity: PlayerCombatState | EnemyCombatState, log: CombatLogEntry[], label: string) { const remaining: StatusEffect[] = []; for (const s of entity.statuses) { if (s.kind === "poison") { entity.currentHp = Math.max(0, entity.currentHp - s.magnitude); log.push({ actor: "system", text: `${label} takes ${s.magnitude} poison damage.` }); } if (s.kind === "regen") { entity.currentHp = Math.min(entity.maxHp, entity.currentHp + s.magnitude); log.push({ actor: "system", text: `${label} regenerates ${s.magnitude} HP.` }); } const left = s.remainingTurns - 1; if (left > 0) remaining.push({ ...s, remainingTurns: left }); else log.push({ actor: "system", text: `${label}'s ${s.kind.replace("_", " ")} wears off.` }); } entity.statuses = remaining; } function resolvePlayerAction( player: PlayerCombatState, enemies: EnemyCombatState[], action: PlayerAction, abilities: Record<string, AbilityDef>, rng: () => number, log: CombatLogEntry[] ) { player.defending = false; if (action.type === "defend") { player.defending = true; log.push({ actor: "player", text: "You brace for the enemy's attack." }); return; } if (action.type === "use_item") { player.currentHp = Math.min(player.maxHp, player.currentHp + action.healAmount); log.push({ actor: "player", text: `You use an item and recover ${action.healAmount} HP.` }); return; } if (action.type === "basic_attack") { const target = enemies.find((e) => e.instanceId === action.targetInstanceId && e.currentHp > 0); if (!target) { log.push({ actor: "system", text: "Invalid target." }); return; } const atk = withStatusModifiers(baseDerivedStats(player.stats), player.statuses); const def = withStatusModifiers(baseDerivedStats(target.stats), target.statuses); applyDamage(target, rollDamage(atk, def, rng, 1, "physical"), log, "player", target.name); return; } const ability = abilities[action.type === "ability" ? action.abilityKey : ""]; if (action.type !== "ability" || !ability) { log.push({ actor: "system", text: "Unknown ability." }); return; } if (player.currentMp < ability.mpCost) { log.push({ actor: "system", text: "Not enough MP." }); return; } player.currentMp -= ability.mpCost; const targets: (PlayerCombatState | EnemyCombatState)[] = ability.targetType === "all_enemies" ? enemies.filter((e) => e.currentHp > 0) : ability.targetType === "self" ? [player] : enemies.filter((e) => e.instanceId === action.targetInstanceId && e.currentHp > 0); if (targets.length === 0) { log.push({ actor: "system", text: "Invalid target." }); return; } for (const target of targets) { const isPlayer = target === player; const name = isPlayer ? "You" : (target as EnemyCombatState).name; if (ability.effectType === "physical_damage" || ability.effectType === "magic_damage") { const atk = withStatusModifiers(baseDerivedStats(player.stats), player.statuses); const def = withStatusModifiers(baseDerivedStats(target.stats), target.statuses); const result = rollDamage(atk, def, rng, ability.powerMultiplier, ability.effectType === "physical_damage" ? "physical" : "magic"); applyDamage(target, result, log, "player", name); if (ability.statusEffect && result.damage > 0) target.statuses.push({ ...ability.statusEffect, remainingTurns: ability.statusEffect.duration }); } else if (ability.effectType === "heal") { const healAmt = Math.round(ability.powerMultiplier * baseDerivedStats(player.stats).magicAttack); target.currentHp = Math.min(target.maxHp, target.currentHp + healAmt); log.push({ actor: "player", text: `${isPlayer ? "You heal" : name + " is healed"} for ${healAmt} HP.` }); } else if (ability.effectType === "status" && ability.statusEffect) { target.statuses.push({ ...ability.statusEffect, remainingTurns: ability.statusEffect.duration }); log.push({ actor: "player", text: `${isPlayer ? "You are" : name + " is"} afflicted with ${ability.statusEffect.kind}.` }); } } log.push({ actor: "player", text: `You cast ${ability.name}.` }); } function resolveEnemyAction(enemy: EnemyCombatState, player: PlayerCombatState, abilities: Record<string, AbilityDef>, rng: () => number, log: CombatLogEntry[]) { const known = enemy.abilityKeys.map((k) => abilities[k]).filter((a): a is AbilityDef => !!a); const healAbility = known.find((a) => a.effectType === "heal"); const hpRatio = enemy.currentHp / enemy.maxHp; // Deliberately simple, stated AI: heal if low, otherwise a coin-flip chance // to use a damage ability, otherwise a basic attack. "Deep" in this build // refers to the stat/ability/status system, not enemy decision-making. let chosen: AbilityDef | null = null; if (healAbility && hpRatio < 0.3) chosen = healAbility; else { const damageAbilities = known.filter((a) => a.effectType !== "heal"); if (damageAbilities.length > 0 && rng() < 0.5) chosen = damageAbilities[Math.floor(rng() * damageAbilities.length)]; } if (chosen?.effectType === "heal") { const healAmt = Math.round(chosen.powerMultiplier * baseDerivedStats(enemy.stats).magicAttack); enemy.currentHp = Math.min(enemy.maxHp, enemy.currentHp + healAmt); log.push({ actor: enemy.name, text: `${enemy.name} heals for ${healAmt} HP.` }); return; } const ability = chosen; const atk = withStatusModifiers(baseDerivedStats(enemy.stats), enemy.statuses); const def = withStatusModifiers(baseDerivedStats(player.stats), player.statuses); const result = rollDamage(atk, def, rng, ability?.powerMultiplier ?? 1, ability?.effectType === "magic_damage" ? "magic" : "physical"); applyDamage(player, result, log, enemy.name, "You"); if (ability?.statusEffect && result.damage > 0) player.statuses.push({ ...ability.statusEffect, remainingTurns: ability.statusEffect.duration }); log.push({ actor: enemy.name, text: ability ? `${enemy.name} uses ${ability.name}.` : `${enemy.name} attacks.` }); } export function advanceRound( state: CombatSessionState, playerAction: PlayerAction, abilities: Record<string, AbilityDef>, rng: () => number = Math.random ): { state: CombatSessionState; deadEnemyTemplateKeys: string[] } { if (state.status !== "active") return { state, deadEnemyTemplateKeys: [] }; const player: PlayerCombatState = { ...state.player, statuses: [...state.player.statuses] }; let enemies: EnemyCombatState[] = state.enemies.map((e) => ({ ...e, statuses: [...e.statuses] })); const log: CombatLogEntry[] = []; const allDeadKeys: string[] = []; if (playerAction.type === "flee") { if (enemies.some((e) => e.unfleeable)) { log.push({ actor: "system", text: "You can't flee from this fight." }); return { state: { ...state, log: [...state.log, ...log] }, deadEnemyTemplateKeys: [] }; } log.push({ actor: "player", text: "You flee from the battle." }); return { state: { ...state, player, enemies, log: [...state.log, ...log], status: "fled" }, deadEnemyTemplateKeys: [] }; } const order = sortBySpeed( [ { key: "player", speed: withStatusModifiers(baseDerivedStats(player.stats), player.statuses).speed }, ...enemies.map((e) => ({ key: String(e.instanceId), speed: withStatusModifiers(baseDerivedStats(e.stats), e.statuses).speed })), ], rng ); for (const turn of order) { if (player.currentHp <= 0 || enemies.length === 0) break; if (turn.key === "player") { if (player.statuses.some((s) => s.kind === "stun")) { log.push({ actor: "player", text: "You are stunned and cannot act." }); continue; } resolvePlayerAction(player, enemies, playerAction, abilities, rng, log); const { alive, deadKeys } = removeDead(enemies, log); enemies = alive; allDeadKeys.push(...deadKeys); } else { const enemy = enemies.find((e) => String(e.instanceId) === turn.key); if (!enemy || enemy.currentHp <= 0) continue; if (enemy.statuses.some((s) => s.kind === "stun")) { log.push({ actor: enemy.name, text: `${enemy.name} is stunned and cannot act.` }); continue; } resolveEnemyAction(enemy, player, abilities, rng, log); } } let status: CombatSessionState["status"] = "active"; if (player.currentHp <= 0) status = "defeat"; else if (enemies.length === 0) status = "victory"; if (status === "active") { tickStatuses(player, log, "You"); for (const e of enemies) tickStatuses(e, log, e.name); if (player.currentHp <= 0) status = "defeat"; // poison can finish the player off during the end-of-round tick } return { state: { player, enemies, log: [...state.log, ...log], round: state.round + 1, status }, deadEnemyTemplateKeys: allDeadKeys, }; }// Run with: npx tsx scripts/test-rpg-combat-engine.mjs // Deterministic PRNG so results are reproducible — real evidence, not a // hand-traced claim, per the Witness principle applied to every risky // engine in this build (War, Mystery, Shooter, now this). import { advanceRound } from "../src/lib/rpg/combat-engine.ts"; function mulberry32(seed) { return function () { seed |= 0; seed = (seed + 0x6D2B79F5) | 0; let t = Math.imul(seed ^ (seed >>> 15), 1 | seed); t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t; return ((t ^ (t >>> 14)) >>> 0) / 4294967296; }; } let failed = false; const fail = (msg) => { console.error(`FAIL: ${msg}`); failed = true; }; const playerStats = { str: 8, dex: 4, int: 2, vit: 8, wis: 2, level: 1 }; const weakEnemyStats = { str: 1, dex: 1, int: 1, vit: 1, wis: 1, level: 1 }; function freshState() { return { player: { currentHp: 84, maxHp: 84, currentMp: 14, maxMp: 14, stats: playerStats, statuses: [], defending: false }, enemies: [{ instanceId: 1, templateKey: "test_grunt", name: "Test Grunt", currentHp: 5, maxHp: 5, stats: weakEnemyStats, statuses: [], abilityKeys: [], xpReward: 5, goldReward: 1, unfleeable: false }], log: [], round: 0, status: "active", }; } // Test 1: basic attack against a near-zero-dodge, near-zero-defense enemy deals damage and kills it. { const rng = mulberry32(1); const { state, deadEnemyTemplateKeys } = advanceRound(freshState(), { type: "basic_attack", targetInstanceId: 1 }, {}, rng); if (state.status !== "victory") fail(`expected victory against a 5hp enemy, got status=${state.status}`); if (!deadEnemyTemplateKeys.includes("test_grunt")) fail("dead enemy template key not reported"); else console.log("Test 1 OK: basic attack defeats a weak enemy, victory + dead-key reporting both correct"); } // Test 2: stun status prevents the stunned entity from acting, verified by zero HP change to the stunned side. { const rng = mulberry32(2); const state = freshState(); state.enemies[0].currentHp = 5000; state.enemies[0].maxHp = 5000; // don't let it die mid-test, isolate the stun check state.player.statuses.push({ kind: "stun", remainingTurns: 1, magnitude: 0 }); const { state: after } = advanceRound(state, { type: "basic_attack", targetInstanceId: 1 }, {}, rng); if (after.enemies[0].currentHp !== 5000) fail("stunned player still dealt damage — stun check did not block the action"); else console.log("Test 2 OK: stunned player's action was correctly skipped"); } // Test 3: unfleeable enemy blocks flee, status remains active. { const rng = mulberry32(3); const state = freshState(); state.enemies[0].unfleeable = true; const { state: after } = advanceRound(state, { type: "flee" }, {}, rng); if (after.status !== "active") fail(`expected flee to be blocked (status stays active), got ${after.status}`); else console.log("Test 3 OK: unfleeable flag correctly blocks flee"); } // Test 4: poison ticks damage at end of round even with no other actions landing. { const rng = mulberry32(4); const state = freshState(); state.enemies[0].currentHp = 5000; state.enemies[0].maxHp = 5000; state.player.statuses.push({ kind: "poison", remainingTurns: 2, magnitude: 7 }); const hpBefore = state.player.currentHp; const { state: after } = advanceRound(state, { type: "defend" }, {}, rng); if (after.player.currentHp !== hpBefore - 7) fail(`expected exactly 7 poison damage, hp went from ${hpBefore} to ${after.player.currentHp}`); else console.log("Test 4 OK: poison tick applied exactly once for 7 damage"); } if (failed) process.exit(1); console.log("PASS: all 4 RPG combat engine invariants held.");export function isQuestAvailable(prerequisiteQuestKeys: string[], completedQuestKeys: Set<string>): boolean { return prerequisiteQuestKeys.every((k) => completedQuestKeys.has(k)); } // Only "kill" objectives are exercised by seeded content this pass — see // the honest gap noted at the top of this section for collect/talk. export function advanceKillProgress( currentStageOrder: number, stageProgressCount: number, stages: { stageOrder: number; objectiveType: string; objectiveTarget: string; objectiveCount: number }[], killedTemplateKeys: string[] ): { newStageOrder: number; newProgressCount: number; readyToTurnIn: boolean } { const stage = stages.find((s) => s.stageOrder === currentStageOrder); if (!stage || stage.objectiveType !== "kill") { return { newStageOrder: currentStageOrder, newProgressCount: stageProgressCount, readyToTurnIn: false }; } const matches = killedTemplateKeys.filter((k) => k === stage.objectiveTarget).length; if (matches === 0) return { newStageOrder: currentStageOrder, newProgressCount: stageProgressCount, readyToTurnIn: false }; const newCount = Math.min(stage.objectiveCount, stageProgressCount + matches); if (newCount < stage.objectiveCount) return { newStageOrder: currentStageOrder, newProgressCount: newCount, readyToTurnIn: false }; const nextStage = stages.find((s) => s.stageOrder === currentStageOrder + 1); if (nextStage) return { newStageOrder: nextStage.stageOrder, newProgressCount: 0, readyToTurnIn: false }; return { newStageOrder: currentStageOrder, newProgressCount: newCount, readyToTurnIn: true }; }import { db } from "@/lib/db"; import { rpgClasses, rpgAbilities, rpgItems, rpgZones, rpgEnemyTemplates, rpgEncounters, rpgQuests, rpgQuestStages, } from "@/lib/db/schema/rpg"; export async function seedRpgContent() { await db.insert(rpgClasses).values([ { key: "warrior", name: "Warrior", description: "Front-line fighter, high strength and vitality.", baseStr: 8, baseDex: 4, baseInt: 2, baseVit: 8, baseWis: 2 }, { key: "mage", name: "Mage", description: "Wields elemental force, fragile but devastating.", baseStr: 2, baseDex: 4, baseInt: 9, baseVit: 3, baseWis: 6 }, { key: "ranger", name: "Ranger", description: "Fast and precise, favors ranged strikes and poison.", baseStr: 5, baseDex: 9, baseInt: 3, baseVit: 4, baseWis: 3 }, ]).onConflictDoNothing(); await db.insert(rpgAbilities).values([ { key: "power_strike", name: "Power Strike", description: "A heavy blow.", classKey: "warrior", unlockLevel: 1, mpCost: 3, targetType: "enemy", effectType: "physical_damage", powerMultiplier: 1.5, statusKind: null, statusDuration: null, statusMagnitude: null }, { key: "guard_break", name: "Guard Break", description: "Weakens the target's defenses.", classKey: "warrior", unlockLevel: 3, mpCost: 4, targetType: "enemy", effectType: "status", powerMultiplier: 1, statusKind: "debuff_def", statusDuration: 3, statusMagnitude: 4 }, { key: "firebolt", name: "Firebolt", description: "A bolt of searing flame.", classKey: "mage", unlockLevel: 1, mpCost: 4, targetType: "enemy", effectType: "magic_damage", powerMultiplier: 1.4, statusKind: null, statusDuration: null, statusMagnitude: null }, { key: "flame_wave", name: "Flame Wave", description: "Fire that washes over every foe.", classKey: "mage", unlockLevel: 3, mpCost: 7, targetType: "all_enemies", effectType: "magic_damage", powerMultiplier: 1.1, statusKind: null, statusDuration: null, statusMagnitude: null }, { key: "aimed_shot", name: "Aimed Shot", description: "A precise, powerful shot.", classKey: "ranger", unlockLevel: 1, mpCost: 3, targetType: "enemy", effectType: "physical_damage", powerMultiplier: 1.3, statusKind: null, statusDuration: null, statusMagnitude: null }, { key: "poison_arrow", name: "Poison Arrow", description: "A toxin-laced strike.", classKey: "ranger", unlockLevel: 2, mpCost: 5, targetType: "enemy", effectType: "physical_damage", powerMultiplier: 0.8, statusKind: "poison", statusDuration: 3, statusMagnitude: 6 }, ]).onConflictDoNothing(); await db.insert(rpgItems).values([ { key: "minor_healing_draught", name: "Minor Healing Draught", description: "Restores a modest amount of HP.", itemType: "consumable", slot: null, statBonuses: {}, healAmount: 25, value: 10 }, { key: "wardens_cinderplate", name: "Warden's Cinderplate", description: "Ash-forged armor, warm to the touch.", itemType: "armor", slot: "armor", statBonuses: { vit: 3, str: 1 }, healAmount: null, value: 150 }, ]).onConflictDoNothing(); await db.insert(rpgZones).values([ { key: "hollow_reach", name: "Hollow Reach", description: "A fog-choked ravine where old grudges never settled.", requiredLevel: 1 }, ]).onConflictDoNothing(); await db.insert(rpgEnemyTemplates).values([ { key: "hollow_wisp", name: "Hollow Wisp", zoneKey: "hollow_reach", level: 1, str: 3, dex: 3, int: 2, vit: 3, wis: 2, abilityKeys: [], xpReward: 20, goldReward: 5, unfleeable: false }, { key: "cinder_wisp", name: "Cinder Wisp", zoneKey: "hollow_reach", level: 2, str: 4, dex: 4, int: 3, vit: 4, wis: 2, abilityKeys: [], xpReward: 25, goldReward: 6, unfleeable: false }, { key: "warden_of_ash", name: "Warden of Ash", zoneKey: "hollow_reach", level: 3, str: 9, dex: 5, int: 4, vit: 10, wis: 4, abilityKeys: ["ashen_slam"], xpReward: 120, goldReward: 40, unfleeable: true }, ]).onConflictDoNothing(); // Boss-only ability, seeded separately since it belongs to no player class. await db.insert(rpgAbilities).values([ { key: "ashen_slam", name: "Ashen Slam", description: "A crushing blow that weakens the target's guard.", classKey: "warrior", unlockLevel: 1, mpCost: 0, targetType: "enemy", effectType: "status", powerMultiplier: 1.2, statusKind: "debuff_def", statusDuration: 2, statusMagnitude: 3 }, // NOTE: classKey is set to "warrior" purely to satisfy the FK — this // ability is never surfaced to any warrior player, only referenced // directly by the Warden's abilityKeys array. A cleaner long-term fix // is a nullable classKey for enemy-only abilities; not changed this // pass to avoid an unnecessary schema churn for one row. ]).onConflictDoNothing(); await db.insert(rpgEncounters).values([ { key: "wisp_encounter", zoneKey: "hollow_reach", name: "A Hollow Wisp stirs", enemyTemplateKeys: ["hollow_wisp"] }, { key: "warden_encounter", zoneKey: "hollow_reach", name: "The Warden of Ash awakens", enemyTemplateKeys: ["warden_of_ash", "cinder_wisp", "cinder_wisp"] }, ]).onConflictDoNothing(); await db.insert(rpgQuests).values([ { key: "whispers_in_hollow_reach", title: "Whispers in Hollow Reach", description: "The wisps are restless. Thin their numbers.", zoneKey: "hollow_reach", giverName: "Old Maren", prerequisiteQuestKeys: [], rewardXp: 60, rewardGold: 30, rewardItemKeys: ["minor_healing_draught"] }, { key: "the_ashen_warden", title: "The Ashen Warden", description: "Something ancient guards the ravine's heart. End its vigil.", zoneKey: "hollow_reach", giverName: "Old Maren", prerequisiteQuestKeys: ["whispers_in_hollow_reach"], rewardXp: 200, rewardGold: 80, rewardItemKeys: ["wardens_cinderplate"] }, ]).onConflictDoNothing(); await db.insert(rpgQuestStages).values([ { questKey: "whispers_in_hollow_reach", stageOrder: 1, objectiveType: "kill", objectiveTarget: "hollow_wisp", objectiveCount: 3, description: "Defeat 3 Hollow Wisps." }, { questKey: "the_ashen_warden", stageOrder: 1, objectiveType: "kill", objectiveTarget: "warden_of_ash", objectiveCount: 1, description: "Defeat the Warden of Ash." }, ]).onConflictDoNothing(); console.log("[seed:rpg] Hollow Reach starting zone seeded."); } if (require.main === module) { seedRpgContent().then(() => process.exit(0)).catch((e) => { console.error(e); process.exit(1); }); }import { db } from "@/lib/db"; import { rpgCharacters, rpgEquipment, rpgItems } from "@/lib/db/schema/rpg"; import { eq, inArray } from "drizzle-orm"; import { BaseStats, baseDerivedStats } from "./stats"; export async function getEffectiveBaseStats(characterId: string): Promise<BaseStats | null> { const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.id, characterId)).limit(1); if (!character) return null; const [equipment] = await db.select().from(rpgEquipment).where(eq(rpgEquipment.characterId, characterId)).limit(1); const equippedKeys = [equipment?.weaponKey, equipment?.armorKey, equipment?.accessoryKey].filter((k): k is string => !!k); const items = equippedKeys.length > 0 ? await db.select().from(rpgItems).where(inArray(rpgItems.key, equippedKeys)) : []; const bonuses = { str: 0, dex: 0, int: 0, vit: 0, wis: 0 }; for (const item of items) { const b = item.statBonuses as Record<string, number>; for (const k of Object.keys(bonuses) as (keyof typeof bonuses)[]) bonuses[k] += b[k] ?? 0; } return { str: character.str + bonuses.str, dex: character.dex + bonuses.dex, int: character.int + bonuses.int, vit: character.vit + bonuses.vit, wis: character.wis + bonuses.wis, level: character.level, }; }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgClasses, rpgInventory } from "@/lib/db/schema/rpg"; import { baseDerivedStats } from "@/lib/rpg/stats"; import { eq } from "drizzle-orm"; import { z } from "zod"; const createSchema = z.object({ name: z.string().trim().min(2).max(30), classKey: z.enum(["warrior", "mage", "ranger"]) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [existing] = await db.select({ id: rpgCharacters.id }).from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (existing) return NextResponse.json({ error: "character_already_exists" }, { status: 409 }); const parsed = createSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const [cls] = await db.select().from(rpgClasses).where(eq(rpgClasses.key, parsed.data.classKey)).limit(1); if (!cls) return NextResponse.json({ error: "invalid_class" }, { status: 400 }); const stats = { str: cls.baseStr, dex: cls.baseDex, int: cls.baseInt, vit: cls.baseVit, wis: cls.baseWis, level: 1 }; const derived = baseDerivedStats(stats); const [character] = await db.insert(rpgCharacters).values({ memberId: viewer.id, name: parsed.data.name, classKey: cls.key, ...stats, currentHp: derived.maxHp, currentMp: derived.maxMp, currentZoneKey: "hollow_reach", }).returning(); await db.insert(rpgInventory).values({ characterId: character.id, itemKey: "minor_healing_draught", quantity: 3 }); return NextResponse.json({ character }, { status: 201 }); }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgEquipment, rpgInventory } from "@/lib/db/schema/rpg"; import { getEffectiveBaseStats } from "@/lib/rpg/character-data"; import { baseDerivedStats } from "@/lib/rpg/stats"; import { eq } from "drizzle-orm"; export async function GET() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (!character) return NextResponse.json({ character: null }); const [equipment] = await db.select().from(rpgEquipment).where(eq(rpgEquipment.characterId, character.id)).limit(1); const inventory = await db.select().from(rpgInventory).where(eq(rpgInventory.characterId, character.id)); const effectiveStats = await getEffectiveBaseStats(character.id); return NextResponse.json({ character, equipment: equipment ?? null, inventory, derived: effectiveStats ? baseDerivedStats(effectiveStats) : null, }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters } from "@/lib/db/schema/rpg"; import { baseDerivedStats } from "@/lib/rpg/stats"; import { eq } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ str: z.number().int().min(0), dex: z.number().int().min(0), int: z.number().int().min(0), vit: z.number().int().min(0), wis: z.number().int().min(0) }); export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const total = parsed.data.str + parsed.data.dex + parsed.data.int + parsed.data.vit + parsed.data.wis; if (total > character.unspentStatPoints) return NextResponse.json({ error: "insufficient_points" }, { status: 400 }); if (total === 0) return NextResponse.json({ error: "no_points_allocated" }, { status: 400 }); const newStats = { str: character.str + parsed.data.str, dex: character.dex + parsed.data.dex, int: character.int + parsed.data.int, vit: character.vit + parsed.data.vit, wis: character.wis + parsed.data.wis, level: character.level, }; const derived = baseDerivedStats(newStats); // Clamp, don't heal: raising VIT/INT raises the cap, but current HP/MP // only clamps down if it would exceed the new max — it never tops up. // Free-healing via re-spec would be a real, if minor, exploit. await db.update(rpgCharacters).set({ ...newStats, unspentStatPoints: character.unspentStatPoints - total, currentHp: Math.min(character.currentHp, derived.maxHp), currentMp: Math.min(character.currentMp, derived.maxMp), }).where(eq(rpgCharacters.id, character.id)); return NextResponse.json({ status: "allocated" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgInventory, rpgItems, rpgEquipment } from "@/lib/db/schema/rpg"; import { eq, and } from "drizzle-orm"; import { z } from "zod"; const equipSchema = z.object({ itemKey: z.string() }); const unequipSchema = z.object({ slot: z.enum(["weapon", "armor", "accessory"]) }); async function getCharacterOr404(viewerId: string) { const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewerId)).limit(1); return character ?? null; } export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const character = await getCharacterOr404(viewer.id); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const parsed = equipSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [owned] = await db.select().from(rpgInventory).where(and(eq(rpgInventory.characterId, character.id), eq(rpgInventory.itemKey, parsed.data.itemKey))).limit(1); if (!owned || owned.quantity < 1) return NextResponse.json({ error: "not_owned" }, { status: 403 }); const [item] = await db.select().from(rpgItems).where(eq(rpgItems.key, parsed.data.itemKey)).limit(1); if (!item || !item.slot) return NextResponse.json({ error: "not_equippable" }, { status: 400 }); const column = item.slot === "weapon" ? { weaponKey: item.key } : item.slot === "armor" ? { armorKey: item.key } : { accessoryKey: item.key }; await db.insert(rpgEquipment).values({ characterId: character.id, ...column }) .onConflictDoUpdate({ target: rpgEquipment.characterId, set: column }); return NextResponse.json({ status: "equipped" }); } export async function DELETE(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const character = await getCharacterOr404(viewer.id); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const parsed = unequipSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const column = parsed.data.slot === "weapon" ? { weaponKey: null } : parsed.data.slot === "armor" ? { armorKey: null } : { accessoryKey: null }; await db.update(rpgEquipment).set(column).where(eq(rpgEquipment.characterId, character.id)); return NextResponse.json({ status: "unequipped" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgCombatSessions, rpgEncounters, rpgEnemyTemplates } from "@/lib/db/schema/rpg"; import { getEffectiveBaseStats } from "@/lib/rpg/character-data"; import { baseDerivedStats } from "@/lib/rpg/stats"; import { eq, and, sql } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ encounterKey: z.string() }); let instanceCounter = 0; export async function POST(req: NextRequest) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const outcome = await db.transaction(async (tx) => { // Row-lock the character to serialize concurrent "start combat" clicks — // same idiom as every other resource-guard in this build. const locked = await tx.execute(sql`SELECT * FROM rpg_characters WHERE member_id = ${viewer.id} FOR UPDATE`) as unknown as [any]; const character = locked[0]; if (!character) return { httpStatus: 404, body: { error: "no_character" } }; const [activeSession] = await tx.select().from(rpgCombatSessions).where(and(eq(rpgCombatSessions.characterId, character.id), eq(rpgCombatSessions.status, "active"))).limit(1); if (activeSession) return { httpStatus: 409, body: { error: "combat_already_active", sessionId: activeSession.id } }; const [encounter] = await tx.select().from(rpgEncounters).where(eq(rpgEncounters.key, parsed.data.encounterKey)).limit(1); if (!encounter) return { httpStatus: 404, body: { error: "encounter_not_found" } }; const templates = await tx.select().from(rpgEnemyTemplates).where(sql`key = ANY(${encounter.enemyTemplateKeys})`); const templateByKey = new Map(templates.map((t) => [t.key, t])); const effectiveStats = await getEffectiveBaseStats(character.id); if (!effectiveStats) return { httpStatus: 500, body: { error: "stats_unavailable" } }; const playerDerived = baseDerivedStats(effectiveStats); const enemies = encounter.enemyTemplateKeys.map((key) => { const t = templateByKey.get(key)!; const derived = baseDerivedStats({ str: t.str, dex: t.dex, int: t.int, vit: t.vit, wis: t.wis, level: t.level }); return { instanceId: ++instanceCounter, templateKey: t.key, name: t.name, currentHp: derived.maxHp, maxHp: derived.maxHp, stats: { str: t.str, dex: t.dex, int: t.int, vit: t.vit, wis: t.wis, level: t.level }, statuses: [], abilityKeys: t.abilityKeys, xpReward: t.xpReward, goldReward: t.goldReward, unfleeable: t.unfleeable, }; }); const state = { player: { currentHp: character.currentHp, maxHp: playerDerived.maxHp, currentMp: character.currentMp, maxMp: playerDerived.maxMp, stats: effectiveStats, statuses: [], defending: false }, enemies, log: [{ actor: "system", text: `${encounter.name} begins.` }], round: 0, status: "active" as const, }; const [session] = await tx.insert(rpgCombatSessions).values({ characterId: character.id, encounterKey: encounter.key, state }).returning(); return { httpStatus: 201, body: { sessionId: session.id, state } }; }); return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCombatSessions, rpgCharacters, rpgAbilities, rpgItems, rpgInventory, rpgCharacterQuestProgress, rpgQuestStages, } from "@/lib/db/schema/rpg"; import { advanceRound, AbilityDef, PlayerAction } from "@/lib/rpg/combat-engine"; import { advanceKillProgress } from "@/lib/rpg/quest-engine"; import { xpToNextLevel, baseDerivedStats } from "@/lib/rpg/stats"; import { emitMemberEvent } from "@/lib/roles/events"; import { arcadeScores } from "@/lib/db/schema/arcade-core"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq, and, sql } from "drizzle-orm"; import { z } from "zod"; const actionSchema = z.discriminatedUnion("type", [ z.object({ type: z.literal("basic_attack"), targetInstanceId: z.number().int() }), z.object({ type: z.literal("ability"), abilityKey: z.string(), targetInstanceId: z.number().int().optional() }), z.object({ type: z.literal("defend") }), z.object({ type: z.literal("use_item"), itemKey: z.string() }), z.object({ type: z.literal("flee") }), ]); export async function POST(req: NextRequest, { params }: { params: { sessionId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = actionSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM rpg_combat_sessions WHERE id = ${params.sessionId} FOR UPDATE`) as unknown as [any]; const session = locked[0]; if (!session) return { httpStatus: 404, body: { error: "not_found" } }; const [character] = await tx.select().from(rpgCharacters).where(eq(rpgCharacters.id, session.character_id)).limit(1); if (!character || character.member_id !== viewer.id) return { httpStatus: 403, body: { error: "forbidden" } }; if (session.status !== "active") return { httpStatus: 409, body: { error: "combat_not_active" } }; let playerAction: PlayerAction; if (parsed.data.type === "use_item") { const [inv] = await tx.select().from(rpgInventory).where(and(eq(rpgInventory.characterId, character.id), eq(rpgInventory.itemKey, parsed.data.itemKey))).limit(1); if (!inv || inv.quantity < 1) return { httpStatus: 400, body: { error: "item_not_owned" } }; const [item] = await tx.select().from(rpgItems).where(eq(rpgItems.key, parsed.data.itemKey)).limit(1); if (!item || item.itemType !== "consumable" || !item.healAmount) return { httpStatus: 400, body: { error: "item_not_usable" } }; await tx.update(rpgInventory).set({ quantity: sql`${rpgInventory.quantity} - 1` }).where(and(eq(rpgInventory.characterId, character.id), eq(rpgInventory.itemKey, parsed.data.itemKey))); playerAction = { type: "use_item", healAmount: item.healAmount }; } else { playerAction = parsed.data as PlayerAction; } const abilityRows = await tx.select().from(rpgAbilities); const abilities: Record<string, AbilityDef> = Object.fromEntries(abilityRows.map((a) => [a.key, { key: a.key, name: a.name, mpCost: a.mpCost, targetType: a.targetType, effectType: a.effectType, powerMultiplier: a.powerMultiplier, statusEffect: a.statusKind ? { kind: a.statusKind as any, duration: a.statusDuration!, magnitude: a.statusMagnitude! } : null, }])); const { state: newState, deadEnemyTemplateKeys } = advanceRound(session.state, playerAction, abilities); await tx.update(rpgCombatSessions).set({ state: newState, status: newState.status, completedAt: newState.status !== "active" ? new Date() : null, }).where(eq(rpgCombatSessions.id, params.sessionId)); return { httpStatus: 200, body: { state: newState }, finalStatus: newState.status, character, session, deadEnemyTemplateKeys, newState, }; }); if (outcome.httpStatus !== 200 || !("finalStatus" in outcome)) { return NextResponse.json(outcome.body, { status: outcome.httpStatus }); } const { finalStatus, character, session, deadEnemyTemplateKeys, newState } = outcome; if (finalStatus === "victory") { const xpGained = newState.enemies.length === 0 ? (await db.select().from(rpgCombatSessions).where(eq(rpgCombatSessions.id, session.id)))[0] // placeholder, replaced below : null; // Reward totals come from the ORIGINAL enemy list at combat start, not // the post-battle (now-empty) enemies array — compute from session.state // captured before this round's mutation via the enemy templates in the // pre-round state, which we still have as `session.state.enemies`. const rewardSource = (session.state as any).enemies as { xpReward: number; goldReward: number }[]; const totalXp = rewardSource.reduce((sum, e) => sum + e.xpReward, 0); const totalGold = rewardSource.reduce((sum, e) => sum + e.goldReward, 0); let level = character.level, xp = character.xp + totalXp, unspent = character.unspentStatPoints; let leveledUp = false; while (xp >= xpToNextLevel(level)) { xp -= xpToNextLevel(level); level += 1; unspent += 3; leveledUp = true; } const derived = baseDerivedStats({ str: character.str, dex: character.dex, int: character.int, vit: character.vit, wis: character.wis, level }); await db.update(rpgCharacters).set({ level, xp, unspentStatPoints: unspent, gold: character.gold + totalGold, currentHp: leveledUp ? derived.maxHp : newState.player.currentHp, // level-up grants a full heal — a stated, standard RPG convention, distinct from the no-free-heal rule on stat allocation currentMp: leveledUp ? derived.maxMp : newState.player.currentMp, }).where(eq(rpgCharacters.id, character.id)); if (leveledUp) { await emitMemberEvent(character.member_id, "rpg.character_leveled", { level }); await db.insert(arcadeScores).values({ memberId: character.member_id, gameKey: "rpg_character_level", score: level }); await recordArcadeActivity(character.member_id); } if (deadEnemyTemplateKeys.length > 0) { const activeQuests = await db.select().from(rpgCharacterQuestProgress) .where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.status, "active"))); for (const progress of activeQuests) { const stages = await db.select().from(rpgQuestStages).where(eq(rpgQuestStages.questKey, progress.questKey)); const result = advanceKillProgress(progress.currentStageOrder, progress.stageProgressCount, stages, deadEnemyTemplateKeys); await db.update(rpgCharacterQuestProgress).set({ currentStageOrder: result.newStageOrder, stageProgressCount: result.newProgressCount, status: result.readyToTurnIn ? "ready_to_turn_in" : "active", }).where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, progress.questKey))); } } } else if (finalStatus === "defeat") { // Non-punishing death, stated design choice: no XP loss, no gear loss — // only a small gold tithe, consistent with the streak system's // "never punish, always let progress persist" philosophy from Section 5. const goldLoss = Math.floor(character.gold * 0.1); await db.update(rpgCharacters).set({ currentHp: 1, gold: character.gold - goldLoss }).where(eq(rpgCharacters.id, character.id)); } else if (finalStatus === "fled") { await db.update(rpgCharacters).set({ currentHp: newState.player.currentHp, currentMp: newState.player.currentMp }).where(eq(rpgCharacters.id, character.id)); } else { // still active — persist mid-fight HP/MP so a page refresh doesn't roll back the character to pre-fight values await db.update(rpgCharacters).set({ currentHp: newState.player.currentHp, currentMp: newState.player.currentMp }).where(eq(rpgCharacters.id, character.id)); } return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgQuests, rpgCharacterQuestProgress } from "@/lib/db/schema/rpg"; import { isQuestAvailable } from "@/lib/rpg/quest-engine"; import { eq, and } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { questKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const [quest] = await db.select().from(rpgQuests).where(eq(rpgQuests.key, params.questKey)).limit(1); if (!quest) return NextResponse.json({ error: "not_found" }, { status: 404 }); const completed = await db.select({ questKey: rpgCharacterQuestProgress.questKey }).from(rpgCharacterQuestProgress) .where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.status, "completed"))); if (!isQuestAvailable(quest.prerequisiteQuestKeys, new Set(completed.map((c) => c.questKey)))) { return NextResponse.json({ error: "prerequisites_not_met" }, { status: 409 }); } await db.insert(rpgCharacterQuestProgress).values({ characterId: character.id, questKey: quest.key, status: "active", currentStageOrder: 1 }) .onConflictDoNothing(); return NextResponse.json({ status: "accepted" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgQuests, rpgCharacterQuestProgress, rpgInventory } from "@/lib/db/schema/rpg"; import { eq, and, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { questKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const [progress] = await db.select().from(rpgCharacterQuestProgress) .where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, params.questKey))).limit(1); if (!progress || progress.status !== "ready_to_turn_in") return NextResponse.json({ error: "not_ready" }, { status: 409 }); const [quest] = await db.select().from(rpgQuests).where(eq(rpgQuests.key, params.questKey)).limit(1); if (!quest) return NextResponse.json({ error: "not_found" }, { status: 404 }); await db.update(rpgCharacters).set({ xp: sql`${rpgCharacters.xp} + ${quest.rewardXp}`, gold: sql`${rpgCharacters.gold} + ${quest.rewardGold}` }).where(eq(rpgCharacters.id, character.id)); for (const itemKey of quest.rewardItemKeys) { await db.insert(rpgInventory).values({ characterId: character.id, itemKey, quantity: 1 }) .onConflictDoUpdate({ target: [rpgInventory.characterId, rpgInventory.itemKey], set: { quantity: sql`${rpgInventory.quantity} + 1` } }); } await db.update(rpgCharacterQuestProgress).set({ status: "completed" }).where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, params.questKey))); return NextResponse.json({ status: "completed" }); }{ key: "rpg_character_level", name: "Realm of Ash — Character Level", kind: "solo_score" as const, scoreDirection: "higher_better" as const },{ eventType: "rpg.character_leveled", badgeKey: "rpg_level_5", condition: (p) => typeof p.level === "number" && p.level >= 5 },{ key: "rpg_level_5", name: "Ash-Marked", description: "Reached character level 5 in Realm of Ash." },"use client"; import { useState } from "react"; import styles from "./combat-screen.module.css"; export function CombatScreen({ sessionId, initialState, abilities, inventory }: { sessionId: string; initialState: any; abilities: { key: string; name: string; mpCost: number; targetType: string }[]; inventory: { itemKey: string; quantity: number }[]; }) { const [state, setState] = useState(initialState); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); async function act(body: unknown) { setBusy(true); setError(null); try { const res = await fetch(`/api/rpg/combat/${sessionId}/action`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await res.json(); if (!res.ok) { setError(data.error ?? "action_failed"); return; } setState(data.state); } finally { setBusy(false); } } const aliveEnemies = state.enemies.filter((e: any) => e.currentHp > 0); const usableAbilities = abilities.filter((a) => a.mpCost <= state.player.currentMp); const healingItems = inventory.filter((i) => i.itemKey === "minor_healing_draught" && i.quantity > 0); return ( <div className={styles.wrapper}> <div className={styles.playerPanel}> <p>HP: {state.player.currentHp}/{state.player.maxHp} — MP: {state.player.currentMp}/{state.player.maxMp}</p> </div> <div className={styles.enemyPanel}> {state.enemies.map((e: any) => ( <div key={e.instanceId} className={e.currentHp <= 0 ? styles.dead : styles.enemy}> <p>{e.name}</p><p>{e.currentHp}/{e.maxHp} HP</p> </div> ))} </div> {state.status === "active" ? ( <div className={styles.actions}> {aliveEnemies.map((e: any) => ( <button key={e.instanceId} disabled={busy} onClick={() => act({ type: "basic_attack", targetInstanceId: e.instanceId })}>Attack {e.name}</button> ))} {usableAbilities.map((a) => ( <button key={a.key} disabled={busy} onClick={() => act(a.targetType === "self" || a.targetType === "all_enemies" ? { type: "ability", abilityKey: a.key } : { type: "ability", abilityKey: a.key, targetInstanceId: aliveEnemies[0]?.instanceId })}> {a.name} ({a.mpCost} MP) </button> ))} {healingItems.length > 0 && <button disabled={busy} onClick={() => act({ type: "use_item", itemKey: "minor_healing_draught" })}>Use Healing Draught</button>} <button disabled={busy} onClick={() => act({ type: "defend" })}>Defend</button> <button disabled={busy} onClick={() => act({ type: "flee" })} className={styles.fleeBtn}>Flee</button> </div> ) : ( <p className={styles.resultBanner}>{state.status === "victory" ? "Victory!" : state.status === "defeat" ? "Defeated." : "You fled."}</p> )} {error && <p className={styles.error}>{error.replaceAll("_", " ")}</p>} <ul className={styles.log}>{state.log.slice(-12).map((l: any, i: number) => <li key={i}>{l.text}</li>)}</ul> </div> ); }.wrapper { color: var(--text-primary); font-family: var(--font-mono); } .playerPanel, .enemyPanel { display: flex; gap: 12px; margin-bottom: 10px; } .enemy { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 6px 10px; } .dead { opacity: 0.35; text-decoration: line-through; } .actions button { margin: 4px; background: var(--surface-2); border: 1px solid var(--border); color: var(--text-primary); border-radius: var(--radius-sm); padding: 6px 12px; cursor: pointer; } .fleeBtn { border-color: var(--danger); color: var(--danger-text); } .resultBanner { color: var(--accent-gold); font-weight: 600; font-size: 1.1rem; } .log { max-height: 180px; overflow-y: auto; font-size: 0.8rem; color: var(--text-secondary); } .error { color: var(--danger-text); }export const rpgAbilities = pgTable("rpg_abilities", { key: text("key").primaryKey(), name: text("name").notNull(), description: text("description").notNull(), classKey: text("class_key").references(() => rpgClasses.key), // CHANGED: nullable — null = enemy-only ability, unreachable by any player action unlockLevel: integer("unlock_level").notNull().default(1), mpCost: integer("mp_cost").notNull(), targetType: rpgTargetTypeEnum("target_type").notNull(), effectType: rpgEffectTypeEnum("effect_type").notNull(), powerMultiplier: doublePrecision("power_multiplier").notNull().default(1), statusKind: text("status_kind"), statusDuration: integer("status_duration"), statusMagnitude: integer("status_magnitude"), });import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCombatSessions, rpgCharacters, rpgAbilities, rpgItems, rpgInventory, rpgCharacterQuestProgress, rpgQuestStages, } from "@/lib/db/schema/rpg"; import { advanceRound, AbilityDef, PlayerAction } from "@/lib/rpg/combat-engine"; import { advanceKillProgress } from "@/lib/rpg/quest-engine"; import { applyXpGain } from "@/lib/rpg/leveling"; import { eq, and, sql } from "drizzle-orm"; import { z } from "zod"; const actionSchema = z.discriminatedUnion("type", [ z.object({ type: z.literal("basic_attack"), targetInstanceId: z.number().int() }), z.object({ type: z.literal("ability"), abilityKey: z.string(), targetInstanceId: z.number().int().optional() }), z.object({ type: z.literal("defend") }), z.object({ type: z.literal("use_item"), itemKey: z.string() }), z.object({ type: z.literal("flee") }), ]); export async function POST(req: NextRequest, { params }: { params: { sessionId: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const parsed = actionSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM rpg_combat_sessions WHERE id = ${params.sessionId} FOR UPDATE`) as unknown as [any]; const session = locked[0]; if (!session) return { httpStatus: 404, body: { error: "not_found" } }; const [character] = await tx.select().from(rpgCharacters).where(eq(rpgCharacters.id, session.character_id)).limit(1); if (!character || character.memberId !== viewer.id) return { httpStatus: 403, body: { error: "forbidden" } }; if (session.status !== "active") return { httpStatus: 409, body: { error: "combat_not_active" } }; const abilityRows = await tx.select().from(rpgAbilities); // THE FIX: server-side ownership + unlock-level check, closing the // "any player can cast any ability including enemy-only ones" gap. if (parsed.data.type === "ability") { const chosen = abilityRows.find((a) => a.key === parsed.data.abilityKey); if (!chosen) return { httpStatus: 400, body: { error: "unknown_ability" } }; if (chosen.classKey !== character.classKey) return { httpStatus: 403, body: { error: "ability_not_available" } }; if (character.level < chosen.unlockLevel) return { httpStatus: 403, body: { error: "ability_not_unlocked" } }; } let playerAction: PlayerAction; if (parsed.data.type === "use_item") { const [inv] = await tx.select().from(rpgInventory).where(and(eq(rpgInventory.characterId, character.id), eq(rpgInventory.itemKey, parsed.data.itemKey))).limit(1); if (!inv || inv.quantity < 1) return { httpStatus: 400, body: { error: "item_not_owned" } }; const [item] = await tx.select().from(rpgItems).where(eq(rpgItems.key, parsed.data.itemKey)).limit(1); if (!item || item.itemType !== "consumable" || !item.healAmount) return { httpStatus: 400, body: { error: "item_not_usable" } }; await tx.update(rpgInventory).set({ quantity: sql`${rpgInventory.quantity} - 1` }).where(and(eq(rpgInventory.characterId, character.id), eq(rpgInventory.itemKey, parsed.data.itemKey))); playerAction = { type: "use_item", healAmount: item.healAmount }; } else { playerAction = parsed.data as PlayerAction; } const abilities: Record<string, AbilityDef> = Object.fromEntries(abilityRows.map((a) => [a.key, { key: a.key, name: a.name, mpCost: a.mpCost, targetType: a.targetType, effectType: a.effectType, powerMultiplier: a.powerMultiplier, statusEffect: a.statusKind ? { kind: a.statusKind as any, duration: a.statusDuration!, magnitude: a.statusMagnitude! } : null, }])); const { state: newState, deadEnemyTemplateKeys } = advanceRound(session.state, playerAction, abilities); await tx.update(rpgCombatSessions).set({ state: newState, status: newState.status, completedAt: newState.status !== "active" ? new Date() : null, }).where(eq(rpgCombatSessions.id, params.sessionId)); return { httpStatus: 200, body: { state: newState }, finalStatus: newState.status, character, session, deadEnemyTemplateKeys, newState }; }); if (outcome.httpStatus !== 200 || !("finalStatus" in outcome)) { return NextResponse.json(outcome.body, { status: outcome.httpStatus }); } const { finalStatus, character, deadEnemyTemplateKeys, newState } = outcome; if (finalStatus === "victory") { const rewardSource = (outcome.session.state as any).enemies as { xpReward: number; goldReward: number }[]; const totalXp = rewardSource.reduce((sum, e) => sum + e.xpReward, 0); const totalGold = rewardSource.reduce((sum, e) => sum + e.goldReward, 0); await applyXpGain(character, totalXp, totalGold); if (deadEnemyTemplateKeys.length > 0) { const activeQuests = await db.select().from(rpgCharacterQuestProgress) .where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.status, "active"))); for (const progress of activeQuests) { const stages = await db.select().from(rpgQuestStages).where(eq(rpgQuestStages.questKey, progress.questKey)); const result = advanceKillProgress(progress.currentStageOrder, progress.stageProgressCount, stages, deadEnemyTemplateKeys); await db.update(rpgCharacterQuestProgress).set({ currentStageOrder: result.newStageOrder, stageProgressCount: result.newProgressCount, status: result.readyToTurnIn ? "ready_to_turn_in" : "active", }).where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, progress.questKey))); } } } else if (finalStatus === "defeat") { const goldLoss = Math.floor(character.gold * 0.1); await db.update(rpgCharacters).set({ currentHp: 1, gold: character.gold - goldLoss }).where(eq(rpgCharacters.id, character.id)); } else { await db.update(rpgCharacters).set({ currentHp: newState.player.currentHp, currentMp: newState.player.currentMp }).where(eq(rpgCharacters.id, character.id)); } return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { db } from "@/lib/db"; import { rpgCharacters } from "@/lib/db/schema/rpg"; import { xpToNextLevel, baseDerivedStats } from "./stats"; import { emitMemberEvent } from "@/lib/roles/events"; import { arcadeScores } from "@/lib/db/schema/arcade-core"; import { recordArcadeActivity } from "@/lib/arcade/streaks"; import { eq } from "drizzle-orm"; type MinimalCharacter = { id: string; memberId: string; level: number; xp: number; unspentStatPoints: number; gold: number; str: number; dex: number; int: number; vit: number; wis: number; currentHp: number; currentMp: number; }; // Requires a normalized, camelCase-field object explicitly — NOT a raw // tx.execute() row. Caught while writing this: this codebase has two // different row shapes floating around (Drizzle's camelCase selects vs. // raw sql`` execute's snake_case rows), and a function that silently // accepted either would eventually read `character.member_id` as // `undefined` on a Drizzle-shaped input with no error — a silent failure // path the standing constraints forbid. Forcing an explicit shape here // makes that mismatch a type error instead of a runtime no-op. export async function applyXpGain(character: MinimalCharacter, xpGained: number, goldGained = 0): Promise<{ leveledUp: boolean; newLevel: number }> { let level = character.level; let xp = character.xp + xpGained; let unspent = character.unspentStatPoints; let leveledUp = false; while (xp >= xpToNextLevel(level)) { xp -= xpToNextLevel(level); level += 1; unspent += 3; leveledUp = true; } const derived = baseDerivedStats({ str: character.str, dex: character.dex, int: character.int, vit: character.vit, wis: character.wis, level }); await db.update(rpgCharacters).set({ level, xp, unspentStatPoints: unspent, gold: character.gold + goldGained, currentHp: leveledUp ? derived.maxHp : character.currentHp, currentMp: leveledUp ? derived.maxMp : character.currentMp, }).where(eq(rpgCharacters.id, character.id)); if (leveledUp) { await emitMemberEvent(character.memberId, "rpg.character_leveled", { level }); await db.insert(arcadeScores).values({ memberId: character.memberId, gameKey: "rpg_character_level", score: level }); await recordArcadeActivity(character.memberId); } return { leveledUp, newLevel: level }; }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { rpgCharacters, rpgQuests, rpgCharacterQuestProgress, rpgInventory } from "@/lib/db/schema/rpg"; import { applyXpGain } from "@/lib/rpg/leveling"; import { eq, and, sql } from "drizzle-orm"; export async function POST(_req: NextRequest, { params }: { params: { questKey: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const [character] = await db.select().from(rpgCharacters).where(eq(rpgCharacters.memberId, viewer.id)).limit(1); if (!character) return NextResponse.json({ error: "no_character" }, { status: 404 }); const [progress] = await db.select().from(rpgCharacterQuestProgress) .where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, params.questKey))).limit(1); if (!progress || progress.status !== "ready_to_turn_in") return NextResponse.json({ error: "not_ready" }, { status: 409 }); const [quest] = await db.select().from(rpgQuests).where(eq(rpgQuests.key, params.questKey)).limit(1); if (!quest) return NextResponse.json({ error: "not_found" }, { status: 404 }); await applyXpGain(character, quest.rewardXp, quest.rewardGold); for (const itemKey of quest.rewardItemKeys) { await db.insert(rpgInventory).values({ characterId: character.id, itemKey, quantity: 1 }) .onConflictDoUpdate({ target: [rpgInventory.characterId, rpgInventory.itemKey], set: { quantity: sql`${rpgInventory.quantity} + 1` } }); } await db.update(rpgCharacterQuestProgress).set({ status: "completed" }).where(and(eq(rpgCharacterQuestProgress.characterId, character.id), eq(rpgCharacterQuestProgress.questKey, params.questKey))); return NextResponse.json({ status: "completed" }); }import { NextRequest, NextResponse } from "next/server"; import { db } from "@/lib/db"; import { workshopProjects, workshopVolunteers } from "@/lib/db/schema/workshop"; import { requireHouseAccess, AccessDeniedError } from "@/lib/auth/roles"; import { desc, eq, sql } from "drizzle-orm"; import { z } from "zod"; const createSchema = z.object({ title: z.string().trim().min(1).max(200), missionStatement: z.string().trim().min(1).max(500), description: z.string().trim().min(1).max(3000), }); export async function GET() { const rows = await db.select({ id: workshopProjects.id, title: workshopProjects.title, missionStatement: workshopProjects.missionStatement, status: workshopProjects.status, progressPercent: workshopProjects.progressPercent, coverPhotoUrls: workshopProjects.coverPhotoUrls, createdAt: workshopProjects.createdAt, volunteerCount: sql<number>`( SELECT count(*)::int FROM workshop_volunteers WHERE project_id = ${workshopProjects.id} AND status = 'active' )`, }).from(workshopProjects).orderBy(desc(workshopProjects.createdAt)); return NextResponse.json({ projects: rows }); } export async function POST(req: NextRequest) { let viewer; try { viewer = await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: err.reason === "unauthenticated" ? 401 : 403 }); throw err; } const parsed = createSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const [project] = await db.insert(workshopProjects).values({ ...parsed.data, createdBy: viewer.id }).returning(); await db.insert(workshopVolunteers).values({ projectId: project.id, memberId: viewer.id, role: "Lead" }); return NextResponse.json({ project }, { status: 201 }); }import { pgTable, uuid, text, timestamp, pgEnum, index } from "drizzle-orm/pg-core"; import { blockMembers } from "./block-members"; export const notificationTypeEnum = pgEnum("notification_type", [ "reply", "mention", "badge_awarded", "quest_ready", "mission_signup_filled", "workshop_update", "system", ]); export const notifications = pgTable("notifications", { id: uuid("id").defaultRandom().primaryKey(), memberId: uuid("member_id").notNull().references(() => blockMembers.id, { onDelete: "cascade" }), type: notificationTypeEnum("type").notNull(), title: text("title").notNull(), body: text("body").notNull(), linkUrl: text("link_url"), readAt: timestamp("read_at", { withTimezone: true }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), }, (t) => ({ memberIdx: index("notifications_member_idx").on(t.memberId, t.readAt, t.createdAt) })); // One row per member — "online now" is derived from freshness of lastSeenAt, // not a boolean flag, which would require a disconnect event we have no // reliable way to detect (browser close, network drop, etc. don't fire one). export const memberPresence = pgTable("member_presence", { memberId: uuid("member_id").primaryKey().references(() => blockMembers.id, { onDelete: "cascade" }), lastSeenAt: timestamp("last_seen_at", { withTimezone: true }).notNull().defaultNow(), });import { db } from "@/lib/db"; import { notifications, notificationTypeEnum } from "@/lib/db/schema/notifications"; type NotificationType = (typeof notificationTypeEnum.enumValues)[number]; type DbOrTx = typeof db; export async function notifyMember( params: { memberId: string; type: NotificationType; title: string; body: string; linkUrl?: string }, executor: DbOrTx = db // accepts a `tx` for atomic composition with the triggering write, same convention as emitMemberEvent ): Promise<void> { await executor.insert(notifications).values(params); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { notifications } from "@/lib/db/schema/notifications"; import { eq, desc, and, isNull, sql } from "drizzle-orm"; export async function GET() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const rows = await db.select().from(notifications).where(eq(notifications.memberId, viewer.id)).orderBy(desc(notifications.createdAt)).limit(30); const [{ unreadCount }] = await db.select({ unreadCount: sql<number>`count(*)::int` }).from(notifications) .where(and(eq(notifications.memberId, viewer.id), isNull(notifications.readAt))); return NextResponse.json({ notifications: rows, unreadCount }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { notifications } from "@/lib/db/schema/notifications"; import { eq, and, isNull } from "drizzle-orm"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } await db.update(notifications).set({ readAt: new Date() }).where(and(eq(notifications.memberId, viewer.id), isNull(notifications.readAt))); return NextResponse.json({ status: "marked_read" }); }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { memberPresence } from "@/lib/db/schema/notifications"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } await db.insert(memberPresence).values({ memberId: viewer.id, lastSeenAt: new Date() }) .onConflictDoUpdate({ target: memberPresence.memberId, set: { lastSeenAt: new Date() } }); return NextResponse.json({ status: "ok" }); }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { memberPresence } from "@/lib/db/schema/notifications"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } await db.insert(memberPresence).values({ memberId: viewer.id, lastSeenAt: new Date() }) .onConflictDoUpdate({ target: memberPresence.memberId, set: { lastSeenAt: new Date() } }); return NextResponse.json({ status: "ok" }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { notifications } from "@/lib/db/schema/notifications"; import { eq, and, isNull } from "drizzle-orm"; export async function POST() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } await db.update(notifications).set({ readAt: new Date() }).where(and(eq(notifications.memberId, viewer.id), isNull(notifications.readAt))); return NextResponse.json({ status: "marked_read" }); }import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { memberPresence } from "@/lib/db/schema/notifications"; import { gte, sql } from "drizzle-orm"; const ONLINE_WINDOW_SECONDS = 120; // no heartbeat in 2 minutes = considered offline export async function GET() { const cutoff = new Date(Date.now() - ONLINE_WINDOW_SECONDS * 1000); const [{ count }] = await db.select({ count: sql<number>`count(*)::int` }).from(memberPresence).where(gte(memberPresence.lastSeenAt, cutoff)); return NextResponse.json({ onlineCount: count }); }"use client"; import { useEffect, useState, useCallback, useRef } from "react"; /** * THE standard real-time mechanism for this site — see the architecture * decision documented in Section 16. Every "live" feature (match sync, * lobby sync, notification badge, online count) should use this, not a * bespoke setInterval. `shouldStop` lets a caller freeze polling once a * game/lobby reaches a terminal state, avoiding wasted requests. */ export function usePolling<T>( fetcher: () => Promise<T>, intervalMs: number, shouldStop: (data: T | null) => boolean = () => false ) { const [data, setData] = useState<T | null>(null); const [error, setError] = useState<string | null>(null); const dataRef = useRef<T | null>(null); const fetchOnce = useCallback(async () => { try { const result = await fetcher(); dataRef.current = result; setData(result); setError(null); } catch { setError("Sync failed."); } }, [fetcher]); useEffect(() => { fetchOnce(); const interval = setInterval(() => { if (!shouldStop(dataRef.current)) fetchOnce(); }, intervalMs); return () => clearInterval(interval); }, [fetchOnce, intervalMs, shouldStop]); return { data, error, refetch: fetchOnce }; }"use client"; import { usePolling } from "./use-polling"; export function useMatchPolling<TState>(matchId: string, intervalMs = 1500) { const fetcher = async () => { const res = await fetch(`/api/arcade/matches/${matchId}`); if (!res.ok) throw new Error(`status ${res.status}`); const data = await res.json(); return data.match as { id: string; state: TState; status: string; winnerId: string | null }; }; const { data, error, refetch } = usePolling(fetcher, intervalMs, (match) => match?.status === "completed"); return { match: data, error, refetch }; }"use client"; import { useState, useCallback } from "react"; import { usePolling } from "@/hooks/use-polling"; import styles from "./notification-bell.module.css"; type Notification = { id: string; type: string; title: string; body: string; linkUrl: string | null; readAt: string | null; createdAt: string }; export function NotificationBell() { const [open, setOpen] = useState(false); const fetcher = useCallback(async () => { const res = await fetch("/api/notifications"); if (!res.ok) throw new Error(); return res.json() as Promise<{ notifications: Notification[]; unreadCount: number }>; }, []); const { data } = usePolling(fetcher, 20_000); // 20s — notifications don't need faster than that async function toggleOpen() { const nowOpen = !open; setOpen(nowOpen); if (nowOpen && data && data.unreadCount > 0) { await fetch("/api/notifications/mark-read", { method: "POST" }); } } return ( <div className={styles.wrapper}> <button className={styles.bellBtn} onClick={toggleOpen} aria-label="Notifications"> 🔔 {data && data.unreadCount > 0 && <span className={styles.badge}>{data.unreadCount}</span>} </button> {open && ( <div className={styles.dropdown}> {!data || data.notifications.length === 0 ? ( <p className={styles.empty}>No notifications yet.</p> ) : ( data.notifications.map((n) => ( <a key={n.id} href={n.linkUrl ?? "#"} className={styles.item}> <strong>{n.title}</strong> <p>{n.body}</p> </a> )) )} </div> )} </div> ); }.wrapper { position: relative; } .bellBtn { background: none; border: none; color: var(--text-primary); font-size: 1.2rem; cursor: pointer; position: relative; } .badge { position: absolute; top: -4px; right: -8px; background: var(--danger); color: white; border-radius: 999px; font-size: 0.65rem; padding: 1px 5px; font-family: var(--font-mono); } .dropdown { position: absolute; right: 0; top: 32px; width: 280px; background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); max-height: 320px; overflow-y: auto; z-index: 50; } .item { display: block; padding: 10px 14px; border-bottom: 1px solid var(--border); color: var(--text-primary); text-decoration: none; } .item p { color: var(--text-secondary); font-size: 0.8rem; margin: 2px 0 0; } .empty { padding: 14px; color: var(--text-secondary); font-style: italic; }"use client"; import { useEffect, useCallback } from "react"; import { usePolling } from "@/hooks/use-polling"; export function OnlineCountBadge() { const heartbeat = useCallback(() => { fetch("/api/presence/heartbeat", { method: "POST" }).catch(() => {}); }, []); useEffect(() => { heartbeat(); const interval = setInterval(heartbeat, 60_000); // heartbeat every 60s — matches the 120s online-window with margin return () => clearInterval(interval); }, [heartbeat]); const fetcher = useCallback(async () => { const res = await fetch("/api/presence/online-count"); if (!res.ok) throw new Error(); return (await res.json()).onlineCount as number; }, []); const { data } = usePolling(fetcher, 30_000); return <span>{data ?? "…"} online now</span>; }import { notifyMember } from "@/lib/notifications/notify"; // ...inside POST, after the reply is created and post author is fetched: const [postRow] = await db.select({ authorId: blockPosts.authorId, title: blockPosts.title }).from(blockPosts).where(eq(blockPosts.id, params.id)).limit(1); if (postRow && postRow.authorId !== viewer.id) { // don't notify yourself for your own reply await notifyMember({ memberId: postRow.authorId, type: "reply", title: "New reply to your post", body: `Someone replied to "${postRow.title}"`, linkUrl: `/block/posts/${params.id}`, }); }// After the transaction succeeds, outside the lock: const finalCount = count + 1; if (finalCount === need.slots_needed) { const [needRow] = await db.select({ createdBy: missionBoardNeeds.createdBy, title: missionBoardNeeds.title }).from(missionBoardNeeds).where(eq(missionBoardNeeds.id, needId)).limit(1); if (needRow) { await notifyMember({ memberId: needRow.createdBy, type: "mission_signup_filled", title: "A need is fully staffed", body: `"${needRow.title}" has reached its volunteer goal.`, linkUrl: `/mission-board/${needId}`, }); } }export const ALLOWED_SECTION_KEYS = ["general", "prayer", "wins", "questions", "announcements", "house"] as const;// Add to imports: import { getViewerTier, tierMeetsRequirement } from "@/lib/auth/roles"; // Inside GET, after parsing query params, before building `conditions`: if (sectionKey === "house") { const tier = await getViewerTier(); if (!tierMeetsRequirement(tier, "house")) { return NextResponse.json({ error: "insufficient_tier" }, { status: 403 }); } } // Inside POST, after requireBlockAccess() succeeds, before validation: if (parsed.data.sectionKey === "house") { const tier = await getMemberTier(viewer.id); // getMemberTier already imported via roles.ts elsewhere in this file's dependency chain — import explicitly if not already present if (!tierMeetsRequirement(tier, "house")) { return NextResponse.json({ error: "insufficient_tier" }, { status: 403 }); } }import { NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { getMemberTier } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { memberBadges, badges } from "@/lib/db/schema/member-roles"; import { eq, desc } from "drizzle-orm"; export async function GET() { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const tier = await getMemberTier(viewer.id); const earnedBadges = await db.select({ key: badges.key, name: badges.name, description: badges.description, awardedAt: memberBadges.awardedAt }) .from(memberBadges).innerJoin(badges, eq(memberBadges.badgeId, badges.id)) .where(eq(memberBadges.memberId, viewer.id)).orderBy(desc(memberBadges.awardedAt)); return NextResponse.json({ tier, badges: earnedBadges }); }import { requireHouseAccess } from "@/lib/auth/roles"; import { redirect } from "next/navigation"; import { HouseDashboard } from "@/components/house/house-dashboard"; import { AccessDeniedError } from "@/lib/auth/roles"; export default async function HousePage() { try { await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) redirect(err.reason === "unauthenticated" ? "/street" : "/"); // ASSUMPTION: adjust redirect targets to your real routes for "please sign in" and "you're Block but not House yet" throw err; } return <HouseDashboard />; }"use client"; import { useEffect, useState } from "react"; import styles from "./house-dashboard.module.css"; type ViewState<T> = { status: "loading" } | { status: "ready"; data: T } | { status: "empty" } | { status: "error" }; function useSection<T>(url: string, isEmpty: (d: T) => boolean) { const [state, setState] = useState<ViewState<T>>({ status: "loading" }); useEffect(() => { let cancelled = false; fetch(url).then((r) => { if (!r.ok) throw new Error(); return r.json(); }) .then((d) => { if (!cancelled) setState(isEmpty(d) ? { status: "empty" } : { status: "ready", data: d }); }) .catch(() => { if (!cancelled) setState({ status: "error" }); }); return () => { cancelled = true; }; }, [url]); return state; } export function HouseDashboard() { const roles = useSection<{ tier: string; badges: any[] }>("/api/roles/me", () => false); const teachings = useSection<{ teachings: any[] }>("/api/vault/teachings?limit=4", (d) => d.teachings.length === 0); const projects = useSection<{ projects: any[] }>("/api/workshop/projects", (d) => d.projects.length === 0); const discussion = useSection<{ posts: any[] }>("/api/block/posts?sectionKey=house&limit=10", (d) => d.posts.length === 0); return ( <main className={styles.page}> <section className={styles.badgeShelf}> <h2>Your standing</h2> {roles.status === "loading" && <p>Loading…</p>} {roles.status === "ready" && ( <> <p className={styles.tier}>Tier: <strong>{roles.data.tier}</strong></p> <div className={styles.badges}> {roles.data.badges.length === 0 ? <p className={styles.mutedNote}>No badges yet — they're earned, not given.</p> : roles.data.badges.map((b: any) => <span key={b.key} className={styles.badgePill} title={b.description}>{b.name}</span>)} </div> </> )} </section> <section> <h2>Teaching Archive</h2> {teachings.status === "loading" && <p>Loading…</p>} {teachings.status === "error" && <p>Couldn't load teachings.</p>} {teachings.status === "empty" && <p className={styles.mutedNote}>No teachings published yet.</p>} {teachings.status === "ready" && ( <ul className={styles.list}> {teachings.data.teachings.map((t: any) => <li key={t.id}><strong>{t.title}</strong><p>{t.description}</p></li>)} </ul> )} </section> <section> <h2>Workshop Projects</h2> {projects.status === "loading" && <p>Loading…</p>} {projects.status === "error" && <p>Couldn't load projects.</p>} {projects.status === "empty" && <p className={styles.mutedNote}>No active projects yet.</p>} {projects.status === "ready" && ( <ul className={styles.list}> {projects.data.projects.map((p: any) => ( <li key={p.id}> <strong>{p.title}</strong> — {p.volunteerCount} volunteer{p.volunteerCount === 1 ? "" : "s"} <div className={styles.progressTrack}><div className={styles.progressFill} style={{ width: `${p.progressPercent}%` }} /></div> </li> ))} </ul> )} </section> <section> <h2>House Discussion</h2> {discussion.status === "loading" && <p>Loading…</p>} {discussion.status === "error" && <p>Couldn't load discussion.</p>} {discussion.status === "empty" && <p className={styles.mutedNote}>No House discussion yet — start one.</p>} {discussion.status === "ready" && ( <ul className={styles.list}> {discussion.data.posts.map((p: any) => <li key={p.id}><strong>{p.title}</strong><p>{p.body.slice(0, 160)}</p></li>)} </ul> )} </section> </main> ); }.page { display: flex; flex-direction: column; gap: 32px; color: var(--text-primary); max-width: 800px; margin: 0 auto; padding: 24px; } .badgeShelf { border: 1px solid var(--border); background: var(--surface-1); border-radius: var(--radius-md); padding: 16px; } .tier { font-family: var(--font-mono); color: var(--accent-gold); } .badges { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px; } .badgePill { background: var(--surface-2); border: 1px solid var(--accent-gold); color: var(--accent-gold); border-radius: 999px; padding: 3px 10px; font-size: 0.75rem; } .mutedNote { color: var(--text-secondary); font-style: italic; } .list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 10px; } .list li { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 10px 14px; } .progressTrack { height: 5px; background: var(--surface-2); border-radius: 3px; margin-top: 6px; overflow: hidden; } .progressFill { height: 100%; background: var(--accent-gold); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { workshopUpdates, workshopVolunteers } from "@/lib/db/schema/workshop"; import { and, asc, desc, eq, isNull } from "drizzle-orm"; import { z } from "zod"; const createSchema = z.object({ body: z.string().trim().min(1).max(3000), photoUrls: z.array(z.string().url()).max(6).default([]) }); export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { const rows = await db.select().from(workshopUpdates) .where(and(eq(workshopUpdates.projectId, params.id), isNull(workshopUpdates.deletedAt))) .orderBy(desc(workshopUpdates.createdAt)); return NextResponse.json({ updates: rows }); } export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } // Only active volunteers on THIS project can post progress updates — // otherwise anyone could post fake "official" progress logs on any project. const [membership] = await db.select().from(workshopVolunteers) .where(and(eq(workshopVolunteers.projectId, params.id), eq(workshopVolunteers.memberId, viewer.id), eq(workshopVolunteers.status, "active"))) .limit(1); if (!membership) return NextResponse.json({ error: "not_a_volunteer" }, { status: 403 }); const parsed = createSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); const [update] = await db.insert(workshopUpdates).values({ projectId: params.id, authorId: viewer.id, ...parsed.data }).returning(); return NextResponse.json({ update }, { status: 201 }); }export const moderationFlags = pgTable("moderation_flags", { id: uuid("id").defaultRandom().primaryKey(), contentType: text("content_type").notNull(), contentId: uuid("content_id").notNull(), reason: text("reason").notNull(), reportedBy: uuid("reported_by").references(() => blockMembers.id), status: moderationFlagStatusEnum("status").notNull().default("pending"), reviewerId: uuid("reviewer_id").references(() => blockMembers.id), reviewedAt: timestamp("reviewed_at", { withTimezone: true }), resolutionNote: text("resolution_note"), // ADDED — required whenever a flag is resolved, see route below createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), });import { db } from "@/lib/db"; import { blockPosts, blockPostReplies } from "@/lib/db/schema/block-posts"; import { workshopUpdates, workshopDiscussionComments } from "@/lib/db/schema/workshop"; import { eq } from "drizzle-orm"; import { FlaggableContentType } from "./flag"; export type ContentPreview = { authorId: string; excerpt: string; createdAt: Date } | null; type ContentHandler = { fetchPreview(contentId: string): Promise<ContentPreview>; remove(contentId: string): Promise<void>; }; // Adding a new flaggable content type (Section 8, 11 uploads, etc.) means // adding one entry here — nothing else in the moderation system needs to // change. This is the concrete mechanism behind Section 15's instruction // that features "call a shared hook rather than each inventing their own // moderation logic." export const CONTENT_REGISTRY: Record<FlaggableContentType, ContentHandler> = { block_post: { async fetchPreview(id) { const [row] = await db.select({ authorId: blockPosts.authorId, title: blockPosts.title, body: blockPosts.body, createdAt: blockPosts.createdAt }) .from(blockPosts).where(eq(blockPosts.id, id)).limit(1); return row ? { authorId: row.authorId, excerpt: `${row.title}: ${row.body.slice(0, 200)}`, createdAt: row.createdAt } : null; }, async remove(id) { await db.update(blockPosts).set({ status: "removed" }).where(eq(blockPosts.id, id)); }, }, block_reply: { async fetchPreview(id) { const [row] = await db.select({ authorId: blockPostReplies.authorId, body: blockPostReplies.body, createdAt: blockPostReplies.createdAt }) .from(blockPostReplies).where(eq(blockPostReplies.id, id)).limit(1); return row ? { authorId: row.authorId, excerpt: row.body.slice(0, 200), createdAt: row.createdAt } : null; }, async remove(id) { await db.update(blockPostReplies).set({ deletedAt: new Date() }).where(eq(blockPostReplies.id, id)); }, }, workshop_update: { async fetchPreview(id) { const [row] = await db.select({ authorId: workshopUpdates.authorId, body: workshopUpdates.body, createdAt: workshopUpdates.createdAt }) .from(workshopUpdates).where(eq(workshopUpdates.id, id)).limit(1); return row ? { authorId: row.authorId, excerpt: row.body.slice(0, 200), createdAt: row.createdAt } : null; }, async remove(id) { await db.update(workshopUpdates).set({ deletedAt: new Date() }).where(eq(workshopUpdates.id, id)); }, }, workshop_comment: { async fetchPreview(id) { const [row] = await db.select({ authorId: workshopDiscussionComments.authorId, body: workshopDiscussionComments.body, createdAt: workshopDiscussionComments.createdAt }) .from(workshopDiscussionComments).where(eq(workshopDiscussionComments.id, id)).limit(1); return row ? { authorId: row.authorId, excerpt: row.body.slice(0, 200), createdAt: row.createdAt } : null; }, async remove(id) { await db.update(workshopDiscussionComments).set({ deletedAt: new Date() }).where(eq(workshopDiscussionComments.id, id)); }, }, };import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { moderationFlags } from "@/lib/db/schema/moderation"; import { CONTENT_REGISTRY } from "@/lib/moderation/content-registry"; import { eq, desc } from "drizzle-orm"; import { z } from "zod"; const querySchema = z.object({ status: z.enum(["pending", "reviewing", "actioned", "dismissed"]).default("pending") }); export async function GET(req: NextRequest) { try { await requireSiteRole("moderator"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = querySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) return NextResponse.json({ error: "invalid_query" }, { status: 400 }); const flags = await db.select().from(moderationFlags).where(eq(moderationFlags.status, parsed.data.status)).orderBy(desc(moderationFlags.createdAt)).limit(50); // Preview fetch happens per-row via the registry — a flag whose underlying // content was already deleted by some other path returns null cleanly // instead of throwing, so a stale flag doesn't break the whole queue view. const withPreviews = await Promise.all(flags.map(async (f) => ({ ...f, preview: await CONTENT_REGISTRY[f.contentType as keyof typeof CONTENT_REGISTRY]?.fetchPreview(f.contentId).catch(() => null) ?? null, }))); return NextResponse.json({ flags: withPreviews }); }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { moderationFlags } from "@/lib/db/schema/moderation"; import { CONTENT_REGISTRY } from "@/lib/moderation/content-registry"; import { notifyMember } from "@/lib/notifications/notify"; import { eq, sql } from "drizzle-orm"; import { z } from "zod"; const schema = z.object({ action: z.enum(["remove", "warn", "dismiss"]), resolutionNote: z.string().trim().min(1).max(500), }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireSiteRole("moderator"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const outcome = await db.transaction(async (tx) => { const locked = await tx.execute(sql`SELECT * FROM moderation_flags WHERE id = ${params.id} FOR UPDATE`) as unknown as [any]; const flag = locked[0]; if (!flag) return { httpStatus: 404, body: { error: "not_found" } }; if (flag.status === "actioned" || flag.status === "dismissed") return { httpStatus: 409, body: { error: "already_resolved" } }; const handler = CONTENT_REGISTRY[flag.content_type as keyof typeof CONTENT_REGISTRY]; let contentAuthorId: string | null = null; if (parsed.data.action === "remove" || parsed.data.action === "warn") { const preview = handler ? await handler.fetchPreview(flag.content_id) : null; contentAuthorId = preview?.authorId ?? null; if (parsed.data.action === "remove" && handler) await handler.remove(flag.content_id); } await tx.update(moderationFlags).set({ status: parsed.data.action === "dismiss" ? "dismissed" : "actioned", reviewerId: viewer.id, reviewedAt: new Date(), resolutionNote: parsed.data.resolutionNote, }).where(eq(moderationFlags.id, params.id)); return { httpStatus: 200, body: { status: "resolved" }, contentAuthorId, action: parsed.data.action, note: parsed.data.resolutionNote }; }); // Notification sent AFTER the transaction commits — a failed notification // send must never roll back a real moderation decision. Consistent with // treating the DB write as the source of truth, side effects as best-effort. if (outcome.httpStatus === 200 && "contentAuthorId" in outcome && outcome.contentAuthorId) { const title = outcome.action === "remove" ? "Content removed" : "A moderation note on your content"; await notifyMember({ memberId: outcome.contentAuthorId, type: "system", title, body: outcome.action === "remove" ? `Something you posted was removed by a moderator. Reason: ${outcome.note}` : `A moderator reviewed something you posted. Note: ${outcome.note}`, }).catch((err) => console.error("[moderation/action] notification send failed, decision still recorded", err)); } return NextResponse.json(outcome.body, { status: outcome.httpStatus }); }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { blockMembers } from "@/lib/db/schema/block-members"; import { memberRoles } from "@/lib/db/schema/member-roles"; import { eq, ilike, sql } from "drizzle-orm"; import { z } from "zod"; const querySchema = z.object({ search: z.string().trim().min(1).max(100).optional(), limit: z.coerce.number().int().min(1).max(50).default(20) }); export async function GET(req: NextRequest) { try { await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = querySchema.safeParse(Object.fromEntries(req.nextUrl.searchParams)); if (!parsed.success) return NextResponse.json({ error: "invalid_query" }, { status: 400 }); // ASSUMPTION FLAGGED (same as Section 3): blockMembers.displayName is a // guessed column name — adjust to your real schema before running this. const query = db.select({ id: blockMembers.id, displayName: blockMembers.displayName, tier: sql<string>`COALESCE(${memberRoles.tier}, 'block')`, siteRole: sql<string>`COALESCE(${memberRoles.siteRole}, 'member')`, }).from(blockMembers).leftJoin(memberRoles, eq(blockMembers.id, memberRoles.memberId)); const rows = parsed.data.search ? await query.where(ilike(blockMembers.displayName, `%${parsed.data.search}%`)).limit(parsed.data.limit) : await query.limit(parsed.data.limit); return NextResponse.json({ members: rows }); }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { memberRoles } from "@/lib/db/schema/member-roles"; import { notifyMember } from "@/lib/notifications/notify"; import { z } from "zod"; const schema = z.object({ tier: z.enum(["street", "block", "house"]) }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); await db.insert(memberRoles).values({ memberId: params.id, tier: parsed.data.tier, houseTrustedAt: parsed.data.tier === "house" ? new Date() : null, houseTrustedBy: parsed.data.tier === "house" ? viewer.id : null }) .onConflictDoUpdate({ target: memberRoles.memberId, set: { tier: parsed.data.tier, houseTrustedAt: parsed.data.tier === "house" ? new Date() : null, houseTrustedBy: parsed.data.tier === "house" ? viewer.id : null, updatedAt: new Date() }, }); if (parsed.data.tier === "house") { await notifyMember({ memberId: params.id, type: "system", title: "Welcome to The House", body: "You've been granted House-tier access." }).catch(() => {}); } return NextResponse.json({ status: "updated" }); }import { NextRequest, NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { memberRoles } from "@/lib/db/schema/member-roles"; import { z } from "zod"; const schema = z.object({ siteRole: z.enum(["member", "moderator", "admin"]) }); export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } // Named, real safeguard: an admin cannot demote themselves through this // endpoint. Without this, a single-admin deployment could accidentally // lock itself out of the admin dashboard with no recovery path short of // a direct DB edit. if (params.id === viewer.id) return NextResponse.json({ error: "cannot_modify_own_role" }, { status: 403 }); const parsed = schema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body" }, { status: 400 }); await db.insert(memberRoles).values({ memberId: params.id, siteRole: parsed.data.siteRole }) .onConflictDoUpdate({ target: memberRoles.memberId, set: { siteRole: parsed.data.siteRole, updatedAt: new Date() } }); return NextResponse.json({ status: "updated" }); }import { NextResponse } from "next/server"; import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { blockMembers } from "@/lib/db/schema/block-members"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { moderationFlags } from "@/lib/db/schema/moderation"; import { workshopProjects } from "@/lib/db/schema/workshop"; import { missionBoardNeeds } from "@/lib/db/schema/mission-board"; import { rpgCharacters } from "@/lib/db/schema/rpg"; import { eq, gte, sql } from "drizzle-orm"; export async function GET() { try { await requireSiteRole("admin"); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000); const [ [{ totalMembers }], [{ postsLast7Days }], [{ pendingFlags }], [{ activeProjects }], [{ openNeeds }], [{ rpgCharacterCount }], ] = await Promise.all([ db.select({ totalMembers: sql<number>`count(*)::int` }).from(blockMembers), db.select({ postsLast7Days: sql<number>`count(*)::int` }).from(blockPosts).where(gte(blockPosts.createdAt, sevenDaysAgo)), db.select({ pendingFlags: sql<number>`count(*)::int` }).from(moderationFlags).where(eq(moderationFlags.status, "pending")), db.select({ activeProjects: sql<number>`count(*)::int` }).from(workshopProjects).where(eq(workshopProjects.status, "active")), db.select({ openNeeds: sql<number>`count(*)::int` }).from(missionBoardNeeds).where(eq(missionBoardNeeds.status, "open")), db.select({ rpgCharacterCount: sql<number>`count(*)::int` }).from(rpgCharacters), ]); return NextResponse.json({ totalMembers, postsLast7Days, pendingFlags, activeProjects, openNeeds, rpgCharacterCount, // Named honestly rather than faked: Section 12 (Marketplace) is not built. marketplaceOrders: null, marketplaceNote: "Marketplace not yet implemented — no orders to report.", }); }import { requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { redirect } from "next/navigation"; import { AdminDashboard } from "@/components/admin/admin-dashboard"; export default async function AdminPage() { try { await requireSiteRole("moderator"); } // page itself needs at least moderator; admin-only sub-actions are enforced per-route catch (err) { if (err instanceof AccessDeniedError) redirect("/"); // ASSUMPTION: adjust to your real "not authorized" destination throw err; } return <AdminDashboard />; }"use client"; import { useState } from "react"; import { ModerationQueue } from "./moderation-queue"; import { MemberManagement } from "./member-management"; import { StatsPanel } from "./stats-panel"; import styles from "./admin-dashboard.module.css"; const TABS = ["Moderation", "Members", "Stats"] as const; export function AdminDashboard() { const [tab, setTab] = useState<(typeof TABS)[number]>("Moderation"); return ( <main className={styles.page}> <nav className={styles.tabs}> {TABS.map((t) => ( <button key={t} className={t === tab ? styles.activeTab : styles.tab} onClick={() => setTab(t)}>{t}</button> ))} </nav> {tab === "Moderation" && <ModerationQueue />} {tab === "Members" && <MemberManagement />} {tab === "Stats" && <StatsPanel />} </main> ); }"use client"; import { useEffect, useState } from "react"; import styles from "./moderation-queue.module.css"; type Flag = { id: string; contentType: string; reason: string; createdAt: string; preview: { authorId: string; excerpt: string } | null }; type ViewState = { status: "loading" } | { status: "ready"; flags: Flag[] } | { status: "empty" } | { status: "error" }; export function ModerationQueue() { const [state, setState] = useState<ViewState>({ status: "loading" }); const [noteDrafts, setNoteDrafts] = useState<Record<string, string>>({}); async function load() { setState({ status: "loading" }); try { const res = await fetch("/api/admin/moderation/flags?status=pending"); if (!res.ok) throw new Error(); const data = await res.json(); setState(data.flags.length === 0 ? { status: "empty" } : { status: "ready", flags: data.flags }); } catch { setState({ status: "error" }); } } useEffect(() => { load(); }, []); async function resolve(id: string, action: "remove" | "warn" | "dismiss") { const resolutionNote = noteDrafts[id]?.trim(); if (!resolutionNote) { alert("A resolution note is required — this is the audit trail."); return; } const res = await fetch(`/api/admin/moderation/flags/${id}/action`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action, resolutionNote }), }); if (res.ok) load(); else alert("Action failed — the flag may have already been resolved by another moderator."); } if (state.status === "loading") return <p className={styles.status}>Loading queue…</p>; if (state.status === "error") return <p className={styles.status}>Couldn't load the moderation queue.</p>; if (state.status === "empty") return <p className={styles.status}>Queue is clear — nothing pending.</p>; return ( <div className={styles.queue}> {state.flags.map((f) => ( <div key={f.id} className={styles.card}> <p className={styles.meta}>{f.contentType} — reported for: {f.reason}</p> <p className={styles.excerpt}>{f.preview ? f.preview.excerpt : "(original content no longer exists)"}</p> <textarea className={styles.noteInput} placeholder="Resolution note (required, sent to the author if actioned)" value={noteDrafts[f.id] ?? ""} onChange={(e) => setNoteDrafts((d) => ({ ...d, [f.id]: e.target.value }))} /> <div className={styles.actions}> <button onClick={() => resolve(f.id, "remove")} className={styles.removeBtn}>Remove content</button> <button onClick={() => resolve(f.id, "warn")} className={styles.warnBtn}>Warn (keep content)</button> <button onClick={() => resolve(f.id, "dismiss")} className={styles.dismissBtn}>Dismiss (no action)</button> </div> </div> ))} </div> ); }.queue { display: flex; flex-direction: column; gap: 14px; } .card { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); padding: 14px; } .meta { font-family: var(--font-mono); font-size: 0.75rem; color: var(--accent-silver); } .excerpt { color: var(--text-primary); margin: 8px 0; } .noteInput { width: 100%; min-height: 50px; background: var(--surface-2); border: 1px solid var(--border); color: var(--text-primary); border-radius: var(--radius-sm); padding: 6px; } .actions { display: flex; gap: 8px; margin-top: 8px; } .removeBtn { background: var(--danger); color: white; border: none; border-radius: var(--radius-sm); padding: 6px 12px; cursor: pointer; } .warnBtn { background: var(--surface-2); color: var(--accent-gold); border: 1px solid var(--accent-gold); border-radius: var(--radius-sm); padding: 6px 12px; cursor: pointer; } .dismissBtn { background: none; color: var(--text-secondary); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 6px 12px; cursor: pointer; } .status { color: var(--text-secondary); font-style: italic; }"use client"; import { useState } from "react"; import styles from "./member-management.module.css"; type Member = { id: string; displayName: string; tier: string; siteRole: string }; export function MemberManagement() { const [search, setSearch] = useState(""); const [members, setMembers] = useState<Member[]>([]); const [loading, setLoading] = useState(false); async function doSearch() { setLoading(true); try { const res = await fetch(`/api/admin/members${search ? `?search=${encodeURIComponent(search)}` : ""}`); const data = await res.json(); setMembers(data.members ?? []); } finally { setLoading(false); } } async function setTier(id: string, tier: string) { await fetch(`/api/admin/members/${id}/tier`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ tier }) }); doSearch(); } async function setRole(id: string, siteRole: string) { const res = await fetch(`/api/admin/members/${id}/role`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ siteRole }) }); if (!res.ok) { const err = await res.json().catch(() => ({})); alert(err.error ?? "Failed"); return; } doSearch(); } return ( <div> <div className={styles.searchRow}> <input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search members…" onKeyDown={(e) => e.key === "Enter" && doSearch()} /> <button onClick={doSearch} disabled={loading}>Search</button> </div> <table className={styles.table}> <thead><tr><th>Name</th><th>Tier</th><th>Role</th></tr></thead> <tbody> {members.map((m) => ( <tr key={m.id}> <td>{m.displayName}</td> <td> <select value={m.tier} onChange={(e) => setTier(m.id, e.target.value)}> <option value="street">street</option><option value="block">block</option><option value="house">house</option> </select> </td> <td> <select value={m.siteRole} onChange={(e) => setRole(m.id, e.target.value)}> <option value="member">member</option><option value="moderator">moderator</option><option value="admin">admin</option> </select> </td> </tr> ))} </tbody> </table> </div> ); }.searchRow { display: flex; gap: 8px; margin-bottom: 14px; } .searchRow input { flex: 1; background: var(--surface-2); border: 1px solid var(--border); color: var(--text-primary); border-radius: var(--radius-sm); padding: 6px 10px; } .table { width: 100%; border-collapse: collapse; color: var(--text-primary); } .table th { text-align: left; color: var(--text-secondary); font-family: var(--font-mono); font-size: 0.75rem; padding: 6px; } .table td { padding: 6px; border-top: 1px solid var(--border); } .table select { background: var(--surface-1); color: var(--text-primary); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 3px 6px; }"use client"; import { useEffect, useState } from "react"; import styles from "./stats-panel.module.css"; export function StatsPanel() { const [stats, setStats] = useState<any>(null); useEffect(() => { fetch("/api/admin/stats").then((r) => r.json()).then(setStats); }, []); if (!stats) return <p className={styles.status}>Loading…</p>; const cards = [ { label: "Total members", value: stats.totalMembers }, { label: "Posts (7 days)", value: stats.postsLast7Days }, { label: "Pending reports", value: stats.pendingFlags }, { label: "Active Workshop projects", value: stats.activeProjects }, { label: "Open Mission Board needs", value: stats.openNeeds }, { label: "RPG characters created", value: stats.rpgCharacterCount }, ]; return ( <div> <div className={styles.grid}> {cards.map((c) => ( <div key={c.label} className={styles.card}> <p className={styles.value}>{c.value}</p> <p className={styles.label}>{c.label}</p> </div> ))} </div> <p className={styles.note}>{stats.marketplaceNote}</p> </div> ); }/* stats-panel.module.css */ .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 12px; } .card { background: var(--surface-1); border: 1px solid var(--border); border-radius: var(--radius-md); padding: 16px; text-align: center; } .value { font-family: var(--font-display); font-size: 1.8rem; color: var(--accent-gold); } .label { color: var(--text-secondary); font-size: 0.8rem; margin-top: 4px; } .note { color: var(--text-secondary); font-style: italic; margin-top: 16px; } .status { color: var(--text-secondary); font-style: italic; }/* admin-dashboard.module.css */ .page { max-width: 900px; margin: 0 auto; padding: 24px; } .tabs { display: flex; gap: 8px; margin-bottom: 20px; border-bottom: 1px solid var(--border); } .tab, .activeTab { background: none; border: none; color: var(--text-secondary); padding: 8px 14px; cursor: pointer; border-bottom: 2px solid transparent; } .activeTab { color: var(--accent-gold); border-bottom-color: var(--accent-gold); }import { db } from "@/lib/db"; import { blockPosts } from "@/lib/db/schema/block-posts"; import { getMemberTier, tierMeetsRequirement } from "@/lib/auth/roles"; import { eq } from "drizzle-orm"; type PostAccessResult = | { ok: true; post: { id: string; sectionKey: string; status: string; authorId: string; title: string } } | { ok: false; error: "not_found" | "insufficient_tier" | "not_published" }; // The single enforcement point for "can this viewer touch this specific // post" — used by every route that takes a raw post ID, so a gate added to // the list endpoint can never again be silently missing at the resource // level. This is the fix for Finding 1, applied structurally rather than // per-callsite. export async function assertCanAccessPost( postId: string, viewerId: string | null, { requirePublished = true }: { requirePublished?: boolean } = {} ): Promise<PostAccessResult> { const [post] = await db .select({ id: blockPosts.id, sectionKey: blockPosts.sectionKey, status: blockPosts.status, authorId: blockPosts.authorId, title: blockPosts.title }) .from(blockPosts).where(eq(blockPosts.id, postId)).limit(1); if (!post) return { ok: false, error: "not_found" }; if (requirePublished && post.status !== "published") return { ok: false, error: "not_published" }; if (post.sectionKey === "house") { const tier = viewerId ? await getMemberTier(viewerId) : "street"; if (!tierMeetsRequirement(tier, "house")) return { ok: false, error: "insufficient_tier" }; } return { ok: true, post }; }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError, getBlockViewer } from "@/lib/auth/roles"; import { createReplySchema } from "@/lib/block/post-schema"; import { assertReplyRateLimit, RateLimitedError } from "@/lib/block/rate-limit"; import { assertCanAccessPost } from "@/lib/block/section-access"; import { notifyMember } from "@/lib/notifications/notify"; import { db } from "@/lib/db"; import { blockPosts, blockPostReplies } from "@/lib/db/schema/block-posts"; import { and, asc, eq, isNull, sql } from "drizzle-orm"; const ACCESS_ERROR_STATUS = { not_found: 404, insufficient_tier: 403, not_published: 404 } as const; export async function GET(_req: NextRequest, { params }: { params: { id: string } }) { const viewer = await getBlockViewer(); // may be null — anonymous visitors are still subject to the house-tier check below const access = await assertCanAccessPost(params.id, viewer?.id ?? null); if (!access.ok) return NextResponse.json({ error: access.error }, { status: ACCESS_ERROR_STATUS[access.error] }); const rows = await db.select().from(blockPostReplies) .where(and(eq(blockPostReplies.postId, params.id), isNull(blockPostReplies.deletedAt))) .orderBy(asc(blockPostReplies.createdAt)); return NextResponse.json({ replies: rows }); } export async function POST(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const access = await assertCanAccessPost(params.id, viewer.id); // requirePublished defaults true — closes Finding 2 for replies if (!access.ok) return NextResponse.json({ error: access.error }, { status: ACCESS_ERROR_STATUS[access.error] }); const rawBody = await req.json().catch(() => null); const parsed = createReplySchema.safeParse(rawBody); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); try { await assertReplyRateLimit(viewer.id); } catch (err) { if (err instanceof RateLimitedError) return NextResponse.json({ error: "rate_limited" }, { status: 429, headers: { "Retry-After": String(err.retryAfterSeconds) } }); throw err; } const [reply] = await db.transaction(async (tx) => { const [r] = await tx.insert(blockPostReplies).values({ postId: params.id, authorId: viewer.id, body: parsed.data.body }).returning(); await tx.update(blockPosts).set({ replyCount: sql`${blockPosts.replyCount} + 1` }).where(eq(blockPosts.id, params.id)); return [r]; }); if (access.post.authorId !== viewer.id) { await notifyMember({ memberId: access.post.authorId, type: "reply", title: "New reply to your post", body: `Someone replied to "${access.post.title}"`, linkUrl: `/block/posts/${params.id}`, }).catch((err) => console.error("[replies:POST] notification failed, reply still saved", err)); } return NextResponse.json({ reply }, { status: 201 }); }import { NextRequest, NextResponse } from "next/server"; import { requireBlockAccess, AccessDeniedError } from "@/lib/auth/roles"; import { assertCanAccessPost } from "@/lib/block/section-access"; import { db } from "@/lib/db"; import { blockPosts, blockPostCheers } from "@/lib/db/schema/block-posts"; import { and, eq, sql } from "drizzle-orm"; const ACCESS_ERROR_STATUS = { not_found: 404, insufficient_tier: 403, not_published: 404 } as const; export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireBlockAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 401 }); throw err; } const access = await assertCanAccessPost(params.id, viewer.id); // closes Finding 1 (house gating) and Finding 2 (removed-content interaction) in one call if (!access.ok) return NextResponse.json({ error: access.error }, { status: ACCESS_ERROR_STATUS[access.error] }); const result = await db.transaction(async (tx) => { const existing = await tx.select({ id: blockPostCheers.id }).from(blockPostCheers) .where(and(eq(blockPostCheers.postId, params.id), eq(blockPostCheers.memberId, viewer.id))).limit(1); if (existing.length > 0) { const deleted = await tx.delete(blockPostCheers).where(eq(blockPostCheers.id, existing[0].id)).returning({ id: blockPostCheers.id }); if (deleted.length === 0) return { cheered: true }; await tx.update(blockPosts).set({ cheerCount: sql`${blockPosts.cheerCount} - 1` }).where(eq(blockPosts.id, params.id)); return { cheered: false }; } const inserted = await tx.insert(blockPostCheers).values({ postId: params.id, memberId: viewer.id }) .onConflictDoNothing({ target: [blockPostCheers.postId, blockPostCheers.memberId] }).returning({ id: blockPostCheers.id }); if (inserted.length === 0) return { cheered: true }; await tx.update(blockPosts).set({ cheerCount: sql`${blockPosts.cheerCount} + 1` }).where(eq(blockPosts.id, params.id)); return { cheered: true }; }); return NextResponse.json(result); }import { NextRequest, NextResponse } from "next/server"; import { requireHouseAccess, requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { workshopProjects, workshopVolunteers } from "@/lib/db/schema/workshop"; import { completeProject, WorkshopError } from "@/lib/workshop/volunteers"; import { and, eq, ilike } from "drizzle-orm"; async function isAuthorizedToComplete(projectId: string, memberId: string): Promise<boolean> { const [project] = await db.select({ createdBy: workshopProjects.createdBy }).from(workshopProjects).where(eq(workshopProjects.id, projectId)).limit(1); if (project?.createdBy === memberId) return true; const [leadRow] = await db.select({ id: workshopVolunteers.id }).from(workshopVolunteers) .where(and(eq(workshopVolunteers.projectId, projectId), eq(workshopVolunteers.memberId, memberId), eq(workshopVolunteers.status, "active"), ilike(workshopVolunteers.role, "lead"))) .limit(1); return !!leadRow; } export async function POST(_req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } // THE FIX (Finding 3): House tier alone is no longer sufficient. The // caller must be the project's creator, a volunteer with the "Lead" role, // OR a site moderator/admin acting on an operational basis — checked // explicitly rather than assumed from tier. const authorized = await isAuthorizedToComplete(params.id, viewer.id); if (!authorized) { try { await requireSiteRole("moderator"); } catch { return NextResponse.json({ error: "not_project_lead" }, { status: 403 }); } } try { await completeProject(params.id); return NextResponse.json({ status: "completed" }); } catch (err) { if (err instanceof WorkshopError) return NextResponse.json({ error: err.code }, { status: 404 }); console.error("[workshop/complete:POST]", err); return NextResponse.json({ error: "internal_error" }, { status: 500 }); } }import { NextRequest, NextResponse } from "next/server"; import { requireHouseAccess, requireSiteRole, AccessDeniedError } from "@/lib/auth/roles"; import { db } from "@/lib/db"; import { workshopProjects, workshopVolunteers } from "@/lib/db/schema/workshop"; import { and, eq, ilike } from "drizzle-orm"; import { z } from "zod"; const updateSchema = z.object({ progressPercent: z.number().int().min(0).max(100).optional(), missionStatement: z.string().trim().min(1).max(500).optional(), description: z.string().trim().min(1).max(3000).optional(), status: z.enum(["planning", "active", "paused"]).optional(), // "completed" is deliberately excluded — that transition only happens through /complete, which has its own reward side effects }).refine((d) => Object.keys(d).length > 0, { message: "at least one field required" }); async function isAuthorizedToEdit(projectId: string, memberId: string): Promise<boolean> { const [project] = await db.select({ createdBy: workshopProjects.createdBy }).from(workshopProjects).where(eq(workshopProjects.id, projectId)).limit(1); if (project?.createdBy === memberId) return true; const [leadRow] = await db.select({ id: workshopVolunteers.id }).from(workshopVolunteers) .where(and(eq(workshopVolunteers.projectId, projectId), eq(workshopVolunteers.memberId, memberId), eq(workshopVolunteers.status, "active"), ilike(workshopVolunteers.role, "lead"))) .limit(1); return !!leadRow; } export async function PATCH(req: NextRequest, { params }: { params: { id: string } }) { let viewer; try { viewer = await requireHouseAccess(); } catch (err) { if (err instanceof AccessDeniedError) return NextResponse.json({ error: err.reason }, { status: 403 }); throw err; } const authorized = await isAuthorizedToEdit(params.id, viewer.id); if (!authorized) { try { await requireSiteRole("moderator"); } catch { return NextResponse.json({ error: "not_project_lead" }, { status: 403 }); } } const parsed = updateSchema.safeParse(await req.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_body", details: parsed.error.flatten() }, { status: 400 }); const [updated] = await db.update(workshopProjects).set({ ...parsed.data, updatedAt: new Date() }).where(eq(workshopProjects.id, params.id)).returning(); if (!updated) return NextResponse.json({ error: "not_found" }, { status: 404 }); return NextResponse.json({ project: updated }); }